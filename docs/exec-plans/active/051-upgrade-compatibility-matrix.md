# 051 升级兼容性测试矩阵

## 状态与完整责任

- **状态**：已确认，待实施（下一步 S0）；待决问题已于 2026-09-25 由用户确认，见“已确认的决定”。
- **日期**：2026-09-25。
- **依据**：[Engine 测试架构](../../design-docs/testing-architecture.md)（nextest 负责进程调度、分组与超时，
  `uc-testkit::Scenario` 负责单场景预算、阶段、事件、资源与工件）；独立进程测试宿主 `uc-connectivity-host`；
  `scripts/testing/run-connection-recovery-e2e.sh` 已验证的做法：取旧版本源码、覆盖当前测试宿主、以小补丁适配后
  单独构建旧版宿主。049 收尾时确认：升级与跨版本互通目前只有单版本内的格式迁移测试，没有真实旧版本程序参与的验证。
- **完整负责人**：
  - 锚点版本清单（Desktop 版本 → Engine rev）与每个矩阵单元的期望结果：`tests/upgrade-matrix/` 下的清单与期望登记，
    本计划维护；新增 Desktop 发布时由发布流程追加。
  - 旧版宿主的获取、构建与缓存：构建脚本 `scripts/testing/build-upgrade-anchors.sh`。
  - 单个矩阵单元的生命周期（进程、资料目录、预算、阶段、事件、工件、清理）：矩阵测试台，内部持有
    `uc_testkit::Scenario`。
  - 矩阵展开、过滤、并发与总期限：cargo-nextest 与 `.config/nextest.toml`。
  - 本计划顺序与验收：本计划。
- **调用方唯一动作**：`bash scripts/testing/run-test-group.sh upgrade-matrix [--smoke] [nextest 参数]`；CI 通过同一入口。
  入口自行确保所需旧版宿主已构建。
- **成功结果**：每个矩阵单元的实际结果与期望登记一致，并留下 `result.json`、`summary.txt` 与汇总矩阵表
  （行 = 起始版本，列 = 目标版本，按维度分表）。
- **失败结果**：实际结果与登记不一致即失败，包括登记为“已知不兼容”却通过（须更新登记）。缺少旧版宿主或
  环境不可用的单元记为“跳过”，不得记为通过；失败保留首次失败分类、所在阶段、最后事件与双方宿主日志。
- **重启与重试责任**：测试台与 nextest 都不重试；被测的升级恢复、重试仍由产品各自负责人承担。

## 范围

### 锚点版本

第一版以 Desktop 各个 1.0.0 发布实际锁定的 Engine rev 为锚点（Desktop `Cargo.toml` 中 `uc-engine` 的 git rev），
不以 Engine tag 为准。只收录在 GitHub 上公开发布过的 Desktop 版本；未发布的 alpha.1、alpha.6、alpha.7 及
`-upstream-fix.*`、`-codex-*`、`-engine-rc*`、`-rc17fix.*` 变体不收录。

| 锚点 | Desktop 版本 | Engine rev | 最近 Engine tag | 数据库迁移数 |
| --- | --- | --- | --- | --- |
| A01 | v1.0.0-alpha.2 | `7a72f8dcd7be1b96bf312686841d1afbd7bc66a6` | v1.0.0-rc.1 | 48 |
| A02 | v1.0.0-alpha.3 | `90aff6b071a1466343fd1f301654ea89b5f98ce5` | v1.0.0-rc.6 | 48 |
| A03 | v1.0.0-alpha.4 | `49976d5ea21c5d448d9ef5df2c581b77abeb52cf` | v1.1.0-rc.2 | 53 |
| A04 | v1.0.0-alpha.5 | `b390487672caa97a57a28657b22d3900bf5631f1` | v1.1.0-rc.3 | 53 |
| A05 | v1.0.0-alpha.8 | `168a3ebd7c2701f2e9f8a0ebe0c4a0af4aa14543` | v1.1.0-rc.14 | 58 |
| A06 | v1.0.0-alpha.9 | `9149a875f389bfbf3c24dc9e3c9f7e50c517c07e` | v1.1.0-rc.14 之后的侧分支提交 | 58 |
| A07 | v1.0.0-alpha.10 | `f6f305d9689e4e79e7ab6d0e4921061f9416e4a6` | v1.1.0-rc.15 | 58 |
| A08 | v1.0.0-alpha.11 | `63a347fa2d6effed50f7f451e30d00ca64e6444b` | v1.1.0-rc.15 之后 | 58 |
| A09 | v1.0.0-alpha.12 | `fdae47290a39cf3aca212b989685a31480ae2506` | v1.1.0-rc.16 | 61 |
| A10 | v1.0.0-alpha.13 | `63cb50a49b47c828c288ae17fd931bec37f8911d` | v1.1.0-rc.16 之后 | 61 |
| A11 | v1.0.0-alpha.14、alpha.15 | `cca03181c2c650f66fedbc3228a927fa12bed512` | v1.1.0-rc.16 之后 | 61 |
| A12 | v1.0.0-alpha.16 | `a13ff9d78398f830a122f4042bafdb4e2a5acb05` | v1.1.0-rc.17 | 61 |
| A13 | v1.0.0-alpha.17 | `5e8d3d4322ca4eb83113f342068d4cb3297ea5eb` | v1.1.0-rc.18 | 61 |
| HEAD | 当前源码 | 被测提交 | — | — |

以上为 2026-09-25 从 Desktop 仓库 tag 与 GitHub 发布列表读取的结果，S0 以脚本重新生成并固定。同一 rev 被多个
Desktop 版本使用时只算一个锚点。共 14 个点（13 个旧 rev 与当前源码）。

### 矩阵维度

设锚点按发布顺序为 V1 < V2 < … < Vn，Vn 为当前源码。“单元”是一个维度下的一对（或一条链）版本。

| 维度 | 含义 | 展开方式 | 单元数（n = 14） |
| --- | --- | --- | --- |
| D1 单设备资料升级 | Vi 建立资料并写入代表性内容后退出，Vj 打开同一资料目录 | 全部有序对 i<j，外加完整链 V1→V2→…→Vn | 91 + 1 |
| D2 两台设备先后升级 | 两台 Vi 设备已配对并有历史；A 先升到 Vj 并与仍为 Vi 的 B 互通，B 再升到 Vj | 全部有序对 i<j，外加两台设备同步逐级走完整链 | 91 + 1 |
| D3 新旧设备混用 | 一台 Vi、一台 Vj 从零开始配对并互通；邀请方分别为旧版与新版 | 全部有序对 i<j × 两种邀请方向 | 182 |
| D4 降级回退 | Vi 升到 Vj 并写入新内容后，再用 Vi 打开同一资料 | 全部有序对 i<j | 91 |

每个单元内的动作与断言（全部经测试宿主的公开 Engine 操作，不读数据库、不依赖内部类型）：

- **写入（旧版）**：创建 Space、设置设备名，写入文本、文件与图片历史，修改同步偏好；D2/D3 另含配对、双向同步、
  移除后重新加入。
- **升级后核对**：启动成功（含启动升级进度终态），Space 可解锁，设备列表、成员状态与设备更新汇总符合预期，
  历史条目数量与内容摘要一致、文件可读，设置保留；随后继续执行新操作（发送、邀请、移除），证明升级后仍可用。
- **跨版本互通（D2 中段、D3）**：双向发送文本与文件，查询设备组选择，执行一次移除，双方在期限内收敛。
- **降级（D4）**：旧版必须明确报告需要处理（或在其能读懂资料时正常工作），且不改写资料；再次用 Vj 打开时
  内容与降级前一致。

每个单元的期望结果登记为“通过”或“已知不兼容（原因、链接到计划或 ADR）”。“已知不兼容”必须是产品已接受的
行为（例如旧版对端不支持的协议升级明确拒绝），不能用来掩盖缺陷。

### 运行方式

- **PR 冒烟**：只跑“上一个锚点 → 当前源码”（A13 → HEAD）的 D1、D2、D3（两种方向）与 D4，共 5 个单元。
- **夜间与手动**：全矩阵，约 457 个单元；按维度与起始版本分片并行，旧版宿主按 rev 缓存复用。
- 旧版宿主构建在仓库 `target` 下的可再生目录 `target/upgrade-anchors/<rev>/`，与 rc15 先例一致；
  入口提供清理命令，并在 CI 结束时回收。

### 非目标

- 不覆盖 Desktop 应用外壳、界面或 Desktop 自有存储；只覆盖 Engine 负责的资料与设备间行为。
- 不覆盖移动端（iOS、Android、HarmonyOS）实体设备；绑定层行为由各自宿主契约测试负责。
- 不覆盖 v1.0.0 之前的 Desktop 版本，也不以 Engine tag 为锚点（后续版本可另行扩展锚点来源）。
- 不修改产品代码来迁就测试；测试宿主只使用各锚点已有的公开 Engine 接口与 `dev-tools` 能力。
- 不在本计划中实现版本回退能力本身（计划 043 负责）；D4 只验证现状并固定期望。

## 设计要点

### 一个宿主源码，多个锚点构建

- 所有锚点共用当前的 `tests/hosts/connectivity` 源码与同一控制协议（stdin/stdout JSON）。构建某个锚点时，取该 rev
  的源码快照（`git archive`），覆盖当前宿主目录，应用 `tests/upgrade-matrix/anchors/<锚点>.patch`（仅在该 rev 的公开
  API 与宿主不一致时存在），以 `--no-default-features` 离线构建。补丁只允许改宿主与工作区成员声明，不得改被测 Engine。
- 宿主启动后报告自身锚点标识与支持的指令集合；测试台据此发现能力差异，缺少某项能力时该断言按登记处理，
  不静默跳过。
- 核实结果：13 个旧 rev 的 `uc-engine` 都有 `EngineConfig`、`HostCapabilities`、`HostDirectories`、
  `HostSecureStorage` 与 `dev-tools`，rc15（A07）先例只需 10 行补丁。

### 资料在进程之间传递

- 资料目录由测试台以 `TempDirLease` 持有，跨版本进程复用同一目录；安全存储沿用宿主已有的导出与导入指令，
  由测试台保存在单元目录内。测试资料全部是合成内容，工件不含真实用户资料。
- 新增宿主指令 `observe`：输出规范化的可观察状态（设备与成员状态、设备更新汇总、历史条目数与内容摘要、设置），
  只含测试合成数据的摘要，用于同一单元内升级前后比较。

### 测试台与报告

- 新测试 crate `tests/upgrade-matrix`（依赖 `uc-testkit`），矩阵在编译期由锚点清单展开为具名测试
  （如 `d1::a05_to_head`），nextest 按名称过滤、分片与设定期限。
- 每个单元一个 `Scenario`，阶段为“构建检查、旧版写入、升级、核对、继续操作、清理”；失败分类沿用 testkit。
- 汇总脚本把全部 `result.json` 汇成矩阵表工件（Markdown 与 JSON），标注通过、失败、已知不兼容、跳过。

## 切片

### S0 锚点清单与期望登记

- `scripts/testing/resolve-desktop-anchors.mjs`：读取 Desktop 仓库 tag 与 GitHub 发布列表，输出锚点清单
  `tests/upgrade-matrix/anchors.json`（Desktop 版本、Engine rev、发布日期、来源说明）；清单入库，脚本用于核对与追加。
- `tests/upgrade-matrix/expectations.json`：全部单元初始期望为“通过”。
- **验证**：脚本再次运行结果与入库清单一致；清单中每个 rev 在本仓库存在。

### S1 旧版宿主构建

- `scripts/testing/build-upgrade-anchors.sh [锚点…]`：按 rev 构建并缓存，记录每个锚点的构建耗时与补丁摘要。
- 逐个锚点构建，需要时新增最小补丁；同时让 rc15 旧有流程改用同一构建脚本，不保留两套做法。
- **验证**：13 个锚点全部构建成功；宿主启动报告的锚点标识与清单一致。

### S2 宿主协议补齐

- 宿主新增：锚点与能力报告、`observe`、按指定资料目录启动与正常退出；确认安全存储导出导入在所有锚点可用。
- **验证**：每个锚点上执行“启动 → 写入 → observe → 退出 → 重启 → observe”，前后一致。

### S3 矩阵测试台与 D1

- 建立 `tests/upgrade-matrix` 与 nextest 分组 `upgrade-matrix`，实现 D1 全部有序对与完整链。
- **验证**：D1 全部单元跑完；首轮暴露的不一致逐项判定为测试问题或产品问题，产品问题记录根因后另行决定修复
  方式，不在本切片顺手修改，也不通过修改登记掩盖。

### S4 D2 与 D3

- 实现先后升级与新旧混用；互通断言以期限内收敛为准，不以固定等待代替。
- **验证**：同 S3。

### S5 D4 降级回退

- 按“已确认的决定”第 1 项固定期望，实现并登记。
- **验证**：同 S3；每个单元都证明再次升级后内容与降级前一致。

### S6 CI

- PR 检查增加冒烟 5 个单元；夜间与手动工作流运行全矩阵，上传矩阵表、JUnit 与失败单元工件，缓存旧版宿主。
- **验证**：冒烟本地连续运行 10 次全部通过；全矩阵在本地或 CI 完整跑通一次并记录总耗时。

### S7 文档与发布流程

- 更新 `docs/design-docs/testing-guide.md`、`testing-architecture.md` 与测试采用清单；在发布流程中加入
  “新 Desktop 发布后追加锚点并跑一次全矩阵”。
- **验证**：交付前检查全部通过。

## 验收

1. 锚点清单与 Desktop 已发布版本一一对应，可由脚本重新生成。
2. 13 个旧锚点宿主全部可构建；缓存可清理，构建目录不残留在仓库 `target` 之外。
3. D1–D4 全部单元执行完毕，每个单元的实际结果与登记一致；登记中的“已知不兼容”都有原因与链接。
4. PR 冒烟连续 10 次通过；全矩阵完整运行一次并记录耗时与分片方式。
5. 汇总矩阵表作为工件产出，未执行单元明确记为“跳过”。
6. 交付前检查：

```bash
cargo metadata --locked --format-version 1
cargo check --workspace --all-targets --locked
cargo fmt --all -- --check
node scripts/architecture/check-rust-style.mjs
node scripts/architecture/check-engine-repository.mjs
git diff --check
bash scripts/testing/run-test-group.sh upgrade-matrix --smoke
```

## 风险

| 风险 | 应对 |
| --- | --- |
| 13 个旧 rev 完整构建耗时长 | 按 rev 缓存；PR 只构建 A13；夜间复用缓存，只在清单变化时重建 |
| 旧 rev 的公开 API 与当前宿主有差异 | 每个锚点一个最小补丁，只改宿主；补丁变大说明需要能力分层，先记录再决定 |
| 旧 rev 依赖的外部服务（rendezvous、relay）不可用或已变化 | 测试一律使用本地直连与本地 relay 宿主，不访问外部服务；无法关闭外部依赖的锚点记为跳过并登记原因 |
| 首轮全矩阵暴露大量不一致 | 先判定并登记，产品问题另行立项，不在测试计划里修产品 |
| 全矩阵夜间耗时过长 | 按维度与起始版本分片；若仍超预算，先报告实测数据再决定是否收缩组合 |

## 已确认的决定

2026-09-25 由用户确认：

1. **D4 降级的期望结果**：旧版明确报告需要处理且不改写资料；再次用新版打开后内容完整。旧版能读懂资料时
   正常工作同样视为通过。计划 043 完成版本回退后，按其结果更新登记。
2. **未公开发布的 Desktop 构建**：alpha.1、alpha.6、alpha.7 及各类变体未交付给用户，不纳入锚点。
3. **单元内容范围**：文本、文件与图片历史、同步偏好，以及配对、双向同步、移除与重新加入已足够，不另加大文件、
   大量历史或多 Space 切换。
