# Progress

## 2026-08-12

- 接管并确认 `v0.19.0` 构建会话成功结束。
- 建立本次独立的跨版本验收计划。
- 审计两代测试宿主，确认旧宿主启动时清理资料，当前宿主可重启但默认使用内存安全存储。
- 用正式 0.19 便携程序创建真实单机 Space，并让当前 Engine 原地接管；解锁和设备身份读取成功，但新成员状态为空，旧空间安全提升失败。
- 开始双机旧版验收；公共配对服务订阅失败，改走同版本的隐藏本地地址邀请工具。
- 用固定网络端口成功建立一组双机配对，但发现默认 profile 令同机两个长期后台争用同一本地控制端口。
- 尝试在配对后切换 A/B profile，确认 profile 会改变网络身份并导致 A 出现重复自身记录；废弃该组临时资料，改为从初始化起固定 profile 与网络端口。
- 从初始化起固定 A/B profile 与网络端口，重新完成正式 0.19 双机配对；双方成员列表均显示对方在线。
- 完成 0.19 双向内容基线：A -> B、B -> A 的唯一文本都从接收端历史实际读回。
- 首次单边升级确认数据库和安全会话可恢复，但 A 的网络身份被替换；根因收窄为运行节点错误使用主安全存储，忽略已接管的 0.19 身份目录。
- 开始以完整生产启动入口补回归测试：预置 0.19 格式身份，要求当前版本启动和重启均复用原文件，且不向主安全存储写入第二把网络身份。
- 完整启动回归先按旧接线失败，确认首次启动会在主安全存储生成第二身份；修正后首次启动和重启都复用 0.19 原身份，测试通过。
- 配置迁移相关 32 项库测试通过；网络身份继续按独立目录随配置迁移，主安全资料包只携带当前 KEK。
- 新建干净双机验收根目录 `/tmp/uc-v019-v11-e2e.FRQaTn`，准备从零建立第二组正式 0.19 资料。
- 用 `/tmp/uc-v019-v11-e2e.B00X7g` 完成第二组正式 0.19 双机配对和双向内容基线。
- 修复运行节点未复用 0.19 独立网络身份的问题；生产启动回归与 32 项配置迁移测试通过。
- 身份修复后重跑单边升级：A 保持原身份并恢复会话，但未保存 B 的升级提示，约 68 秒超时。
- 将反馈环收窄到 `WorkspaceConvergence` 运行入口：下一步先用局部红测覆盖“启动时成员已经在线但没有新的上线事件”。
- 首次局部测试筛选因缺少完整模块路径实际执行 0 项；确认 79 秒来自首次重编译，不能算测试结果。
- 用完整模块路径跑出准确红测：1 项执行、1 秒超时，证明负责人启动时没有核对已保存成员。
- 在负责人启动入口复用现有逐成员核对流程；同一回归测试转绿，1 项通过、0 项失败。
- 相关 22 项负责人测试全部通过，包含连接失败、当前入口拒绝、提示持久化和清除边界。
- 重跑真实单边升级仍失败：1 项执行、68.31 秒超时；日志确认启动核对早于资料解锁，解锁后未补跑。
- 将核对纳入统一会话恢复活动；时序回归 1 项、会话生命周期 4 项、版本边界 22 项通过。
- 第三次真实单边升级仍失败：1 项执行、68.24 秒超时；锁定错误消失，进一步定位为 0.19 资料没有新成员历史，候选选择为空。
- 新增真实迁移形态红测：无 1.1 历史、仅有旧配对地址时恢复核对，旧实现 1 项超时，修复后 1 项通过。
- 负责人 22 项回归再次全部通过。
- 第四次真实单边升级通过：1 项执行、17.47 秒完成，A 已持久保存 B 需要升级。
- 扩展场景已证明 A 重启后提示仍在、A 到 B 的内容未进入 B 历史；B 到 A 发送正确报告目标离线，但测试错误要求 CLI 成功退出，已改为断言具体离线报告。
- 恢复旧空间安全提升的生产网络入口和后台生命周期；生产组装测试同时确认当前认证、当前成员历史和旧空间提升入口存在，废弃的旧成员移除入口不存在。
- 完成升级后唯一成员历史初始化：稳定发起端建立起点，另一端提交自己签名的当前身份资料；局部两设备回归通过。
- 为正式验收启动增加仅测试可用的固定网络端口覆盖，并扩展同一跨版本 case，使其核对目标 B 进程目录、正常停止 0.19 B、原地启动 1.1 B。
- 正式双机完整场景通过：A 提示跨重启保留，混合版本双向内容暂停；B 原地升级后提示清除、两端保存同一两成员历史，双向内容均从接收端历史读回。1 项通过、0 项失败，33.57 秒。
- 纠正 ADR 和架构文档中“删除旧自动流程”的错误表述，明确旧内容同步不保留，但旧空间安全提升必须保留且不成为第二份成员事实。
- 最终定向验证通过：统一负责人 23 项、旧空间安全提升 9 项、生产接线 1 项；正式双机完整场景此前已通过 1 项。
- 仓库交付检查全部通过：依赖清单、整个工作区检查、格式检查、架构规则检查和差异完整性检查。工作区仍有已有警告，未记为零警告。
- 本次目标 B 的 0.19 进程和本次完整场景测试进程均已退出。另一组旧版 B 和此前遗留的其他测试进程不属于本次验收，保持不动。
- 发布打包和设备矩阵跳过：本次未涉及发布，且验收范围是本机正式 0.19 双进程原地升级，不把未执行项目记为通过。
