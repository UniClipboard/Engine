# Progress

## 2026-08-02

- 完成现状审计和删除检查。
- 开始并行比较最小入口、业务拆分和常用流程优先三种设计。
- 确认现有 `EncryptionFacade` 与 `SpaceSetupFacade` 存在职责重叠。
- 确认网络注册是必要装配步骤，但运行期顺序可完整下沉到应用层。
- 完成三种候选方案比较并选定混合方案。
- 写入完整接口、状态、依赖归属、两阶段网络装配、错误语义、迁移顺序和验收条件。
- 成员自动配对成果已作为独立 checkpoint `704471d` 提交，重构设计未混入该提交。
- 将本目录切换为活动计划，并把设计迁移顺序展开为八个可独立验收的实施阶段。
- 当前进入 Phase 1；下一步先通过稳定 Engine 入口固定现有行为，不先改生产结构。
- 重新载入活动计划并核对仓库状态；会话恢复脚本未发现遗漏内容。
- 确认本轮只推进 Phase 1：先补稳定 Engine 行为测试，不提前改生产结构。
- Phase 1 基线通过：`space_membership_auto_pairing_e2e` 实际完成多引擎创建、首次加入、锁定、恢复、成员收敛、双向发送、重启和关闭（1 passed，76.15s）。
- 新增稳定 `JoinSpace` 自动路由场景：同一设备先首次加入空间，再加入另一空间触发切换，并固定空设备名错误码；正式代码未改动。
- 为稳定锁定/解锁/恢复流程增加接收状态断言：成功锁定后不可接收，成功解锁或恢复后重新可接收。
- 聚焦综合 Engine 生命周期测试通过（1 passed，6.88s）：创建后可接收、锁定后不可接收、口令解锁后恢复，并继续完成工厂重置、重建、暂停/恢复和关闭。
- 完整空间成员端到端测试通过（2 passed，75.40s）：原有锁定/恢复/成员收敛/双向发送场景及新增首次加入/自动切换场景全部通过。
- 成员后台生命周期测试通过（2 passed）：暂停会结束在途网络轮次，恢复后继续工作，关闭会等待任务结束。
- LAN 隔离测试通过（4 passed），仓库架构检查也用负例确认自动 P2P-to-LAN fallback 会被拒绝。
- 全部交付检查通过：锁定依赖元数据、全工作区全部目标检查、格式检查、架构检查和改动检查均成功；仅有一条既有未使用导入提醒。
- Phase 1 完成。当前生产锁定动作不会失败，无法在不加入测试专用后门的情况下触发失败补偿；该验收已移到 Phase 2 新协调者的首个可注入失败测试。
- 开始 Phase 2：重新核对应用入口、旧空间入口和成员传播装配顺序，确定先拆分成员活动控制句柄，再把完整会话动作收进应用层。
- Phase 2 首个 TDD 循环完成：锁定失败补偿测试先因负责人缺失失败，随后最小实现通过；当前顺序为成员暂停、接收关闭、锁定失败、接收恢复、成员恢复。
- 激活顺序 TDD 完成：测试先因搜索活动接口缺失失败，随后固定为搜索恢复、接收恢复、成员传播恢复。
- 成员活动句柄拆分 TDD 完成：现有运行期测试先因 `activity()` 缺失失败，随后控制句柄与唯一任务所有者分离并通过原有暂停/恢复/关闭测试。
- 新增 Engine 分发职责红灯检查，已确认当前仍因 `pause_membership_gossip` 直接调用而失败；下一步将完整会话动作接入应用层后转绿。
- Phase 2 完成：应用层会话负责人统一执行解锁、恢复、锁定和失败补偿；Engine 分发已删除成员传播的单项控制和接收准备编排。
- Phase 2 聚焦验证通过：应用会话测试 4 条、Engine 分发职责检查、真实 Engine 创建/锁定/解锁/恢复/重置/暂停/关闭场景。
- 开始 Phase 3：下一步将成员传播任务和配对入站任务收进应用运行期，并保持应用任务先于共享网络节点关闭。
- Phase 3 完成：新增应用运行期统一持有成员传播任务和配对入站任务；Engine 不再直接持有或查询成员传播对象，关闭顺序保持应用任务先于共享网络节点。
- Phase 3 验证通过：成员运行期暂停/恢复/关闭测试、真实 Engine 生命周期、全部依赖防火墙检查。
- Phase 4 完成：应用层统一校验并保存设备名、读取 setup 状态、自动选择首次加入或切换，并在成功后恢复会话活动；Engine 的 Fresh/Switch 模式和接收恢复分支已删除。
- Phase 4 三 Engine 场景通过：首次加入、再次切换、成员收敛、锁定恢复和双向传输保持正常。
- 开始 Phase 5：将邀请签发、取消、兑换、完成订阅和入站任务统一交给配对负责人。
- Phase 5 完成：邀请签发、指定地址签发、地址列表、取消、兑换、自动加入、切换和完成订阅统一通过 `SpaceAdmissionCoordinator`；Engine 不再直达内部空间对象。
- Phase 6 完成：在线维护及其退避任务移入应用层成员运行期，Engine 只转发在线状态事件；空间模块中的统计身份重置入口已删除；测试装配改用分组依赖。
- Phase 7 完成：唯一入口已切换为 `SpaceFacade`，旧 `SpaceSetupFacade` / `SpaceSetupDeps` 全仓清零；依赖拆成会话、配对和迁移三组；`EncryptionFacade` 只保留状态查询和安全存储检查。
- 创建、解锁、恢复、锁定、加入和工厂重置均已改为 Engine 单次调用；应用层负责活动恢复、静默和失败补偿。
- 13 条依赖防火墙检查通过，覆盖旧类型、重复生命周期、加入路由、成员任务所有权、在线维护、创建和工厂重置职责。
- 开始 Phase 8：运行稳定契约、三组真实 Engine 自动化、全仓测试和交付检查。
- Phase 8 第一组真实 Engine 综合场景通过（1 passed）：创建、锁定、解锁、重置、暂停恢复和关闭均完成。
- 成员端到端测试首次未启用 `dev-tools`，实际运行 0 条，不计通过；已确认正确重跑需启用该开关。
- 启用 `dev-tools` 后，首次加入与已有设备自动切换空间场景通过（1 passed，5.90s）。
- 三 Engine 离线成员发现、锁定恢复、重启收敛和双向传输场景通过（1 passed，75.81s）。
- 全仓所有目标测试通过；公开契约 41 条、应用层 774 条、职责防回归 13 条均通过，原有网络和性能环境项目按配置忽略。
- 锁定依赖元数据、全工作区全部目标检查、格式检查、架构检查和改动检查通过；仅有一条既有未使用导入提醒。
- 最终差异核对确认稳定操作、返回结果和错误编号保持不变；同时修正维护说明中残留的一处旧入口名称。
- iOS 实体设备：跳过，不计通过。
- Android 实体设备：跳过，不计通过。
- HarmonyOS 实体设备：跳过，不计通过。
- Phase 8 完成，Phase 0-8 全部完成。

## 2026-08-03

- 重新按完成标准审计当前工作区，计划检查器确认 9/9 阶段完成。
- 职责防回归检查 13 条、公开契约检查 41 条全部通过。
- 真实 Engine 综合生命周期场景通过（1 passed，8.89s）。
- 启用 `dev-tools` 后，两条多设备加入、切换、离线发现、重启收敛与双向传输场景全部通过（2 passed，76.04s）。
- 全仓所有目标测试通过；锁定依赖元数据、全工作区全部目标检查、格式检查、架构检查和改动检查全部通过。
- 公开 operation、result、error code 定义文件无改动；旧入口、旧加入路由类型和 Engine 侧成员生命周期控制在生产代码中均不存在。
- 仅保留一条既有未使用导入提醒；外部网络、性能及实体设备项目仍按原记录跳过，不计通过。
- `origin/main` 的 rc.17 发布提交未合入；另一个未跟踪计划目录未改动。
