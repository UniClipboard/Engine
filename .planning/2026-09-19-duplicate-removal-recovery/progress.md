# Progress

## 2026-09-19

- 定义完成标准、流程负责人、测试边界和禁止事项。
- 读取故障诊断、测试先行、Engine Rust 开发和文件化计划规则。
- 检查工作区：21 个既有修改文件，均需保留；未开始新的生产代码修改。
- 读取根及相关局部 AGENTS、ARCHITECTURE 和 Rust 编写规范。
- 建立本任务独立计划，下一步读取成员移除/效果恢复设计与现有 E2E 夹具，并先写完整红测。
- 找到既有相邻恢复计划、成员移除负责人、持久效果恢复器和 Infra 安全资料应用入口；尚未修改生产代码。
- 完整阅读成员历史职责、离线优先移除决定、相邻邀请恢复计划和 Space Application 恢复阶段；确定正式 Remove 事实不可回滚，修复必须让持久安全效果幂等完成或诚实暴露失败。
- 检查现有 Engine E2E 和 Remove 安全路径：重新加入已有“替换全部旧叶”，正常移除仍走单一撤销入口；下一步先用正式操作制造等价异常状态并跑红。
- 已形成四个可证伪假设并向用户说明。源码直接显示单次撤销只选择第一个重复叶，下一步建立旧版行为夹具与完整红测确认这是首个失败点。
- 评估了完整场景夹具：现有测试没有异常状态入口；计划仅在 dev-tools/test-util 下模拟旧版准入，避免修改数据库或放宽生产行为。
- 已先加入快速红测，直接要求正常移除清掉同设备的全部旧安全身份；当前实现只删一个，测试正在共享构建目录中执行。
- 快速红测稳定失败：预期残留 0，实际残留 1，确认首个底层失败点。
- 只读检查真实控制库确认撤销记录处于已本机激活、仍待分发确认状态；未读取或输出加密业务资料，也未修改现场。
- 已加入 dev-tools/test-util 专用旧版异常资料夹具和完整 Engine 回归：正常移除后必须无旧身份残留，重启后可邀请并完成新设备加入。尚未修改正式移除逻辑。
- 完整场景修复前稳定失败：设备列表已显示移除，但三个旧身份只撤掉一个，安全资料仍残留两个。
- 正式移除改为按用户选择的设备一次撤销其全部旧身份；不改其他设备、不清历史、不放宽校验。快速测试和完整 Engine 场景均已转绿，后者包括进程重启、重新生成邀请并完成下一台设备加入。
- 设备列表查询正在补齐一致性：成员效果尚未完成时显示“不可用/处理中”，只有收尾 Activated 后才显示“已移除”。
- 列表一致性测试已通过：未完成收尾时不再显示“已移除”，收尾完成后仍按原规则显示移除。
- 多旧身份一次清理、目标已不存在时重复收尾、重启后重建收尾、普通成员移除四项 Infra 回归均通过。
- 现有“切换空间后同设备返回并重启”完整场景通过；现有“离线成员连续移除后可新邀请”完整场景通过。
- 另一项既有“确认配对后重启，再移除并重新加入”场景在尚未执行移除之前等待配对确认超时；需与本轮移除修复分开记录并继续检查其既有恢复告警。
- 成员管理完整测试 136 项中 135 项通过、1 项按仓库设置跳过；更新了一处同类旧断言，未完成收尾时统一显示处理中。
- 仓库要求的 metadata、全目标编译、格式、Rust 规则、架构边界和差异检查全部通过；全目标编译只有一条既有 HarmonyOS 测试未使用导入警告。
- 临时探针搜索无残留；架构圣经和可靠撤销设计已更新。
- “确认配对后重启，再移除并重新加入”干净复核通过；此前两次在移除前超时保留为不稳定信号，不归因于本轮移除修复。
- 已重写交付报告并纠正 `pending_count=0` 的过强解释。真实 Android/Linux 未操作，原任务保持未完成。
