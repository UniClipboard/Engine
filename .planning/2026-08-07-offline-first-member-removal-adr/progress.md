# 进度

- 2026-08-07：确认现有活动计划属于其他工作，创建独立计划目录且未修改 `.planning/.active_plan`。
- 2026-08-07：完成现有 ADR、架构总览、OpenMLS 能力和历史恢复约束核对。
- 2026-08-07：开始将 ADR-015 修订为离线优先的分布式移除意图。
- 2026-08-07：完成 ADR-015 主决策复核；确认需要将详细稳定行为和验收矩阵独立写入 `docs/specs`。
- 2026-08-07：补充两个关键边界：已移除发起者的受限迟交入口，以及迟到合法意图可重新打开已经完成的收敛轮次。
- 2026-08-07：新增 `docs/specs/015-offline-first-member-removal.md`，以 67 个场景定义基本行为、用户给定链式场景、并发、迟交、验证、恢复、隐私和跨平台验收。
- 2026-08-07：ADR-015 改为只记录决策与取舍，详细稳定行为和矩阵由技术说明作为唯一来源；同步更新 ADR-011、文档索引和架构总览。
- 2026-08-07：`cargo metadata --locked --format-version 1`、`cargo fmt --all -- --check`、架构仓库检查和 `git diff --check` 通过。
- 2026-08-07：`cargo check --workspace --all-targets --locked` 未通过；现有 `uc-mobile-probe-core` 未覆盖共享设备刷新结果/事件，且 `crates/uc-engine/src/assembly/host.rs` 测试调用仍多传一个生命周期参数。本次只改文档，未修改这些无关代码。
- 2026-08-07：开始生产实现。完成 ADR-015 实现前验证：新增 `tests/openmls-validation/tests/adr015_fork_recovery.rs` 四个测试全部通过。验证结论：`propose_removals` 可对多目标生成单一向前提交；被移除成员无法导出/处理新状态；`recover_fork_by_readding([执行者]) + provide_key_packages(仅有效成员)` 可让确定执行者从分叉集合生成恢复资料，保留成员经 welcome 导出相同秘密；未参与分叉的成员可线性追赶后按序进入恢复或被移除；分叉提交互相拼接失败(WrongEpoch)。
- 2026-08-08：用户要求实现说明。工作区已有未提交实现，下一步从当前源码和完整测试结果重新建立缺口清单，不能用此前文档完成状态替代实现验收。
- 2026-08-08：`cargo test -p uc-application member_removal --locked` 当前 20 通过、2 失败。失败测试都错误地让已被移除设备通过普通入口发送；同时发现普通入口没有在应用层拒绝未知来源，下一步先以失败测试固定该安全边界。
- 2026-08-08：新增“未知设备不得从普通入口取得 key package”测试，先复现其能得到资料的失败；普通入口现先确认当前成员实例，再判断本机是否已移除该实例，并优先使用当前视图避免旧实例误判重新准入设备。按说明改正两个链式场景的通信路径后，`cargo test -p uc-application member_removal --locked` 23 项通过。
- 2026-08-08：确认 ADR-015 规格已包含完整实施顺序、运行时闭环和 67 项验收场景；实际实现仍缺少重启中断和独立三设备验证，继续补齐而不把现有局部测试当作完成证明。
- 2026-08-08：真实双端 iroh 测试通过，普通交换和受限迟交已分别到达对应接收入口。下一步收紧其内部职责，避免普通与受限通道互相携带无关依赖，并清除设备编号日志。
- 2026-08-08：先将真实双端测试改为要求一次生成两条通信路径，确认编译失败后完成最小整理。普通路径不再持有受限提交依赖，受限路径不再持有成员校验或普通交换依赖；拒绝日志不再记录设备编号。重跑真实双端测试通过。
- 2026-08-08：R03 的测试先失败，证明执行者持久状态缺少可重启安装的私有检查点。已将恢复资料与私有检查点作为同一份加密状态先保存，再安装本机安全状态；目标测试通过。下一步用注入失败验证重启不会生成第二份资料。
- 2026-08-08：R03 注入“本机安装失败”后重启测试通过：重启复用原私有检查点，不会生成第二份恢复资料。完整成员移除应用层测试现为 25 项通过。
- 2026-08-08：补齐 R01 与 R12 的应用层重启验证：首次发送前退出后重启传播相同意图；通知前退出后新运行时发布已保存的完整状态。两项定向测试均通过。
- 2026-08-08：三设备 e2e 修复完成确认传播与旧快照覆盖竞态：后台推进改为锁内决策/锁外网络/锁内应用，本机有效性与执行者判定按设备而非实例，完成投递按发送计划成员实例键记录，完成判定要求恢复摘要与当前意图摘要一致。单移除与连续移除三设备测试各 6-8 次连过；应用层成员移除测试 38 项通过，补 C01/C03/R07 直接验证。
- 2026-08-08：既有断线恢复测试 `offline_members_learn_about_each_other_and_keep_syncing_after_restart` 在本机稳定超时，且已在干净基线 HEAD 上复现相同失败（依赖公网 iroh relay，本机环境不可达），确认为既有环境问题，非本次改动回归，单独记录待处理。
