# t-0010 唯一空间负责人进度

## 2026-09-20

- 已运行 planning-with-files session catchup；没有发现未同步会话输出。
- 已读取 planning-with-files、to-spec、implement、tdd、uni-rust-development 技能说明。
- 已确认测试边界由用户验收要求给定：空间负责人维护入口与入站成员历史入口。
- 已创建独立规划目录 `.planning/2026-09-20-t0010-single-space-owner/`。
- 已阅读根架构、执行计划约定、核心信念、工程原则、Rust 规范和错误处理规则。
- 已确认正式计划位置为 `docs/exec-plans/active/`，跨层恢复责任必须在 active plan 中落盘。
- 已阅读安全、Application 分层、Space 代码地图、成员历史职责和相关历史计划。
- 已确认新负责人应位于 Application；保留准入聚合和成员 ledger 各自事实所有权，只统一运行资格与调度。
- 基线检查发现一个此前已开始但未完成的 rebase；它会改写用户要求保留的 `2d994c8a`。已记录，下一步终止该 rebase 后重新核对分支。
- 已终止该 rebase，分支恢复为 `hp/uni/t-0010-android`，HEAD 精确为 `2d994c8a4fb971e7c651d87c6b0f702b36458a4e`；原提交未改写。
- 恢复后除本任务独立规划目录外，Git 工作区没有其他未提交文件。
- 当前阶段：Phase 0 规格、计划和基线。
- 尚未修改生产代码，尚未运行 Cargo 测试。
- 用户随后明确要求先 rebase；已 fetch `origin/main` 并重放原提交。第一次尝试被外部操作中止；第二次仅合并架构圣经的两处维护记录后成功。新 HEAD `d64a9046`，父提交 `af25532c`。
- 已检查 `origin/main` 是祖先、重放差异无空白问题、冲突标记已清除；独立规划目录仍是唯一未跟踪内容。继续 Phase 0。
- 已核对最新 active plan 约定、成员历史与 Space Application 现有边界，并确认入站历史独立于后台维护；规格需要同时覆盖两个入口。
- Phase 0 完成：正式规格已加入 `docs/exec-plans/active/2026-09-20-single-space-work-owner.md`，索引和架构圣经维护记录已同步。计划将 Phase 1 标为进行中。
- 已定位第一个红灯 seam：维护用例收到“未终结配对、本轮暂时失败”后，现有实现仍继续全部普通维护步骤。
- 已新增红灯测试 `unfinished_pairing_with_transient_network_failure_excludes_ordinary_maintenance`。
- 定向执行结果符合预期：测试失败，实际调用 `admissions, restricted, effects, conflicts, group_updates, synchronize, cleanup`，期望只有 `admissions`；共 960 项被过滤，红灯耗时 0.01 秒。
- 已按阶段性交付要求更新 `.herdr-project/uni-t-0010/report.md`，记录规格路径、六个阶段和首个红灯证据。
- Phase 1 最小实现已开始：新增从持久恢复摘要派生的 `SpaceWorkMode`，删除 `Continue/Yield` 枚举语义；Pairing 与 NeedsAttention 均阻止普通维护。
- 首个红灯已转绿：1 项通过，960 项过滤；最终确认暂时失败只记录准入步骤。
- 第二个红灯固定了 `PeerContact` 绕行：当前实际只调用 `synchronize, cleanup`，没有先裁决 Pairing。移动裁决后该测试转绿。
- 首次运行 19 项维护测试时，1 项旧断言仍期望 PeerContact 不经过准入裁决；实现行为正确，已把断言更新为 `admissions, synchronize, cleanup`，等待复测。
- 维护测试整组复测通过：19 项通过，0 失败；其中两次预期 panic 由运行期失败保留测试捕获，测试结果为通过。
- 准入恢复定向测试通过：27 项通过，0 失败，覆盖离线认证、取消、升级阻塞、空间切换、最终结算和重启恢复。
- Phase 1 完成：持久记录派生 Pairing/Active/NeedsAttention；所有后台触发（含 PeerContact）先经过同一裁决；Pairing 暂时失败不运行普通维护，终态后的下一轮恢复完整维护。
- Phase 1 最终定向验证：维护测试 20 项通过；持久 pending join 模式测试 1 项通过；未新增持久字段或协议消息。
- 当前进入 Phase 2：先为 Pairing 下的入站成员历史写红灯测试。
- 已定位 Phase 2 的公开 seam：网络入站由 `MembershipHistoryAntiEntropy` 转入 `HandleMembershipHistoryMessageUseCase`，目前与准入协议分开装配且会直接读写 ledger。
- 已确认最终实现不能只做一次模式查询；裁决与消息处理需要处于同一个空间串行边界内，避免查询后配对状态改变。
- 已核对 Core 传输错误：当前没有“配对进行中”的可重试分类，应用失败还会统一变成永久拒绝；Phase 2 将先用公开网络端点测试固定正确结果，再调整契约与装配。
- 已列出准入并发约束：本地动作与入站 Sponsor 消息共享动作锁，后台恢复有独立可中断锁。为避免一次性重写，Phase 2 先建立可测试且可删除的持久模式查询适配，Phase 4 再收口为单一事件负责人。
- 已确定可重试网络兼容方案：在现有 framing 增加 BUSY 响应；旧客户端会按传输失败重试，新客户端得到明确配对占用结果，双方都不会继续写入历史。
- Phase 2 首个红灯已运行：`pairing_rejects_inbound_history_as_retryable_before_ledger_access` 失败，当前实际 `Err(Rejected)`，期望 `Err(PairingInProgress)`；963 项过滤，耗时 0.26 秒。测试还要求 ledger 读取和提交均为零。
- Phase 2 入站门禁已转绿：同名测试 1 项通过，配对时在 ledger 读取前返回 `PairingInProgress`；模式从现有持久准入恢复记录派生。
- 网络兼容红灯 `history_busy_status_is_retryable_for_new_clients` 先实际得到 `Transport`、期望 `PairingInProgress`；加入 BUSY framing 后转绿。新版明确延期，旧版把未知字节按传输失败延期，不会误作永久拒绝。
- 入站历史整组 21 项通过；出站历史整组 15 项通过。核对后确认其中的 RelationshipUnconfirmed 是正式提交后的正常历史核对，不是候选传播。
- 后台维护整组 20 项通过、1 项独占日志测试按标记跳过；准入协议整组 65 项通过，覆盖取消中断、重启恢复、最终确认迟到/重复、提交竞态与旧版本升级拒绝。
- Phase 3 标记完成：直接复用并复核当前分支已有 `d64a9046` 的最终确认唯一提交与三设备不可见测试，不重复实现。
- 严格自查发现“查询模式后再处理”的竞态；已改为持有贯穿完整动作的共享许可。本地加入/取消/完成、Sponsor 入站、后台维护和入站历史共用同一串行边界，同时保留后台离线恢复可被取消中断的既有能力。
- 并发回归 `ordinary_work_permit_serializes_a_new_pairing_request` 通过：普通动作结束前新配对不落库，许可释放后请求继续。
- 三设备 E2E `pending_join_is_not_published_before_final_confirmation` 通过（55.98 秒）：Sponsor 重启、第三设备在线并主动联系时仍看不到候选，最终确认后才三端收敛并完成双向内容传输。
- `uc-application` 全部库测试通过：964 项通过、1 项按标记跳过、0 失败，耗时 22.32 秒；覆盖本轮共享许可、准入恢复、历史收发和普通维护，也覆盖其他 Application 回归。
- 已进入 Phase 4 的统一唤醒切片：定位到 Runtime 仍向普通维护泄露 PeerOnline/PeerContact 及设备身份，下一步先把重复联系人测试改为只允许一个通用 StateChanged 轮次。
- Phase 4 统一唤醒红灯符合预期：不同设备联系实际排入 `PeerContact(device-b/device-c)`；改为统一状态变化后转绿，并删除普通维护中的 PeerContact/PeerOnline 特例、定向同步和上线绕过退避。
- 维护、历史同步和准入恢复三组定向测试分别 18、15、21 项通过；外部上线、联系和显式变化现在只合并为通用唤醒，启动、恢复和周期只保留为负责人内部扫描原因。
- 配对交换的暂时失败现在推进同一加密记录内既有重试进度；新红灯先证明次数仍为 0，修复后验证重启可读的下次时间、过早唤醒零联网、到期恰好重试。准入恢复 22 项通过。
- 设备信任查询新增独立维护健康：自动重试返回持久下次时间，稳定历史拒绝返回明确原因和“处理设备信任”动作。Application 11 项与 Engine 映射测试通过；产品展示留给下游接入，不再要求产品猜测。
- 严格只读复核发现共享许可覆盖了配对网络等待，会阻塞本机取消/重新加入。已把许可移动到配对恢复完成之后、普通维护之前并在锁内重新读取状态；新增回归证明本机动作可打断配对恢复，同时普通动作仍阻止新配对并发落库。
- Core 准入测试 102 项通过；重试错误分类和内部方法可见性也已按复核修正。Phase 4 完成，进入 Phase 5 全量验证与本地提交。
- Phase 5 严格复核修正了共享许可覆盖配对网络等待的问题；新增回归证明本机取消可及时中断恢复，普通维护开始后新配对仍会等待。
- `uc-application` 全部库测试 966 项通过、1 项按原标记跳过；入站历史 21 项、设备信任 11 项、Core 准入 102 项均通过。
- 三设备 `pending_join_is_not_published_before_final_confirmation` 再次通过：最终确认前第三设备不可见，确认后才三端收敛并双向传输。
- `cargo metadata`、workspace 全目标编译、格式、Rust 规则、架构仓库、观测隐私和差异检查全部通过；临时诊断探针搜索无残留。
- 全仓长组并行运行两次分别遇到文件恢复等待超时、剪贴板接收计数时序抖动；两项使用相同完整功能配置精确复跑通过，后续串行全仓运行也都通过。串行成员端到端长组另有一次“移除后同设备重加”偶发失败，精确复跑通过。
- Phase 5 完成；未运行 Android/Windows 实体设备验收，未操作真实用户资料。

## 2026-09-21

- t-0028 已接入 Processing 与维护健康结果，但缺少可重复的独立进程红绿测试能力；Phase 6 开始。
- 本阶段测试 seam 已由用户确认：独立测试进程的完整配对入口，以及进程私有的有序测试观测；不向正式产品暴露调试开关。
- 已运行 planning session catchup，工作区在 `b52d37d3` 上干净；旧行为基线为 `d64a9046`，当前行为提交为 `3bb57184`。
- 已读取 implement、TDD、诊断、Uni Rust、严格复核和原子提交规则，以及 Application、Space、admission protocol、Infra 的局部维护约束。
- 当前正在盘点现有 admission test support、独立进程宿主与 dev-tools 边界，先确定最小红灯场景。
- 已确认复用路线：扩展现有 dev-only 最终确认 gate，并通过 connectivity host 的私有管道提供 arm/wait/query；不新增正式 API 或环境变量。
- 已定位观测边界：最终确认在 admission continuation transport，普通更新在 group update dispatch，历史同步在 membership history exchange；准备先写控制器红灯测试，再接入真实装配与独立进程命令。
- 已实现进程私有的一次性最终确认建连失败、有序事件等待/查询，以及 connectivity host 三条命令；测试宿主编译通过。
- 当前 E2E 通过：首次最终确认失败、按持久时间重试、收到回复并完成加入；重试前普通更新和历史同步为零。
- 隔离旧提交 `d64a9046` 使用同一测试能力稳定失败，实际顺序为失败 → 普通更新 → 三次历史同步 → 重试；临时 worktree 已移除，2.7GB 独立构建缓存已移入废纸篓。
- Engine dev-tools 库测试 244 项通过、4 项按原标记跳过；Application 关键维护回归通过；connectivity host 编译通过。
- 严格复核修正了“网络回复成功”被写成“流程成功”的事件命名；最终完成继续以公开状态验收。
- 最终门禁通过：workspace 全目标编译、格式、Rust 规则、Engine 仓库边界、观测隐私和差异检查全部通过；当前 E2E 在最终命名调整后再次通过。
- Phase 6 完成，等待本地原子提交和通知协调者让 t-0028 继续接入。

## 2026-09-21 t-0028 后续 A/B

- 当前工作区在本地提交 `a30cde49` 上干净；用户要求 A/B 独立，禁止推送、PR、发布和合并。
- Phase 7A 开始：先扩展现有 dev-only space work 测试控制，完成后单独提交并通知 t-0028。
- Phase 7B 保持诊断状态：先取得 Desktop 证据与 Engine 独立宿主红灯，在此之前不修改产品逻辑。
- A 已实现可计数注入、三类有序结果事件和独立进程命令；可重试场景定向端到端测试通过。
- A 的需处理场景第一次红灯证明：伪造无效回复会进入较重的证据恢复，不适合作为最小夹具；下一步改用稳定拒绝，并先补持久状态回归测试。
- A 的稳定拒绝持久状态红灯已复现并转绿；可重试与需处理两个真实 Engine 端到端场景均通过，分别验证自动到期恢复和明确放开后的恢复。
- 定向结果：Application 历史同步 16 项、Engine 测试控制 4 项、Engine 端到端 2 项全部通过；connectivity 测试宿主编译通过。进入 A 的完整门禁和独立本地提交。
- A 完整门禁通过：workspace 全目标全功能编译、格式、Rust 规则、Engine 仓库边界、观测隐私和差异检查均通过；Application 967 项通过、1 项按原标记跳过，Engine dev-tools 库 246 项通过、4 项按原标记跳过。
- 严格复核修正三点：稳定拒绝必须写入既有维护进度；需处理窗口由进程私有 clear 明确放开；“收到回复”事件不再被命名成业务恢复成功。Phase 7A 完成，等待独立本地提交。
- A 已独立提交为 `5630080f` 并通过 Herdr 通知协调者交回 t-0028；Phase 7B 开始，只做无故障注入的三设备重启复现和归因。
- B 完成诊断：Desktop 原 C2 场景重复稳定失败；日志把首个断点定位到重启 A 与在线 C 无法重建已认证联系，而不是成员历史落盘丢失或列表读取错误。固定端口对照仍失败；两种地址刷新试验也未使原场景转绿，均已撤回。
- B 不修改产品逻辑；测试资料已从工作目录移入废纸篓，Desktop worktree 恢复干净。结论将以独立文档提交记录并交回协调者。
- B 文档交付检查通过：Cargo 元数据、workspace 全目标编译、格式、Rust 规则、Engine 仓库边界、观测隐私和差异检查均通过；编译仅保留仓库原有的 OHOS 测试未使用导入警告。

## 2026-09-21 Phase 12 收尾

- 独立进程矩阵三项通过：加入方早期退出后双方重启、最终确认首次连接失败后邀请方重启、同一失败后加入方重启，均恢复同一尝试并完成。
- 严格复核修正了失败候选丢失、需处理状态缺少原因与处理方向、成员移除并发变化误报为永久损坏，以及旧宿主测试未等待配对终态的问题。
- 完整短测试、公开契约、持久化、绑定映射和隐私检查通过。完整真实网络组强制单线程连续运行约 31 分钟后有 7 项后段网络超时；直接相关的两项已分别重新运行并通过，其余属于既有分支恢复、空间切换和暂停恢复场景，未用来掩盖本任务结果。
- 现场旧日志仍不能证明此前最终启用失败命中的具体子类，因此报告保留未知；新版本只为以后发生的失败保存安全分类。
- 未操作实体 iPhone、保留模拟器或任何真实空间；原地验收仍需协调者明确确认目标和安装动作。

## 2026-09-21 t-0030 配对失败后持续不可验证诊断

- 已运行 planning-with-files session catchup；进入 Phase 8，本阶段只诊断和建立可重复失败，不修改产品行为。
- 已读取 t-0030 最新报告、发现和进度，并确认保存的邀请方诊断包及仍在运行的专用模拟器日志均可只读访问。
- 两端时间线已初步对齐：最终三次交换和回复校验成功，加入方在准备正式启用时失败并持久保存为拒绝；邀请方随后持续发生设备资料查询失败。
- 加入方在第二次完整尝试之前已每 30 秒稳定报告普通成员维护损坏，因此“第二次失败产生了最初损坏”已被时间顺序排除；第一轮未完成是否产生该损坏仍待测试分类证明。
- 已增加仅测试使用的持久场景 `two_unsettled_sponsor_attempts_survive_restart_and_block_the_public_display`：真实签名历史与两条加密邀请方记录中，第一条待确认可正常查询；第二条进入后查询返回安全依赖失败；重启后两条记录和失败均保持。测试通过。
- 同一场景推进到原期限：两条记录只改为“未确认”；随后恢复摘要已无配对工作和可执行项，公开查询却仍失败。重启后结果不变，固定了“协议工作已结束但查询永久受阻”的现状。
- `uc-infra` 全部库测试 1002 项通过、8 项按原标记跳过；workspace 全目标编译、格式、Rust 规则、Engine 仓库边界、观测隐私与差异检查通过。全目标编译保留仓库原有的 OHOS 测试未使用导入警告。
- 既有加入方测试 `invalid_activation_is_saved_as_a_terminal_rejection` 通过：最终接纳无效在加入方保存为终态拒绝，不继续假装处理中。
- Core 邀请方测试确认：保存完成回复时没有正式成员效果；只有收到最终确认才进入正式提交；等待确认到期只转为未确认并继续接受迟到确认。
- 安全分类测试确认：安全关系转换返回“不一致”时会映射为不可重试的最终接纳无效。但现场日志没有保存其内部来源，所以没有把这一测试类别冒充为现场精确原因。
- 本轮未操作、重置或清理 t-0030 模拟器、实体设备或真实空间；只读取已有日志。未提交、推送、创建 PR 或发布。

## 2026-09-21 配对失败与进程退出后的双端收敛实施

- 用户已授权在当前 t-0010 工作区开始修复；继续使用现有唯一空间负责人和加密准入记录，不新建线程或第二份模式事实。
- 已运行 planning-with-files session catchup；没有未同步输出。首次按旧下划线路径调用失败，随后按技能实际文件名恢复成功。
- 已读取 implement、TDD、Uni Rust、严格审查、原子提交和模块设计规则，并补读 TDD 测试与 mock 参考。
- 已读取准入协议局部维护规则及 ADR-017、ADR-022、ADR-026；确认当前唯一提交点与 ADR-026 早期“较早正式应用”文字需要在本轮统一。
- 已把实施拆成 Phase 9–12：先修复多候选公开查询，再完成正式提交前双端有界收尾、提交后成功重放与旧记录迁移，最后跑独立进程矩阵和完整门禁。
- 当前 Phase 9 进行中；下一步把已有 Infra 诊断场景提升为公开查询红灯，并保持既有正式设备可见。
- Phase 9 首个红灯已稳定复现：`two_unsettled_sponsor_attempts_remain_independently_queryable_after_restart` 在两条等待最终确认记录时返回 `space admission state is corrupt`，而期望两条候选分别可查；1 项失败、1009 项过滤，耗时 0.08 秒。
- Phase 9 存储投影红灯已转绿：两条候选按稳定编号分别返回，重启结果一致；Application 公开设备状态红灯先丢失候选集合，修复后保留两条候选且正式设备列表不包含它们；Engine 映射测试也通过。
- Phase 10 加入方拒绝红灯先证明终态没有待发通知；修复后历史冲突仍公开为拒绝，同时保存可重试放弃通知，下一轮收到确认后停止重发，重启解码仍保留结果。
- Phase 10 邀请方期限规则已改为：Applied 尚未正式提交，期限到达直接终结并丢弃本次未启用准备资料；迟到最终确认不再恢复候选。Core 与 Application 定向测试通过。
- 恢复索引版本提升并从加密准入记录重建，保证旧版已经标成 Unconfirmed 的记录升级后也重新进入期限收尾；两条候选重启场景再次通过，收尾后公开候选集合为空。
- 新协议版本固定为 V4，使不理解失败终态通知和新收尾规则的旧端在创建候选前得到升级结果；已有 V3 持久记录仍可由升级后的本机读取并按持久证据迁移。
- Phase 10/11 定向套件通过：Core 准入 103 项、Application 配对协议 70 项、Infra 准入库 35 项通过且 2 项按原标记跳过、SQLite 准入状态 30 项、Engine 公开结果 50 项。
- 独立进程矩阵使用当前 connectivity host 和独立临时资料运行三项，加入方早期崩溃、最终确认连接失败后邀请方崩溃、同一失败后加入方崩溃均通过，分别耗时 8707、5351、2281 毫秒；证据保存在 `target/t0010-pairing-convergence-processes-final/result.json`。
- 严格复核补齐两项遗漏：失败候选保留不含设备资料的最小公开投影；`NeedsAttention` 带稳定原因、处理方向和空重试时间。三层映射与公开 JSON 契约测试通过。
- 完整成员配对测试首次发现移除成员时并发状态变化被误报为不可恢复；修正为可重试后，`confirmed_pairing_survives_restart_removal_and_same_device_rejoin` 通过。
- Phase 9–11 完成，Phase 12 进入全仓门禁和本地原子提交；未操作实体设备、模拟器或真实空间。
## 2026-09-21 Phase 13 统一对外状态

- 已运行 planning-with-files session catchup，无未同步上下文；入场时工作区干净。
- 已审查 Application 设备信任投影、Engine 结果映射、UniFFI/OHOS JSON 绑定和本地诊断链路。
- 已确认现有公开健康只覆盖历史交换，会漏掉待生效设备效果与设备关系，且把从未完成的历史交换当成健康。
- 已选定最小长期边界：Application 产生唯一整体状态，Engine 对外新字段映射并派生旧兼容字段；不新增持久事实。
- 下一步：先补整体状态红灯，然后实现推导与 Engine/绑定兼容映射，最后补诊断导出证据。
- 整体状态红灯已运行：Application 编译明确失败，尚无 `space_device_update` 和四态类型。实现后设备信任定向 14 项全部通过。
- Engine 新公开字段已映射四种结果；旧 `maintenance_health` 仅从新结果派生，其中新的 `Updating` 映射为旧的 `Retrying`，防止旧客户端把未完成误作健康。
- 已增加安全的历史交换阶段、原因和固定调用路径；公开结果不含子步骤、标识或原始错误。
- 成功回复丢失控制已实现并通过：单元测试验证四个事件的严格顺序；真实双进程 E2E 验证邀请方提交后丢第一份成功回复，加入方按持久时间重试并成功收敛，耗时 6.31 秒。
- 加入方最终确认前重启 E2E 最初只纳入历史与关系时曾达到 `Completed`；严格复核把安全资料投递也纳入后，该断言稳定超时，证明此前完成过早。用例现固定为：正文可传输，但双方整体状态仍不得显示完成。
- 已确认 t-0028 后半段与 `4fd86ad8` Phase 7B 的既有网络恢复断点同类，而 `4fd86ad8` 是 Phase 10 修复 `8df9d5e1` 的父线先行提交；不将该独立问题误并入候选收敛修复。
- 下一步：补公开错误分类和诊断链测试，更新正式文档/迁移说明，再做严格审查与全量门禁。

## 2026-09-21 Phase 13 收尾

- 整体状态现覆盖待生效设备变化、关系确认、历史确认、限制投递和安全资料投递；安全资料未来重试时间直接读取原加密任务记录，被拒绝时进入明确需处理。
- 严格复核移除了测试替身的默认“已完成”，所有安全资料状态实现现在都必须明确返回结果，避免未来新增实现漏报欠账。
- 最终成功回复丢失的真实双进程测试通过：邀请方先持久提交，首次成功回复被测试进程单次丢弃，加入方按已保存时间重试并得到原成功结果；事件顺序可等待，不依赖固定睡眠。
- t-0028 后半段不是候选收敛回归：相同的认证联系恢复断点已在 `4fd86ad8` 记录，早于 Phase 10。当前重启场景可完成正文传输，但安全资料/关系欠账仍使整体状态保持未完成；第三设备成员更新场景精确复跑可通过，说明全量并发组中的即时查询失败是测试时序放大，不足以宣称网络恢复已修复。
- 全仓全目标编译、Cargo 元数据、格式、Rust 规则、仓库边界、观测隐私和差异检查通过。全量测试运行到真实网络长组：其余确定性套件通过；58 项网络组 38 通过、11 按原标记跳过、9 失败。两项因旧“子步骤恢复即整体健康”断言失败的测试已修正并分别通过；第三设备更新也精确复跑通过。其余失败集中在已知分支恢复、切换与暂停恢复的网络时序，未宣称通过。
- 未操作 Android、Windows、iPhone、模拟器或真实空间；未推送、建 PR 或发布。
