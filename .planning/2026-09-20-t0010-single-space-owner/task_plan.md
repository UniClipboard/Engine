# t-0010 唯一空间负责人实施计划

## 目标

在原提交 `2d994c8a4fb971e7c651d87c6b0f702b36458a4e` 已重放至 `origin/main` 的当前提交 `d64a9046` 基础上，以最小可运行切片实现唯一空间负责人：配对未终结时只恢复配对，普通成员维护及入站成员历史均不运行；配对终结后恢复普通维护。状态必须从现有持久配对记录推导，不新增第二份持久模式。

## 完成标准

- 正式规格和 active exec plan 落盘，并固定长期规则、回滚点与真机验收。
- 红灯测试先证明 Pairing 下普通维护和入站历史写入仍会发生。
- 唯一空间负责人形成可运行端到端切片，所有维护触发只表达“可能有变化”。
- 最终确认暂时失败后不发起成员更新或历史同步，配对完成后普通维护恢复。
- 候选资料不进入普通成员传播范围；正式成员只在最终确认点提交。
- 旧的 Continue/Yield、绕行和散落唤醒按阶段删除；未删除迁移层有明确记录。
- 相关测试、全仓检查、严格审查通过；有效问题已修正。
- 形成清晰的本地原子提交，不推送、不创建 PR、不发布。
- Android/Windows 真机仅写验收计划，未获授权不安装或操作真实资料。

## 已确认测试边界

1. 空间负责人公开维护入口：观察配对恢复、普通维护及网络调用。
2. 入站成员历史公开处理入口：观察稳定可重试结果及历史是否写入。

## 阶段

### Phase 0 — 规格、计划和基线（complete）

- 创建独立 planning-with-files 目录。
- 阅读仓库架构、设计原则、Rust、错误、安全、计划约定和相关局部 AGENTS。
- 创建正式规格与 active exec plan。
- 更新 thread report，记录路径、阶段和第一个红灯测试位置。
- 完成标准：文档覆盖所有长期规则、替代方案、阶段、删除项、回滚点、自动与真机验收。
- 回滚点：仅删除本阶段新增文档，不影响运行行为。

### Phase 1 — Pairing 独占后台维护的最小切片（complete）

- 先写空间负责人边界的红灯测试。
- 从现有持久配对记录推导运行资格。
- 让现有维护入口在 Pairing 时只推进配对，普通维护调用次数为零。
- 配对完成后重新读取事实并恢复普通维护。
- 删除目标：本切片消除临时网络失败后继续普通维护的行为；保留旧触发枚举和内部维护步骤作为迁移层。
- 完成标准：t-0025 代表场景红绿通过；定向编译通过。
- 回滚点：恢复旧维护入口，持久格式不变。

### Phase 2 — 入站成员历史统一裁决（complete）

- 先写 Pairing 下入站历史不得写入的红灯测试。
- 配对消息和成员消息进入同一空间负责人裁决。
- Pairing 时返回稳定可重试结果，不解析或写入成员历史。
- 删除目标：移除 PeerContact/入站历史绕过配对的路径。
- 完成标准：入站历史测试、普通历史同步测试和三设备可见性测试通过。
- 回滚点：恢复旧入站路由，持久格式不变。

### Phase 3 — 候选资料与正式历史隔离（complete）

- 先固定最终确认前第三设备不可见、正式历史不含候选的测试。
- 候选资料只保存在配对记录或私有暂存区。
- 最终确认成为正式成员、安全资料和可重放回复的唯一提交点。
- 删除目标：移除未确认成员参与普通历史同步的规则与测试。
- 完成标准：丢请求、丢回复、双方重启、重复消息和同设备重试均只产生一个正式成员。
- 回滚点：按提交边界回退本阶段，保持已有持久版本可读；不得重写用户历史。

本阶段由当前分支已有提交 `d64a9046` 完成；本轮复核并保留其 CompleteAck 唯一提交、重复确认幂等、重启恢复和三设备最终确认前不可见覆盖，没有另建第二套实现。

### Phase 4 — 唤醒、重试与状态收口（complete）

- 配对持久保存下次重试时间和稳定失败。
- 外部触发统一为“可能有变化”；普通维护不理解最终确认或让路。
- 建立 NeedsAttention 稳定 Engine 契约；若完整产品展示超出范围，记录下游阶段但不沿用无限“更新空间”。
- 删除目标：Continue/Yield、散落 wake/schedule_at、业务层多种触发翻译和重复处理中状态。
- 完成标准：启动、恢复、网络恢复、上线、联系、定时、请求/回复丢失和重启测试通过。
- 回滚点：保留兼容读取与旧内部适配一阶段，公开状态不回退。

### Phase 5 — 清理、严格审查、全量验证与提交（complete）

- 更新架构圣经和执行计划状态。
- 严格代码审查并修复有效问题。
- 运行相关完整测试、全仓编译、格式、Rust 规则、架构、隐私和仓库门禁。
- 按独立逻辑整理本地原子提交。
- 完成标准：工作区只剩明确的已提交成果和 Herdr/planning 文件；未执行真机验收清楚记录。
- 回滚点：每个新增原子提交可独立回退；已按用户最新要求将原提交 rebase 到 `origin/main`，不再额外改写其内容。

本阶段已完成严格复核、有效问题修正、定向和全仓验证。全仓并行运行两次分别暴露两个既有宿主时序抖动，二者在相同完整功能配置下单独复跑通过；随后串行运行覆盖到两项均通过。完整成员端到端组中的“移除后同设备重加”在长组串行运行时一次失败，精确复跑 1 项通过。交付据此同时保留完整运行记录与稳定的定向证据，不把偶发失败隐去。

### Phase 6 — t-0028 独立进程红绿验收能力（complete）

- 在现有 admission protocol test support 上增加进程私有、仅测试构建可用的故障控制和有序观测。
- 指定测试进程的首次最终 `CompleteAck` 连接失败一次，之后只按已持久化的重试时间恢复。
- 观测最终确认尝试、普通成员维护与成员历史同步的次数和严格顺序，不用固定等待推断。
- 用同一测试场景证明 `d64a9046` 失败、当前 `3bb57184` 行为成功，并给 t-0028 提供最小使用说明。
- 更新架构圣经、正式计划与 Herdr 报告，运行相关 Engine 测试、严格复核和仓库门禁，形成新的本地原子提交。
- 完成标准：正式产品无可触发入口，不依赖全局环境或真实资料；独立进程测试可重复得到红绿证据；协调者可直接让 t-0028 接入。
- 回滚点：单独回退本阶段测试能力提交，不改变生产协议、持久格式或正式成员规则。

本阶段已用同一 E2E 场景取得红绿证据：`d64a9046` 在最终确认重试前实际执行一次普通成员更新和三次历史同步；当前行为在失败后直接等待持久重试并完成。connectivity host 已提供进程私有的 arm/wait/query 命令，完整门禁通过，正式产品入口和持久格式均未改变。

### Phase 7A — 成员维护失败的独立进程验收能力（completed）

- 复用 `dev-tools` 的 space work 控制与 connectivity host 私有管道，对普通成员历史交换注入可计数失败并允许恢复。
- 先补红灯，覆盖失败后的公开维护健康、持久下次时间、精确事件次数和恢复后的健康结果；不用固定等待。
- 不新增正式 Engine 操作或发布构建入口，不记录成员、地址或消息材料。
- 完成后更新架构圣经、正式计划和 Herdr 报告，运行相关测试与门禁，形成仅包含 A 的本地原子提交。
- 回滚点：单独回退测试能力提交，正式成员维护行为与持久格式不变。

### Phase 7B — 三设备重启后成员关系丢失诊断（completed）

- 先读取 t-0028 的 C2/C3 证据，再用 Engine 独立宿主建立“重启前 3 人、重启后 A 变 2 人”的最小红灯。
- 按成员持久化/恢复、后台启动时序、Desktop 夹具三类假设逐项证伪；没有红灯前不修改产品逻辑。
- 若证实为 t-0010 重启恢复范围内的 Engine 缺陷，另起测试先行的最小修复并独立提交；否则只形成清晰交接。
- 完成标准：B 的结论、命令、证据和提交与 A 完全分开记录。

结论：正式成员历史没有在重启时被删除；独立进程首先失败在重启节点与仍在线新成员无法重新建立已认证联系，历史交换因此没有机会把新成员带回。固定重启端口仍失败，排除了单纯端口变化；两种地址刷新试验也没有让 Desktop 原场景转绿，均已撤回。该断点位于网络恢复与安全历史引导之间，不属于本轮唯一空间负责人调度修复；B 只提交诊断文档，不保留产品逻辑改动。

### Phase 8 — 配对失败后的持续不可验证诊断（diagnosis complete）

- 只读对齐 t-0030 保存的加入方日志与邀请方诊断包，不重置、清理或操作现场设备和真实空间。
- 用不包含身份或受保护资料的分类收窄第二次最终接纳失败的首个校验点。
- 先在现有测试宿主建立“第一次停在中间、第二次最终接纳失败、失败后公开状态与重启收敛”的稳定红灯；本阶段不修改产品行为。
- 区分第一轮残留、第二轮正式提交和后续设备资料查询三条因果链，不能把时间相邻当成已证明因果。
- 若确认产品缺陷，只交付最小长期修复方案和验收清单，等待协调者授权后再实施。
- 完成标准：现场时间线、稳定自动复现、根因边界和失败后正确状态均有证据；不提交、不推送、不创建 PR、不发布。现有现场日志无法还原加入方最终接纳内部的具体子检查，此证据缺口必须明确保留，不能用测试中的一种可复现类别冒充现场事实。

本阶段已建立真实加密记录的自动场景：一条邀请方待确认记录时公开查询正常；第二条进入同一阶段后，查询稳定失败，重启后两条记录仍在。恢复摘要仍显示配对占用，但没有可执行的结束工作。加入方的既有自动测试同时确认最终接纳无效会保存为终态拒绝。该结果证明邀请方持续不可验证的直接缺陷，不证明现场加入方究竟命中回执、签名、历史位置、安全状态或关系转换中的哪一个子检查。

### Phase 9 — 多候选公开状态不再锁住正式设备（completed）

- 先把两条未完成邀请方记录的诊断测试提升到公开查询行为：候选分别可见，既有正式设备仍可查询、可使用。
- 公开状态必须区分待确认、失败和需要处理；数量大于一不能再把整个设备查询判为损坏。
- 不改变成员历史、安全资格或邀请门槛；只从同一份加密准入记录投影公开状态。
- 完成标准：修复前稳定红灯；修复后首次查询、重复查询和双方重启均返回相同结果，正式设备列表不受候选数量影响。
- 回滚点：公开投影和查询映射可独立回退，持久记录不变。

### Phase 10 — 正式提交前的双端有界收尾（completed）

- 加入方不可重试拒绝、用户放弃和本机到期均保存可重发终态；通知丢失时继续按同一尝试重发。
- 邀请方在共同期限到达后，即使没有收到通知，也根据“尚未保存最终确认提交”的持久证据终结候选，只清理该尝试未启用的准备资料。
- 重复、迟到或乱序通知返回同一结果；邀请方确认收到后才压缩加入方终态。
- 完成标准：明确失败通知成功/丢失/重复、双方分别重启、多个阶段强制退出与邀请方独立到期均通过独立进程测试。
- 回滚点：新终态与消息保持向后可读；不删除旧记录，不触碰正式历史。

### Phase 11 — 最终提交后的成功重放与旧记录迁移（completed）

- 最终确认的原子提交同时保存正式成员、安全效果和可重放成功结果；回复丢失或越过期限绝不回滚正式成员。
- 旧记录按可证明事实迁移：未提交者进入有界收尾，已提交者恢复正式成功，证据不足者进入 NeedsAttention。
- 未安全收尾的旧尝试阻止创建新入站尝试并返回稳定、可处理结果；不得叠加第三条悬挂记录。
- 完成标准：完成回复丢失、过期后重放、两条旧候选原地迁移、无法判定旧记录和新尝试门禁测试通过。
- 回滚点：迁移只前进到可验证终态；无法证明的记录保留原密文并停止自动动作。

### Phase 12 — 独立进程矩阵、严格审查和本地提交（completed）

- 运行真实双方独立进程场景，覆盖退出、崩溃、永不重启、丢消息、重复消息及双方分别重启。
- 严格审查条件分支、事务边界、敏感记录和错误来源；先修正再跑完整门禁。
- 更新正式执行计划、架构圣经和 Herdr 报告，按逻辑形成清晰的本地原子提交；不推送、不创建 PR、不发布。
- 完成标准：自动矩阵和仓库门禁全部通过；真机与模拟器原地验收步骤只在明确授权后执行，未执行时不得报告整体完成。
- 回滚点：每个行为切片可独立回退；不删除测试资料、日志、构建缓存或现场资料。

Phase 12 已完成自动矩阵、严格复核和仓库门禁。长时间单线程串行运行完整真实网络组时，后段因网络资源累积出现 7 项超时；其中与本任务直接相关的移除后同设备重加和第三设备成员更新均在独立进程中复跑通过。正式的确定性准入、重启、丢回复、重复消息、旧记录迁移和公开状态测试全部通过。真机与保留模拟器仍未操作，等待明确授权后原地验收。

## 决策

- 推荐互斥的唯一空间负责人，不采用并行“双历史头”方案。
- 不新增持久的 Joining/Active 开关，模式从配对聚合推导。
- 已有正式成员内容传输默认继续；成员控制面暂停，安全取舍在规格中明确。
- 正式成员提交与邀请方最终确认是同一持久动作；历史收敛属于后续普通维护。

## 错误记录

| 时间 | 错误 | 处理 |
|---|---|---|
| 2026-09-20 | 进入任务时工作区停在将 `2d994c8a` rebase 到 `af25532c` 的冲突中，架构圣经未合并 | 用户明确要求继续原 t-0010 分支且不重写 `2d994c8a`；保存本任务新增的未跟踪规划文件后，终止该 rebase，恢复原分支与提交 |
| 2026-09-20 | 用户随后明确要求先 rebase；第一次重试在解决冲突期间被外部操作中止 | 重新确认工作区回到原 HEAD，第二次 rebase 保留架构文档双方记录并成功完成；新 HEAD `d64a9046`，`origin/main` 是其直接父提交 |
| 2026-09-20 | 两次更新 planning 文件的补丁使用了过期上下文 | 每次都重新读取最新文件，按现有标题追加；未重复原失败补丁，未影响仓库代码 |
| 2026-09-20 | 第一次批量更新测试引用时，`membership/mod.rs` 上下文与预期不同，整份补丁未应用 | 读取实际导出结构后拆分补丁，按现有位置更新；没有产生半应用修改 |
| 2026-09-20 | Phase 1 维护测试 19 项中 1 项仍断言旧 PeerContact 绕行序列 | 更新为先经过空间工作裁决后的新稳定序列，随后重跑整组 |
| 2026-09-20 | 一次 Cargo 命令误传了两个测试过滤参数，Cargo 拒绝执行 | 改为分别串行运行两个定向测试；未影响代码或共享构建目录 |
| 2026-09-21 | planning-with-files 文档中的旧示例使用了不存在的 `session_catchup.py` | 检查技能目录后改用实际文件 `session-catchup.py`；恢复脚本成功且无未同步输出 |
| 2026-09-21 | TDD 参考文件被误按 `references/` 子目录读取 | 检查技能清单后读取同目录的 `tests.md` 与 `mocking.md`；未重复错误路径 |
