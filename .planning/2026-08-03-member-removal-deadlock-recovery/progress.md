# 进度记录

## 2026-08-04

- 发布失败排查开始：已从正式工作流日志确认失败不在发布包构建，而在旧版桌面消费者的接口编译。失败日志显示旧消费者接入了新增字段，下一步核对并复现发布工作流的旧版依赖覆盖逻辑。
- 已核对：旧版依赖覆盖逻辑正确；根因是桌面主线遗漏 `v0.20.0-rc.20` 的接口适配。准备在隔离副本复现并用最小桌面改动修复，随后将重新验证旧版和新版构建。
- 隔离副本已稳定复现四个接口编译错误，并确认锁定文件全量来自 `v0.20.0-rc.20`。修复将把普通加入显式设为不迁移，切换空间显式设为保留不可读旧资料；返回的新增计数只在宿主边界接收，不扩散到桌面状态。
- 已更正适配语义：保留不可读资料必须由用户明确确认，不能默认开启。首个多文件补丁因桌面端导入排列与预期不一致被完整拒绝，未造成代码变更；后续将按实际内容拆分写入。
- 复跑双向互通脚本时发现发布流程的第二个缺口：默认构建没有生成 `uniclipd`，新版本 CLI 无法启动本地后台程序。已把两个消费者的构建改为显式产出 `uniclip` 和 `uniclipd`，并增加流程形状测试；等待重新构建两个版本和双向验证。
- 新版桌面构建进一步确认遗漏的事件分支：`MemberRevocationChanged` 尚无桌面守护进程 API 或界面，导致穷尽匹配无法编译。当前互通修复与该独立产品事项分离，只按既有未接入通知的处理方式保留在守护进程内；完整 API、界面和用户确认流程继续由桌面事项 #1495 负责。
- 适配分支恢复到 `rc.20` 后，直接列出新版事件分支会反过来阻止旧版构建。已改为对所有未映射到桌面 WebSocket 的 Engine 通知统一返回空，保持已映射通知不变，并允许同一消费者源码兼容上一版与新发布版。完整前端构建通过；全量前端 lint 因独立工作区缺少文档站的 `eslint-config-next` 依赖失败，后续改为检查本次改动涉及的前端文件。
- 已验证 `rc.20` 与 `v1.0.0-rc.1` 的两个方向均可完成配对和双向内容传输；桌面后端测试、前端构建、受影响前端文件检查、格式和差异检查均通过。桌面全工作区检查通过与 CI 相同的 Tauri sidecar 覆盖配置；全量前端 lint 仍因文档站未安装的 `eslint-config-next` 失败，非本次改动造成。
- 已推送 Engine 发布流程修复提交 `37b7c5f` 到主线，并创建桌面适配 PR #1497（提交 `55aef12f9`）。等待 PR 合并后重跑 `v1.0.0-rc.1` 的互通门槛和产品仓库通知；不自动合并该 PR。
- 桌面 PR #1497 已合并。为避免覆盖已有 GitHub Release，新增并推送 `f96872a`，让正式复核始终使用原发布提交，只确认既有 Release 后再运行互通与通知。
- 正式复核运行 `30884371931` 的输入校验、iOS、Android、HarmonyOS 构建和 Release 完整性核对均通过；旧版与新版桌面消费者均构建成功，两个方向的配对和双向内容传输均通过。
- 最后“通知桌面和移动端”失败，原因是 `actions/create-github-app-token@v3` 收到空的 `ENGINE_RELEASE_APP_CLIENT_ID`。当前身份无权读取组织变量（HTTP 403），不能安全猜测或使用个人令牌绕过；组织管理员需恢复该变量并确认 `ENGINE_RELEASE_APP_PRIVATE_KEY` 已授予 Engine，然后重新运行同一复核入口。
- 后续现场复查：当前管理员身份对 `UniClipboard/Engine` 的实时读取显示仓库变量、仓库密钥和环境列表均为空。两项配置没有保存在此工作流可读取的仓库级位置；私钥还必须作为仓库密钥保存，不能与 Client ID 一同作为变量保存。
- 用户确认两个值存放在组织密钥。根因确定为工作流把 Client ID 作为变量读取；已先让发布传播检查失败，再将 Client ID 读取位置改为组织密钥并补充架构文档维护记录，待完整检查和推送后复跑正式复核。
- 发布复核 `30888439965` 已由主线提交 `8fa8dca` 触发；请求校验和 HarmonyOS 构建已通过，iOS/Android 构建仍在运行。一次用于压缩运行状态的 `jq` 查询语法错误，未触及工作流或发布状态；后续改用已验证的对象查询。
- 发布复核 `30888439965` 的公开发布包、iOS、Android、HarmonyOS、新旧桌面消费者构建，以及两个方向的配对与传输均通过。最后通知步骤确认两个组织密钥均已读到，但 GitHub App 查询 `UniClipboard/UniClipboard` 的安装记录返回 404，说明该 App 未安装或未获选中仓库访问；需在 GitHub App 安装页面授予 `UniClipboard` 和 `UniClip` 访问及内容读写权限后重跑。
- 已尝试使用 `gh` 管理安装范围：当前用户可读取组织安装清单和两个产品仓库标识，但不能读取组织密钥中的 Client ID 来匹配具体 App，也不能以个人登录令牌修改 GitHub App 安装范围。该操作必须由该 App 的拥有者在安装页授权；不得更换为个人令牌绕过。
- 组织完成 App 安装后，Engine 复核运行第二次尝试成功，两个仓库均收到 `repository_dispatch`。桌面端已创建更新 PR #1500；UniClip 的接收流程启动但 Android 打包发现新成员移除状态通知未被原生映射。已在 UniClip 主线推送 `7164fb9`，让 Android、iOS 和界面层识别该通知；定向接口检查、类型检查和格式检查通过。本机提交钩子因项目既有 `.npmrc` 的 Node 参数无法启动，改由已通过的直接检查和远端构建验收。Engine 成功运行不能单独重跑通知，已创建新复核 `30894471574`，待其完成后验证 UniClip 更新请求。
- Engine 复核 `30894471574` 已完整通过并再次通知产品仓库。UniClip 的 Android、iOS 和质量检查均通过，但最后创建更新 PR 仍从变量读取 Client ID 而失败；已在 UniClip 主线直接提交 `0c7f843`，将 Client ID 改为组织密钥并把 Engine 版本、提交和产物校验同步至 `v1.0.0-rc.1`。本地来源校验、定向接口检查、类型检查和格式检查均通过；提交与推送的本机钩子继续受既有 Node 参数阻塞，因此使用已验证的直接检查后推送。
- 桌面端适配已写入独立分支并完成上一版 Engine 依赖升级；OpenAPI 后端定义成功生成。客户端生成器首次运行因独立工作区缺少本地前端依赖而失败（`openapi-ts: command not found`），将安装锁定依赖后重试。

- **状态：** 已再次恢复现场，确认从阶段 4 继续；阶段 1 到 3 的改动仍在，未触碰原有提交和其他计划指针。
- 已确认通知责任边界：应用层发布每次完整状态，Engine 统一转发，绑定只做字段转换；这样可覆盖操作、确认、自动重试和重启恢复。
- 应用层通知行为测试通过：一次成功移除会依次发布等待状态和完成状态。
- Engine 通知转发测试通过；启动恢复已调整为先订阅再恢复，避免启动期间丢失状态。
- UniFFI 完整状态、当前查询、永久丢失继续入口和状态通知已接通；映射测试与真实绑定入口测试通过。
- HarmonyOS N-API 使用同一组字段、入口和通知负载；6 个绑定单元测试通过。
- 阶段 4 完成：UniFFI 全部 42 项、HarmonyOS 全部 31 项、移动验收宿主全部 21 项测试通过。
- 阶段 5 开始，更新三份架构/接口说明并执行完整验收。
- 阶段 5 隐私复核完成：成员移除相关的跟踪和发送失败日志已不再包含设备编号，定向源码测试通过。
- 架构说明已同步：ADR、架构总览和稳定接口说明均已补充等待、查询、永久离线恢复与通知的责任边界。
- 已通过：`cargo test -p uc-application member_revocation_logging_does_not_include_device_identifiers --locked`、`cargo fmt --all -- --check`、`node scripts/architecture/check-engine-repository.mjs` 与 `git diff --check`。
- 剩余：执行完整工作区编译、各核心包测试及最终复核；模拟器、真机和发布产物仍未执行，交付时必须明确标为跳过。
- 完整验收中，`cargo test -p uc-engine --locked` 发现依赖边界检查失败：它仍禁止 Engine 订阅移除恢复入口，与本次“应用层负责恢复、Engine 仅启动应用层流程”的既定边界不一致。下一步只调整该检查的允许清单并重新执行。
- 已调整过期的依赖边界检查：它继续禁止 Engine 接管无关活动步骤，但允许启动后重试一次应用层成员移除恢复，以保证状态通知订阅已就绪。
- **阶段 5 完成。** 完整编译和完整工作区测试均通过；最终格式、架构和差异检查均通过。
- **跳过：** iOS 模拟器、Android 模拟器、HarmonyOS 模拟器、所有真机验收和发布产物验证。本次未构建或安装平台应用，也未生成发布包，因此这些项目不计通过。
- 发布准备一度发现当前本地 `main` 比远端多 9 个既有提交；用户随后已将其推送，复核确认本地与远端主线一致，因此本次改动已独立建立在该主线上。
- 已创建并推送 `feat/member-removal-deadlock-recovery`，提交为 `e1868a5`；Engine PR 已创建为 https://github.com/UniClipboard/Engine/pull/18。
- 已创建桌面端适配事项 https://github.com/UniClipboard/UniClipboard/issues/1495 和移动端适配事项 https://github.com/UniClipboard/UniClip/issues/7；两个事项均依赖并链接 Engine PR，PR 也已反向链接两个事项。
- 阶段 6 开始：逐项核对成员移除恢复 PR 的审查建议。用户要求只修复仍有效项，并对跳过项给出简短原因；未提供原评论链接，因此本轮不创建带评论链接的审查提交。
- 已完成本轮 13 条审查意见的代码核对：均仍有效，未发现可跳过项。下一步实施核心原子性、混合选择和恢复前验证，再处理接口文案与各平台宿主反馈。
- **阶段 6 完成。** 13 条意见均已修复：混合选择可继续、恢复在写入前完整验证、失败不留下半完成状态、永久丢失选择拥有独立反馈、宿主失败会回退并显示错误、iOS 仅记录非识别性进度字段，且成员相关日志不再写入设备编号。
- 验证通过：核心定向测试、应用层全部 785 项、基础设施恢复提交测试、Engine 错误映射测试、HarmonyOS 绑定全部 31 项、移动宿主全部 21 项、`cargo check --workspace --all-targets --locked`、`cargo test --workspace --locked --quiet`、格式、架构和差异检查。
- **跳过：** iOS、Android、HarmonyOS 模拟器和真机验收，以及发布产物验证。本轮没有构建平台应用或发布包，因此不计通过。
- 阶段 7 开始：用户要求发布 `1.0.0-rc.1`。已确认 PR #18 的合并提交包含审查修复，`v1.0.0-rc.1` 标签和公开发布均不存在；当前主线版本仍为 `0.20.0-rc.20`，需要先同步版本后再触发正式预发布。
- **状态：** 已恢复计划现场，阶段 1 开始。
- 已确认之前没有生产代码改动，本轮将先固定第二次移除错误、重复请求行为、统一快照、当前查询与状态通知契约。
- 恢复过程中一度误改全局活动计划指针，已按提交中的原值恢复，未留下该项改动。
- 已读取核心层、应用层和基础设施层的局部规则，以及既有旧设备移除记录；确认阶段 1 可在现有可靠移除流程上增量完成。
- 已定位入口顺序和错误压平位置，下一步按测试先行补同目标重试与不同目标冲突用例。
- 失败测试已确认：编译明确指出缺少当前移除查询、具体设备列表、更新时间和独立冲突类型，失败原因与阶段 1 目标一致。
- 应用层最小实现完成：两个新增场景分别通过，应用层全部 781 个测试通过。
- Engine 稳定错误和完整快照定向测试通过；公开契约 42 项全部通过。
- 阶段 1 完成，阶段 2 开始验证加密状态查询与重启一致性。
- 阶段 2 完成：底层定向测试和真实数据库重启恢复测试通过。
- 阶段 3 开始，先固定永久离线恢复的核心约束和多轮顺序。
- 阶段 3 完成：核心多轮测试、真实群组顺序测试、数据库原子重启测试、应用层合法/非法选择测试和 Engine 操作契约通过。
- 阶段 4 开始，接通通知和平台绑定。

## 2026-08-03

- **状态：** 方案完成，尚未开始实现。
- 核对了当前移除状态、并发限制、稳定错误、查询入口、平台绑定和产品端使用情况。
- 确定 `uc-application` 继续作为完整流程的唯一负责人。
- 形成第二次移除明确错误、当前状态查询、具体等待设备、状态变化通知和永久离线恢复的统一方案。
- 完成可实施性复核：保留现有结果取值；冲突详情通过当前状态查询提供；永久离线恢复使用同一移除标识下的有序多轮安全更新。
- 新增文件：
  - `.planning/2026-08-03-member-removal-deadlock-recovery/task_plan.md`
  - `.planning/2026-08-03-member-removal-deadlock-recovery/findings.md`
  - `.planning/2026-08-03-member-removal-deadlock-recovery/progress.md`

## 检查结果

| 检查 | 结果 |
| --- | --- |
| 当前实现与方案逐项对应 | 完成 |
| 唯一负责人和产品端入口 | 完成 |
| A/B/C/D 等待与永久离线场景 | 完成 |
| 生产代码修改 | 未开始 |
| `cargo metadata --locked --format-version 1` | 通过 |
| `cargo check --workspace --all-targets --locked` | 通过；仅有一条任务前已有的未使用引用提醒 |
| `cargo fmt --all -- --check` | 通过 |
| `node scripts/architecture/check-engine-repository.mjs` | 通过；包含 4 项 OpenMLS 验证 |
| `git diff --check` 与文档行尾检查 | 通过 |
| 行为测试 | 未执行；本次只输出方案，没有修改生产行为 |

## 最终验收（2026-08-04）

| 检查 | 结果 |
| --- | --- |
| `cargo metadata --locked --format-version 1` | 通过 |
| `cargo check --workspace --all-targets --locked` | 通过 |
| `cargo test -p uc-core --locked` | 通过 |
| `cargo test -p uc-application --locked` | 通过 |
| `cargo test -p uc-infra --locked` | 通过；部分网络和基准检查按环境要求跳过 |
| `cargo test -p uc-engine --locked` | 通过；局域网多播检查因环境限制跳过 |
| `cargo test --workspace --locked` | 通过；平台绑定与移动验收宿主包含在内 |
| `cargo fmt --all -- --check` | 通过 |
| `node scripts/architecture/check-engine-repository.mjs` | 通过，含 4 项 OpenMLS 验证 |
| `git diff --check` | 通过 |
