# 规格 021 实施进度

## 2026-08-13

- 读取 `implement` 与测试先行规则。
- 审计规格和当前公开入口，确认必须继续实现而非仅交付文档。
- 建立实施完成标准与阶段计划。
- 写入首组核心红测，覆盖可信来源/精确目标和从成员历史查询已完成决定。
- 首次红测命令因传入两个过滤参数被 Cargo 拒绝，尚不计为有效红测。
- 核心测试文件因缺少可信待决定事实和已完成决定查询按预期失败。
- 最小实现完成后核心测试 15 项全部通过。
- 补充历史设备资料红测并转绿；已移除设备仍可从加密历史返回可信展示资料。
- 应用完整查询红测转绿，覆盖三设备来源、目标、两种影响和逐设备四维事实。
- 稳定决定红测转绿，覆盖首次保留、相同重复、相反重复及本机退出二次确认。
- 应用 facade 编译通过；只有仓库原有警告。
- Engine 稳定接口红测按预期因缺少查询、决定结果和提醒失败。
- 补齐完整查询、五类决定结果与仅带版本号的提醒后，公开接口 3 项验收全部通过。
- 修复测试公共装配缺少在线状态替身的问题，Engine 提醒测试恢复通过。
- iOS/Android、HarmonyOS 和移动探针的完整字段透传红测均按预期失败后转绿。
- 加密状态仓库 3 项通过，证明重启可读且敏感标记未明文落盘。
- 三设备应用变化、保留当前组和待决定重启场景通过；四设备并发场景首次在决定前基线发送阶段失败，正在补强前置等待。
- 严格复审发现锁定状态的新查询会返回错误；先补真实三设备红测，再返回稳定不可用结果，完整锁定、恢复与实际传输场景通过。
- 严格复审发现旧决定入口未共享新入口的串行和幂等规则；回归测试先按预期失败，统一决定锁并补相同决定重复处理后转绿。
- 首次并发回归测试受调度影响直接通过，改成确定顺序的跨入口重复测试后才获得有效红测。
- 严格复审确认完整查询过早释放权威状态锁，已收紧为同一逻辑时点构建结果。
- 正式 Engine、UniFFI、HarmonyOS 和移动验收宿主已删除旧查询、旧决定和旧提醒；诊断构建仍可供多设备测试观察内部状态。
- 默认构建和启用诊断能力的 Engine 构建均通过编译检查。
- UniFFI、HarmonyOS 和移动探针完整字段测试通过；移动宿主边界测试的字段文本检查不适用于统一序列化，已改为验证新操作边界。
- 锁定真实场景复跑发现仓储锁定被门面压扁为普通失败；新增精确未解锁分类，直接锁定和仓储锁定都保留该语义，其他仓储失败不受影响。
- 全工作区首次运行发现迁移后的新建空间宿主断言过强，已改为验证稳定本机标识和无虚构变化；另一个既有邀请测试仅在全套并行时失败，正在复跑确认。
- 全工作区第二次运行中，应用、核心、Engine 正式契约等测试已通过，最终被既有配对场景阻断；该场景在当前工作区单独复跑和 `git archive HEAD` 未改基线中均稳定出现同一失败，确认不是本次设备信任改动引入。
- DTR-01 至 DTR-04、DTR-06 至 DTR-11 已获得接收端保存、应用或等价强证据；DTR-05 因环境缺少 `UC_V019_CLI`、`UC_V11_A_BIND_PORT` 和 `UC_V11_B_BIND_PORT` 跳过；共同设备组恢复与 DTR-12 未实施。
- `cargo metadata --locked --format-version 1`、全工作区全目标编译、格式检查、架构检查和差异检查全部通过。
- 核心规则 16 项、应用设备信任 9 项、Engine 正式契约 4 项、UniFFI 1 项、HarmonyOS 1 项和移动验收宿主 21 项复跑通过；未把一次零命中过滤当作验证。
- 最终边界扫描确认旧查询、旧决定和旧提醒只在诊断构建中存在；正式 Engine 与三个移动平台只暴露完整查询、稳定决定和替换式提醒。新增内容未发现禁止写法或身份、变化编号和文件信息日志。
