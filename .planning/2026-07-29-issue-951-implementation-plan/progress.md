# Issue #951 规划进度

## 2026-07-29

### 实施阶段 0：安全止血与方案验证

- **状态：完成**
- 已恢复规划上下文并确认产品代码尚未修改。
- 已新增真实日志捕获测试，并确认修复前因两条日志均包含密钥前四字节而失败。
- 已删除 `HmacProofAdapter` 生成与校验失败日志中的密钥片段，同一回归测试已转绿。
- 已新增 ADR，固定威胁模型、提交点、离线语义、旧客户端策略和恢复原则。
- OpenMLS 四项行为测试通过：撤销隔离、离线补提交、分叉恢复、冷存储恢复。
- 桌面、iOS、Android、HarmonyOS Rust 目标编译通过；模拟器与真机均跳过。
- 优化验收程序为 1,535,632 字节，记录为阶段 0 体积基线。

### 实施阶段 1：密钥代次领域模型与加密持久化

- **状态：完成**
- 领域模型与端口的 7 项契约测试通过。
- SQLite 仓储的 6 项测试通过：重复开始幂等、敏感负载密文落库、原子激活、失败整体回滚、暂存内容重启恢复、未完成流程重启发现。
- `uc-infra` 全量单元测试通过：549 项通过，4 项按原有条件忽略。
- 新增迁移暴露的旧隐私测试固定迁移数量问题已修复并回归通过。

### 实施阶段 2：单密钥读写迁入密钥目录

- **状态：完成**
- 会话已从单一主密钥扩展为加密持久化的密钥目录，解锁和静默恢复均会重新装载。
- `legacy-v1` 保留读取旧数据；当前密钥按正文、传输等用途隔离。
- 普通业务密文、文件密文和分块传输的新写入已分别切换到 V2、新版 UCBL 和 V4，旧格式继续只读兼容。
- 新格式已绑定 Space、代次、密钥编号、用途和业务身份；缺少新密钥、错误代次或篡改时失败关闭，不回退旧密钥。
- 格式与会话目录定向测试共 16 项通过；`uc-engine` 回归 103 项通过、1 项失败。
- 当前唯一失败：安全存储恢复后强制重建搜索索引，关键词查询返回零条；下一步核对重建时读取历史正文和投影跳过路径。
- 已确认根因是两个恢复入口没有使用 `SetupStatus` 中持久化的真实 Space，导致重启后装入错误密钥目录；不是搜索密钥或索引算法问题。
- 已先增加普通历史读取断言和两个入口级 Space 身份测试，三项均在修复前按预期失败。
- 安全存储解锁与空间恢复入口已统一使用持久 Space 身份；两个入口级测试及重启后普通历史与关键词搜索端到端测试均通过。
- 下一步补旧安装缺少 Space 身份时的稳定兼容路径，然后重跑阶段 2 全套回归。
- 旧安装缺少 Space 身份时，口令解锁现使用稳定兼容身份；定向红灯转绿。
- 阶段 2 最终回归：`uc-engine` 104 项全部通过，`uc-application` 711 项全部通过。
- 格式检查发现一处标准换行差异，已运行仓库格式化修正。

### 实施阶段 3：把配对和准入切换到群组代次

- **状态：完成**
- 首要硬门槛：配对双方必须安装同一份当前密钥目录；加入设备不得在缺失目录时自行生成随机当前密钥。
- 已核对配对 wire、JoinOffer、Sponsor/Joiner 握手和证明适配器，确认旧流程在确认前共享整份 keyslot，并用内容主密钥生成准入证明。
- 已把阶段 3 拆为目录可移植化、准入证明换根、MLS 产品接入、wire v4 和端到端五个切片。
- 目录可移植化红灯已通过：joiner 使用不同本机保护根安装 sponsor 目录后，可读取 sponsor 的 V1 历史密文，本机根保持不变。
- 密钥目录已升级为 v2 并显式包含 `legacy-v1`；V1 JSON、旧 UCBL 和 V3 传输读取均改从目录取历史密钥。
- 会话目录现有 4 项测试继续通过。
- 下一步为“口令派生结果 + 本次邀请码”的准入证明建立端口和失败测试，移除证明对内容主密钥的读取。
- 新准入 offer 与证明派生端口已完成：offer 不含被包装的内容主密钥，证明受邀请码、配对会话和 Space 共同绑定，定向测试通过。
- OpenMLS 产品基础模块已完成：sponsor/joiner 可通过 KeyPackage 与 Welcome 建立同一代次包装密钥，群组快照冷恢复后可继续加入下一台设备。
- 群组基础模块的身份错配、错误待加入状态和冷恢复测试均通过。
- 下一步把群组代次、完整密钥目录和现有加密仓储组合成原子安装流程，再切换配对 wire v4。
- 配对 wire 已升级到 v4；旧版本、未知安全能力和缺失 KeyPackage 均失败关闭。
- Sponsor 握手已改用邀请/会话/Space 绑定的准入证明，证明通过后通过 MLS Welcome 推进群组代次并换当前内容密钥。
- Joiner 私有待加入状态只留在本机；收到 Confirm 后验证 Welcome 和加密目录，再创建独立本机保护根并保存。
- 安装失败回滚测试通过：原空间、原本机保护根和原 keyslot 均保持不变。
- 不同本机保护根、共享历史目录、当前内容密钥一致和冷恢复测试通过。
- 阶段 3 定向验证：配对 66 项、wire v4 10 项、MLS 3 项、准入与安装 4 项全部通过。
- 生产证明校验已移除内容主密钥 fallback；阶段 3 完成，开始阶段 4 可靠撤销状态机。
- 阶段 4 首次定向编译通过：`uc-infra`、`uc-application`、`uc-engine` 全目标检查成功；新增的群组移除与提交应用接口没有编译错误。
- 已确认可靠撤销的前置条件：新增第三台设备时，已有保留成员必须先应用成员新增提交；后续三设备测试与 outbox 会同时覆盖新增和移除两类提交。
- OpenMLS 三设备产品回归通过：Bob 先应用 Charlie 的加入更新，再应用 Charlie 的撤销更新后与 Alice 得到同一新代次；Charlie 无法应用撤销更新，也无法取得新代次密钥。群组模块共 4 项测试通过。
- 撤销分发进度已实现并通过 7 项仓储回归：激活后进入发送中，逐设备确认加密保存，重启可继续，最后一台确认后进入完成并清理临时消息。
- 产品安全适配器的可靠撤销测试通过：三成员空间撤销一台后，本机内容密钥前进一代，待发送消息只包含保留设备，并返回真实的待确认数量。
- 成员入口已改为先执行安全撤销、成功后再清理本机成员记录；注入失败测试与原有移除回归共 7 项通过，可靠撤销失败时不会删除任何成员、地址或信任记录。
- `uc-engine` 已增加结构化撤销结果并通过稳定结果映射测试，能够区分仅本机、本机已生效且等待确认、全部完成，并保留撤销编号与待确认数量。
- 生产装配已注入可靠撤销端口，UniFFI 已同步返回结构化结果；`uc-engine` 与 UniFFI 全目标检查通过。
- 产品级三设备接收端测试通过：Bob 依次应用 Charlie 的加入更新和撤销更新后，与 Alice 的群组代次和当前内容密钥完全一致。
- 应用层送达编排测试通过：待办发送成功后逐台确认，最后一台确认会把结果从等待推进到完成。
- 专用群组更新 P2P 通道已接入共享 Iroh 节点：只接受已知成员，接收端成功安装后才确认；生产门面已使用该通道发送撤销待办。`uc-infra`、`uc-application`、`uc-engine` 全目标检查通过。
- 撤销记录现加密保存保留设备快照；应用启动会恢复未完成流程，查询撤销状态时会重试未确认设备。完整工作区全目标检查通过，包含 UniFFI、HarmonyOS 与移动验收宿主。
- 成员新增提交已改为随新群组状态一起进入加密待办；Sponsor 从真实成员仓储筛选旧成员，排除本机与新设备。
- 新增待办会在加入完成后立即发送，并在启动、每 30 秒和设备重新在线时重试；后台任务由引擎持有并在关闭时停止。
- 数据库验证通过：收件人和提交内容无明文，重启后待办仍存在，确认后消失。
- 三设备验证补充了重复更新幂等与本机待办继承；修复了接收新代次时清空本机待办的问题。
- 撤销激活验证补充了待办继承与目标过滤；修复了激活覆盖加入待办、以及向被撤销设备保留旧待办的问题。
- 补发顺序固定为先加入、后撤销，应用层顺序测试已由红灯转绿。
- 已把成员更新送达、Sponsor 成员筛选、可靠撤销与发送顺序涉及的端口测试替身改为 `mockall`，并用 helper 集中配置。
- 首轮定向回归中，送达、撤销成功和撤销失败测试通过；Sponsor 旧成员筛选测试因额外创建了一个未使用的严格 mock 而失败，已改为只创建该测试实际使用的依赖，等待重跑。
- Sponsor 单项回归已通过；完整 Sponsor 测试发现 5 个“不开始配对”的既有用例不应强制调用准备接口，helper 已改为允许 0 或 1 次并继续禁止重复调用。成员撤销整组 19 项全部通过。
- 应用层 715 项、核心 7 项、存储层 578 项（4 项环境跳过）、UniFFI 33 项、HarmonyOS 26 项通过。
- `uc-engine` 全目标回归在配对端到端用例中稳定失败，加入方报告密钥材料损坏；单独重跑仍失败，已进入分阶段诊断，不能计整仓回归通过。
- 配对端到端失败已定位：Joiner 错误要求 Sponsor 的会话编号等于本机拨号句柄。已删除错误比较，把单元测试默认夹具改成两端使用不同编号，并恢复同一 Space 身份断言，等待回归。
- 删除错误比较后，端到端流程暴露测试装配仍未注入密钥代次仓储，Sponsor 在确认前失败而 Joiner 等待超时；已改用临时真实 SQLite 仓储，等待原用例重跑。
- 原始配对端到端用例已通过，耗时 0.90 秒；加入方会话编号差异和测试仓储装配两个问题均已消除。
- 稳定入口全量回归继续运行时，3 个在线状态用例因同样的旧装配等待超过 60 秒；已停止运行，并把临时真实 SQLite 仓储接入其余 3 个配对验收用例。
- 配对、在线状态和剪贴板端到端用例已全部重跑通过；mDNS 用例因当前环境不支持局域网组播而跳过。
- 本功能新增的端口测试替身已全部改为 `mockall`，由 helper 集中配置；状态型安全存储和撤销仓储替身也已按同一规则收口，真实 SQLite 回归保持不变。
- 最终自动回归通过：应用层 715 项、存储层 578 项（4 项环境跳过）、稳定入口全目标、UniFFI 33 项、HarmonyOS 26 项均无失败。
- 实体设备矩阵未执行，记为跳过；本次不发布，没有产物目录，因此发布包核验未执行。

### 阶段 1：Issue 与仓库核对

- **状态：完成**
- 已读取 issue 正文、元数据和时间线。
- 已核对当前成员移除、网络准入、配对自证、密钥会话、密文格式和迁移机制。
- 已确认现有实现只达到“本地隐藏”，issue 的四项验收要求均未完成。

### 阶段 2：设计与实施顺序

- **状态：完成**
- 已选择“成熟群组代次协议 + 内容密钥目录”的推荐方向。
- 已定义不重写历史的旧数据迁移方式。
- 已定义激活后只向前恢复的撤销状态机。
- 已拆分为七个按依赖顺序执行的阶段。

### 阶段 3：验证设计

- **状态：完成**
- 已定义三设备、离线、崩溃、分叉、重放、混合版本、明文扫描和发布验证。
- 已运行本次文档改动所需的仓库检查，并复核规划文件一致性。

### 阶段 4：交付

- **状态：完成**
- 实施计划、调研结论和验证记录均已落盘。
- 产品代码未修改。

## 已遇到的问题

| 问题 | 处理结果 |
|------|----------|
| 通用成员更新待办改造后的首次窄范围编译缺少现有成员参数，并仍有测试读取旧字段 | 预期接口红灯；接入真实成员列表、统一测试调用后重跑 |
| 记录上述失败时首次补丁按不存在的“错误与处理”标题定位 | 补丁未生效；改按实际“已遇到的问题”表格写入 |
| 阶段 2 首次 `uc-engine` 回归有 6 项启动、恢复、文件流程失败 | 集中定位共同的目录迁移接线问题，修复前均不计通过 |
| 重启后历史和搜索均为空 | 临时开启单测内部日志，确认恢复入口装入了错误 Space 的新密钥目录；移除临时日志后改为读取持久 Space，回归转绿 |
| 阶段 2 格式检查发现一处自动换行差异 | 运行 `cargo fmt --all` 修正；不涉及行为变化 |
| 阶段 2 首次 `uc-infra` 全量回归中未知设备网络拒绝测试遇到对端提前关闭 | 单测通过且随后全套 563 项通过，确认是一次并发时序波动 |
| V4 传输首次编译时共享 AAD 模块只对 security 子模块可见 | 将模块可见性扩大到当前 crate 后重测 |
| 基础设施全量测试中旧隐私迁移测试硬编码回退 4 次，新增迁移后无法到达旧表结构 | 改为检测旧字段出现后停止回退，消除对后续迁移数量的依赖 |
| 撤销仓储首次编译时 8 处会话密钥错误无法转换为领域错误 | 统一在仓储边界映射为仓储错误后重测 |
| 密钥状态实现与导出组合补丁受格式化后的单行导出影响 | 整体未生效；拆成实现和导出两个补丁完成 |
| 冷恢复测试补丁按格式化前上下文定位失败 | 补丁未生效；重新读取文件后拆分加入依赖、提供器和测试 |
| 分叉恢复构建器直接返回 `Welcome`，测试重复执行了网络解包 | 删除重复解包后重新运行分叉行为测试 |
| OpenMLS 验证测试前两次编译使用了内部测试便捷接口，并把 provider 当作 crypto 传入 | 改为按公开接口执行消息序列化往返、检查消息类型，并通过 provider 的 crypto/storage 子接口访问 |
| 代码与进度文件的组合补丁上下文不匹配 | 整个补丁未生效；拆分为两个独立补丁后完成修改 |
| 首次定向测试使用 `--exact`，完整模块名未匹配，实际执行 0 项 | 不计为测试证据；改用名称筛选重新运行 |
| 初始环境无法访问 GitHub | 权限更新后绕过失效代理，`gh` 已恢复 |
| 浏览器备用路径不可用 | 停止重复尝试，改用恢复后的 `gh` |
| `gh` 字段与当前版本不兼容 | 使用当前版本支持的字段后成功读取 |
| 自检补丁定位错误 | 原补丁未生效，拆分后完成更新 |
| 阶段 4 分发进度红灯因缺少开始发送、确认收件人和确认状态接口而无法编译 | 这是预期的 TDD 红灯；下一步补充领域模型与加密仓储实现 |
| 分发仓储首轮实现后，配对测试使用的内存仓储缺少两个新增接口 | 给该测试仓储补同语义的最小实现后重跑，不修改生产流程 |
| 可靠撤销适配器红灯首次还缺少测试内的状态类型导入 | 修正测试导入后重跑，确认唯一失败原因是产品入口尚未实现 |
| 成员入口安全顺序红灯因缺少可靠撤销依赖构造器和错误类型而失败 | 预期红灯；下一步注入窄端口并保持安全流程失败时不做任何删除 |
| 稳定撤销结果红灯因缺少结构化结果、映射函数和新错误分支而失败 | 预期红灯；同步补齐稳定结果与脱敏错误映射后重跑 |
| 三设备接收端红灯因加入结果丢失旧成员更新、适配器缺少应用更新入口而失败 | 预期红灯；补齐不透明更新和严格校验安装后重跑 |
| 接收端接口的组合补丁未匹配两处配对测试样例 | 整个补丁未生效；读取实际片段后拆分更新 |
| 撤销消息送达红灯因缺少待办读取、专用发送端口和发送错误类型而失败 | 预期红灯；补齐不透明待办与成功后确认编排 |
| 送达实现首轮构建被空间设置测试的旧假适配器缺少新接口阻断 | 给测试替身补明确的不可调用实现后重跑 |
| 禁止项修正首次匹配到节点构建器中的既有相似代码 | 恢复误触位置，只修改新增群组更新接线 |
| 并发撤销红灯证明仓储允许同一空间同时撤销不同成员 | 保留同目标幂等，不同目标在前一流程完成前明确拒绝 |
| 应用层定向测试命令一次传入两个过滤词，Cargo 拒绝第二个参数 | 不计为测试证据；拆为独立定向命令运行 |
| 接收新代次的待办继承修复首次命中同文件更早的相似构造位置 | 编译失败且改动未生效；恢复误触位置后按完整上下文修改正确接收路径 |
| 撤销激活修复首次引用了不存在的事务内读取辅助函数 | 新增与现有加密读取边界一致的事务内读取函数后转绿 |
| Sponsor mockall 转换后旧成员筛选测试报告预期调用 0 次 | 测试误从默认 helper 创建了未使用的额外 mock；改为只创建实际注入的 mock 后重跑 |
| Sponsor 完整测试中 5 个无会话用例报告准备接口未调用 | 这些用例有意不开始配对；默认 helper 改为允许 0 或 1 次，失败 helper 仍严格要求 1 次 |
| `slice1_handshake_e2e` 加入方稳定返回密钥材料损坏 | 单独重跑排除并行干扰；正在区分空间身份、代次、目录解包和会话安装四个阶段 |
| 配对探针显示 offer 会话编号与 Joiner 本地句柄不同 | 两者本来就是各端本地编号；证明继续绑定 Sponsor 发送的编号，删除无效的相等检查并补回归 |
| 修复会话编号后配对端到端用例长期等待 | 日志确认 Sponsor 缺少密钥代次仓储而无法发送确认；测试装配改用临时真实 SQLite 仓储 |

## 检查结果

| 检查 | 结果 | 说明 |
|------|------|------|
| 旧会话恢复 | 通过 | 没有遗漏的历史规划上下文 |
| Issue 权威内容 | 通过 | 正文、标签、状态和时间线均已读取 |
| 代码路径核对 | 通过 | 当前限制与 issue 威胁模型一致 |
| `cargo metadata --locked --format-version 1` | 通过 | 识别 12 个工作区成员，包含 OpenMLS 验证项目 |
| `cargo check --workspace --all-targets --locked` | 通过 | 全部完成；仅有一条既有未使用引用提醒 |
| `cargo fmt --all -- --check` | 通过 | 无格式问题 |
| `node scripts/architecture/check-engine-repository.mjs` | 通过 | 架构检查和反例检查均通过 |
| `git diff --check` 与规划文件检查 | 通过 | 无空白或路径问题 |
| `cargo test -p uc-infra revocation_repo::tests --lib` | 通过 | 6 项仓储与恢复测试全部通过 |
| `cargo test -p uc-core --test key_epoch_contract` | 通过 | 7 项领域契约测试全部通过 |
| `cargo test -p uc-infra --lib` | 通过 | 578 项通过，4 项环境跳过，0 项失败 |
| `cargo test -p uc-application --lib` | 通过 | 715 项通过，0 项失败 |
| `cargo test -p uc-engine --all-targets --quiet` | 通过 | 稳定入口及配对、在线状态、剪贴板端到端均通过；mDNS 1 项环境跳过 |
| `cargo test -p uc-engine-uniffi --all-targets` | 通过 | 33 项通过，0 项失败 |
| `cargo test -p uc-ohos-napi --all-targets` | 通过 | 26 项通过，0 项失败 |
