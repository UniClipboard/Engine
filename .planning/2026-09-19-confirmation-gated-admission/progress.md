# Progress

## 2026-09-19

- 用户确认目标语义：最终确认前不得成为正式成员或传播给第三设备；要求测试先行、严格审查、本地提交，禁止真机与远端操作。
- 读取 implement、TDD、codebase-design、Uni Rust、strict review、branch guard、atomic commit 与 planning 技能。
- 确认测试 seam：Engine 完整多实例操作、SpaceAdmissionProtocol 完整入口、必要的 Core 纯状态不变量。
- 读取根与相关局部 AGENTS、架构入口、Rust 规范、Space Application 和成员历史职责。
- 新建本轮独立计划；下一步盘点当前状态机、持久格式、消息版本和现有修改。
- 工作区盘点确认 28 个既有修改/新增路径，全部保留。
- 定位协议消息链和三个主要实现点：Sponsor Complete、Joiner activation、Sponsor Settled；下一步精读保存顺序和正式成员效果接口。
- 精读确认正式成员当前在 Sponsor 处理 Applied 时提前写入，CompleteAck 只做结清；已形成把唯一正式提交点移动到 CompleteAck 的最小方案，下一步先建立完整红测。
- 选择两层红测：Application 完整协议入口锁定正式激活时机；Engine 三实例通过 dev-tools 闸门停在最终确认前，观察第三设备成员视图。未修改生产协议。
- 盘点 Engine 组装和现有 dev 故障注入：没有可直接暂停最终确认的入口；计划先添加不改变生产行为的测试事件/闸门，再运行红测。
- 新增 Application 协议红测并实际运行：0 通过、1 失败，断言明确命中“CompleteAck 前已发布正式成员”。
- 开始设计 Engine dev-tools Joiner 最终确认闸门，确认需要贯穿会话重建而非只包首次实例。
- 已加入 dev-tools 专用最终确认闸门并贯穿 Engine 会话重建：真实 Joiner 本机激活完成后可暂停，测试可等待和释放；非 dev 构建无此路径。尚未改 Sponsor 正式提交时机。
- 三实例红测实际失败：最终确认暂停期间 Sponsor 已显示 3 个正式成员；第三设备 5 秒内仍为 2，需扩大到维护周期确认传播行为。正式协议仍未修改。
- 第三设备观察扩大到 35 秒仍未自动更新；下一次红测将在确认暂停时重启 Sponsor，以启动恢复驱动历史传播并同时覆盖邀请方先重启。
- 将 Sponsor 正式成员激活从 Applied 阶段移动到 CompleteAck 验证之后；Applied 状态继续加密保存同一份待确认材料，供重启与重放使用。
- Application 提交时机回归转绿；Engine 三实例回归转绿，覆盖最终确认前 Sponsor 重启、第三设备通信、确认后才同步为正式成员。
- 先写 V3 协议红测并确认编译失败，再加入当前协议版本；随后新增两向混合版本真实通道测试。新版加入旧版最初被误归类为身份失败，现已修正为稳定升级结果；旧版加入新版在读取邀请凭据前拒绝。
- 新增 Sponsor 正式提交成功、准入状态提交竞争失败的重试回归；同一 CompleteAck 重试完成收尾且正式激活只记一次。
- 新增 Joiner 最终确认发送前断网并重启的 Engine 回归，确认重启前公开“处理中”、Sponsor 未增员，重启恢复后加入和正文传输成功。
- 先把本机准备后的状态期望改为 Processing 并确认红测，再新增 Application 与 Engine 公开状态及 JSON 契约；避免等待 Settled 时提前显示已加入或仍混作初始 Pending。
- 三设备回归进一步锁定邀请方公开状态：重启后仍单独显示待确认候选，正式设备列表与第三设备均不包含它；最终确认后待确认项消失并成为正式成员。
- 普通切换、切换后返回旧空间、旧异常身份正常移除后再邀请、移除后同设备重新加入均已单项通过。
- 纳入 007 Desktop 现场验收：加入方最终确认请求未送达时记录加入编号，进程重启后连续两次公开查询仍返回同一编号，随后沿同一流程完成加入；宿主无需保留第二份状态。
- Engine 完整单元组首次出现既有剪贴板测试过早读取 Sponsor 状态；改为等待 Sponsor 公开确认后的正式成员，再发送内容。定向复验通过。
- Engine 完整单元组复验通过：235 项通过、4 项按原条件跳过。Core、Application、Infra 与公开接口相关完整测试均通过。
- 严格复查发现新版网络入口虽拒绝旧版本，但身份校验上下文仍写死旧版本编号；已改为绑定当前版本并新增回归。两向混合版本测试和两项最终确认完整场景复验通过。
- 严格复查还发现测试中的重试轮询会持续空转；已改为短间隔等待，完整 Engine 单元组随后通过。稳定接口文档补齐 Processing、同一加入编号跨重启恢复及最终确认唯一提交点。
- 首轮全仓编译发现 HarmonyOS 绑定未处理 Processing；随后同时补齐 HarmonyOS 与 Apple/Android 共用绑定及声明文件，并加入各自映射测试。
- 最终交付检查全部通过：全仓所有目标编译、格式、Rust 规则、架构边界、观测隐私、Collector 隐私、OpenMLS 验证和差异检查均通过；临时诊断标记无残留。第二轮严格复查无新增问题。
