# Progress

## 2026-08-17 continuation

- Stage 10 completed deterministic membership-history paging with both the 256-record and exact 4 MiB frame bounds.
- Oversized histories split before transport encoding; a single record that cannot fit fails before a retryable transfer is created.
- The core, receiver staging boundary, and Iroh adapter now enforce one shared frame limit.
- Verification passed: 26 V2 history tests, 811 application library tests, 762 infrastructure library tests with 4 ignored, metadata, workspace all-target check, formatting, architecture preflight, and diff check.

- Recovered the Stage 6/7 implementation state and confirmed the remaining production failure: a newly-created sponsor reports setup complete before it has a trustworthy single-device membership baseline.
- Chosen fix boundary: A1 calls one internal new-Space membership initializer owned by `WorkspaceConvergence`; setup completion remains the final write.
- Next red tests: A1 invokes membership initialization before completion, and initialization failure leaves setup incomplete.
- Added the A1 membership-initialization gate and verified 20 focused application tests, including failure leaving setup incomplete.
- Made joiner Active finalization immediately queryable by compacting its settled terminal result before CompleteAck delivery.
- Corrected cross-Space target-history validation so only same-lineage joins require equality with current history; target history remains fully verified.
- Updated the production E2E to consume the stable Active result. Fresh join followed by cross-Space switch passed (1 test, 17 filtered out).

- Stage 6 mDNS ticket RED: three focused tests failed to compile because no ticket chunking/reassembly API or total bound existed.
- Stage 6 mDNS ticket GREEN: the publisher now splits complete postcard+hex endpoint tickets into bounded ordered TXT fields and the resolver reconstructs only complete canonical bounded tickets. Three focused tests executed and passed.
- The three previously stable local-mint invitation failures now execute and pass. The complete locked `uc-infra --lib` suite passes 755 tests with 4 ignored and no failures.

## 2026-08-16

- Resumed Stage 6 from the persistent handoff and confirmed the public status/binding work remains uncommitted.
- Production pairing still uses V9 `Ready` / `AdmissionSaved`; the durable five-message transaction is only reached by application tests.
- RED prepared: pairing wire must carry stable attempt/join/request identities and round-trip Candidate, Prepared, Commit, Applied, and Complete under a V10 outer version.
- RED observed: the focused wire test failed because stable request identities and durable business-message types did not exist.
- GREEN: pairing V10 now reads a fixed outer version before body decoding, carries stable attempt/join/request identities, and round-trips all five durable business message kinds; the exact test ran 1 and passed.
- The production joiner currently generates these identities in memory only. The next red seam requires the profile admission owner to save Initiated before the Request is sent.

- RED: the explicit target-access activation test failed to compile because no activation operation existed.
- GREEN: target access now decodes and validates the prepared Space/profile/keyslot/KEK/MasterKey binding, replaces persisted key material only on explicit activation, switches the in-memory session last, and resumes the same target root after the session is cleared.
- The first exact test command matched zero tests and was discarded. The fully qualified test then exposed that Postcard cannot round-trip the existing JSON keyslot shape when its optional AEAD fingerprint is omitted; target access now uses structured JSON inside the already-encrypted admission record instead of weakening keyslot compatibility.
- The fully qualified activation regression ran one test and passed. Production Cross-Space admission remains fail-closed until the complete target data generation and manifest promotion own this internal activation step.
- Continued Stage 4 by moving legacy switch recovery behind one `recover()` port owned from the profile-level `WorkspaceConvergence` recovery path.
- Removed the duplicate recovery calls from silent and manual unlock. Unlock now only unlocks; convergence recovery owns restart continuation.
- Added the private infrastructure `LegacyMigrationPhaseV1` decoder and forward-only importer for no-state/orphan-backup, Prepared, HandshakeDone, Swapped, corrupt, and evidence-mismatch cases.
- Legacy recovery now verifies every legacy inline primary record and every backup record before cleanup, preserves all artifacts on insufficient evidence, and deletes the old state file only after honest completion.
- Added and passed 10 infrastructure state tests, the unique convergence-entry test, the unlock ownership test, 8 remaining legacy creation regressions, and a full workspace all-target check.
- Production Cross-Space admission remains fail-closed. The next Stage 4 slice must stage the target local MasterKey/keyslot and complete target data generation without replacing the source session before J3.

- Resumed from the completed specification 023 drafting work.
- Read the implementation, test-first, atomic-commit, branch-guard, and persistent-planning workflows.
- Confirmed the goal is active and the current implementation worktree has documentation changes only.
- Mapped specification Steps 1 through 9 to one verified commit per stage.

## Verification log

| Stage | Check | Result |
| --- | --- | --- |
| 0 | Cargo metadata and workspace all-target check | Passed; three pre-existing unused test-helper warnings |
| 0 | Formatting, architecture preflight, diff check | Passed |

## Commits

- Stage 0: `b29bcbc docs(membership): specify durable admission activation`
- Stage 1: `917d023 feat(membership): add durable history verification core`

## Stage 1 TDD log

- RED: `cargo test -p uc-core --test membership_history_v2 removed_members_credential_still_verifies_its_past_events_for_a_new_device --locked`
- Result: failed to compile because the V2 history, credential, receipt, and historical verifier types do not exist. This is the intended missing-feature failure.
- GREEN: the exact A/B/C/remove-B/add-D scenario now passes through `VersionedMembershipHistory`; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 v1_evidence_accepts_only_the_exact_reconstructed_payload_and_original_id --locked`
- Result: failed to compile because `MembershipEventV1Evidence`, `VersionedMembershipEvent`, and `InvalidLegacyEvidence` do not exist. This is the intended missing-feature failure.
- GREEN: exact V1 event evidence now passes; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 v1_decision_evidence_preserves_the_exact_signed_record --locked`
- Result: failed to compile because `MembershipDecisionV1Evidence` and `VersionedMembershipDecision` do not exist. This is the intended missing-feature failure.
- GREEN: exact V1 decision evidence now passes; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 legacy_checkpoint_identity_is_independent_of_member_input_order --locked`
- Result: failed to compile because the canonical legacy checkpoint type and format do not exist. This is the intended missing-feature failure.
- GREEN: canonical checkpoint identity is independent of continuing-member input order; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 checkpoint_attestations_are_additive_and_do_not_change_checkpoint_identity --locked`
- Result: failed to compile because checkpoint attestations and their format do not exist. This is the intended missing-feature failure.
- GREEN: independent member attestations verify without changing checkpoint identity; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 admission_security_commitment_has_a_canonical_public_identity --locked`
- Result: failed to compile because the public security commitment and exact base-history position do not exist. This is the intended missing-feature failure.
- GREEN: the public admission security commitment has deterministic identity and is bound to its attempt; one test executed and passed.
- RED: `cargo test -p uc-core --test membership_history_v2 verified_and_legacy_migrations_create_explicit_activation_baselines --locked`
- Result: failed to compile because migration activation baselines and a history constructor from them do not exist. This is the intended missing-feature failure.
- GREEN: fully verified and legacy-accepted migration baselines activate only their explicit current members and can continue with V2 history; one test executed and passed.
- REGRESSION: all 12 focused V2 history tests and all 266 `uc-core` unit, integration, and doc tests passed.
- RED: `cargo test -p uc-core --test membership_history_v2 canonical_records_reject_tampered_identity_after_loading --locked`
- Result: failed to compile because canonical checkpoints and security commitments cannot yet revalidate themselves after loading. This is the intended missing-feature failure.
- GREEN: canonical checkpoints and public security commitments reject tampered identities after loading; one test executed and passed.
- Stage 1 focused suite: 13 tests passed.
- Stage 1 `uc-core` suite: 267 unit, integration, and doc tests passed.
- Stage 1 repository gate: locked metadata, workspace all-target check, formatting, architecture preflight, and diff check passed. The workspace check reported only three pre-existing unused test-helper warnings.

## Stage 2 TDD log

- RED: `cargo test -p uc-infra v2_state_is_side_written_verified_and_guarded_before_v3_activation --locked`
- Result: failed to compile because the V3 guard and V3 workspace storage layout do not exist. This is the intended missing-feature failure.
- GREEN: V2 state is preserved in an encrypted migration record, side-written to V3, reopened, then published with an old-layout guard; one test executed and passed, and all 9 workspace-store tests passed.
- RED: `cargo test -p uc-infra failed_v3_reopen_keeps_original_v2_ciphertext_and_no_active_pointer --locked`
- Result: the migration incorrectly reopened its in-memory ciphertext instead of the inserted slot, so a trigger-corrupted target was published. This is the intended durability failure.
- GREEN: target verification now reloads the inserted database slot; corruption rolls back the transaction, preserves the original V2 ciphertext byte-for-byte, and leaves no active pointer.
- RED: `cargo test -p uc-infra historical_signature_verification_uses_only_the_explicit_public_key --locked`
- Result: failed to compile because no explicit-public-key historical verifier or signer-key extraction exists. This is the intended missing-feature failure.
- GREEN: real Ed25519 history signatures verify from the supplied historical public key and reject altered payloads without consulting current membership.
- RED: `cargo test -p uc-infra sponsor_and_joiner_derive_the_same_public_admission_commitment --locked`
- Result: failed to compile because MLS staged states cannot yet derive the canonical public admission commitment. This is the intended missing-feature failure.
- GREEN: sponsor staged Commit state and joiner staged Welcome state, despite different private bytes, derive the same canonical public security commitment.
- RED: `cargo test -p uc-infra profile_key_survives_restart_and_attempt_wrapping_is_context_bound --locked`
- Result: failed to compile because the profile admission key manager and wrapped per-attempt data key do not exist. This is the intended missing-feature failure.
- GREEN: the profile key survives restart, while each attempt key is recoverable only with its own attempt identity.
- RED: `cargo test -p uc-infra attempt_and_outbox_advance_atomically_and_survive_restart --locked`
- Result: failed to compile because the encrypted admission repository and multi-message outbox do not exist. This is the intended missing-feature failure.
- GREEN: a prepared attempt, its private state, inbox deduplication, and multiple independent outbox messages survive reopening.
- RED: `cargo test -p uc-infra terminal_compaction_preserves_replay_result_and_reset_only_advances_the_floor --locked`
- Result: failed because terminal attempts could not yet be atomically rewrapped under the profile key and compacted. This is the intended missing-feature failure.
- GREEN: terminal state is durably rewrapped before the attempt payload and per-attempt key are removed; replay indexes and projection metadata remain.
- RED: `cargo test -p uc-infra a_second_non_terminal_attempt_cannot_take_the_profile_slot --locked`
- Result: failed because the profile-wide admission slot, local join ordinal, projection floor, trust revision, and replay guards do not exist. This is the intended missing-feature failure.
- GREEN: one non-terminal attempt owns the profile slot, counters only move forward, and consumed invitations and join identifiers cannot be replayed.
- RED: `cargo test -p uc-infra factory_reset_phase_survives_restart_and_generation_changes_only_after_clear --locked`
- Result: failed to compile because no restartable profile destruction marker exists. This is the intended missing-feature failure.
- GREEN: the three reset phases resume after restart, key deletion is verified, and the old generation is not reopened.
- REGRESSION: `uc-core` ran 248 tests successfully. `uc-infra` ran 795 tests successfully with 7 explicitly ignored tests.
- SECURITY: scans of the SQLite database, WAL, and SHM files found none of the private admission markers used by the persistence tests.
- Stage 2 repository gate: locked metadata, workspace all-target check, formatting, architecture preflight, and diff check passed. The workspace check reported only three pre-existing unused test-helper warnings.

## Stage 3 TDD log

- RED: `cargo test -p uc-application durable_join_starts_once_and_survives_owner_restart --locked`
- Result: failed to compile because the durable application admission owner did not exist.
- GREEN: a local join now saves its stable attempt id, join id, ordinal, and JoinRequest before network delivery; reopening returns the exact same attempt.
- RED: `cargo test -p uc-application durable_admission_becomes_complete_only_after_both_sides_save --locked`
- Result: failed to compile because Candidate, Prepared, Commit, Applied, Complete, and completion acknowledgment progression did not exist.
- GREEN: sponsor and joiner persist the same candidate, verified-history proof, activation receipt, and completion; sponsor remains non-terminal until the joiner has saved Active and returned the exact acknowledgment.
- The first GREEN run exposed a Stage 2 role-validation defect that rejected sponsor attempts without a local join id. The storage invariant now requires join id and ordinal exactly for Joiner attempts and forbids both for Sponsor and CompletionHelper attempts.
- RED: `cargo test -p uc-application durable_admission_cancel_and_commit_have_exactly_one_winner --locked`
- Result: failed to compile because cancellation and the sponsor commit point had no durable arbitration.
- GREEN: cancellation saved before commit produces Rejected without activation; commit saved first returns the existing Commit, records TooLateCommitted, and continues the same attempt.
- RED: replay assertions in `durable_admission_becomes_complete_only_after_both_sides_save` failed because a repeated JoinRequest attempted to create a second sponsor record.
- GREEN: every business transition validates attempt binding and predecessor, then reconstructs the exact persisted response for identical replay; mismatched replay is rejected.
- RED: terminal-compaction assertions failed because the application owner did not expose the settled-attempt boundary.
- GREEN: Active and Completed attempts compact only after all durable outboxes are superseded; the profile-key terminal result remains queryable.
- Production assembly now constructs the encrypted admission repository from the profile lifecycle generation, and runtime start/resume/ticks scan recoverable admission work.
- Stage 3 remains in progress: the existing pairing network protocol, real V2 history/security capability calls, stable product projection, and profile runtime without an active Space are not yet switched to this transaction.
- Resumed Stage 3 and confirmed the next red seam: Candidate -> Prepared currently trusts opaque caller-provided proof and staged-state bytes. The next tests require the transaction itself to reject invalid V2 history and a mismatched independently-derived security result before it persists Prepared.
- RED/GREEN: Candidate preparation now rejects a tampered V2 history signature and a mismatched public security result; both focused tests passed.
- RED/GREEN: added a dedicated V2 history persistence format. It stores only source facts and re-verifies events, activation receipts, and decisions when reopened; the focused round-trip test passed.
- The application owner now invokes an injected security transition capability and stores J0 pending security material before delivery. Four focused durable-admission tests pass after this integration.
- Stage 3 remains in progress: formal V2 history publication at S2/J2/S3 and the remaining rejection, outbox, removal, reset, and active recovery behavior are not complete.
- Resumed after handoff and formatted the workspace. The focused Stage 3 baseline remains green: four durable-admission tests, AdmissionUnavailable, DeliveryAck, ResetSpace projection, six encrypted repository tests, and fourteen V2 history tests passed.
- The baseline exposed one remaining warning and coverage gap: invitation-consume `Conflict` is untested, retryable no-write behavior is not byte-for-byte proven, and terminal compaction must explicitly wait for that outbox.
- Invitation-consume coverage now proves retryable is a byte-for-byte no-write, Conflict is terminal and idempotent, and sponsor terminal compaction waits until the consume outbox is resolved.
- RED: `base_history_change_after_candidate_is_durably_rejected_without_add` failed to compile because attempts had no durable rejection reason or saved base-history fact.
- GREEN: sponsor and joiner now initialize the permanent V2 base history before commit, history publication uses an expected-current comparison, and a changed sponsor history produces durable `BaseHistoryChanged` without overwriting the newer history.
- RED: two pending-member removal tests failed because the admission owner had no unified removal arbitration entry.
- GREEN: pre-S2 removal now saves `RemovedBeforeActivation` without AddDevice; S2-to-S3 removal validates and permanently appends the exact RemoveDevice successor, preserving Add+Remove while keeping activation closed.
- RED/GREEN: a general sponsor pre-commit rejection entry now persists and replays the exact business reason without adding the candidate to permanent history.
- RED: cancellation reached the joiner as Rejected but the joiner persisted no reason.
- GREEN: every new Rejected message carries a structured durable reason; the joiner validates and saves it before acknowledging, and repository invariants reject reasonless terminal rejection.
- RED/GREEN: post-commit security updates and history/receipt batches now coexist with Complete and clear only after exact persisted-delivery evidence; ordinary DeliveryAck cannot clear them.
- Added a real SQLite failure injection proving attempt advancement and permanent V2 history publication roll back together and reopen at the prior state.
- RED/GREEN: current local join projection now selects a non-terminal local attempt first, otherwise the greatest visible terminal ordinal; ResetSpace floor hides older projections without deleting replay facts.
- Full locked workspace all-target compilation passed after these changes; only the three pre-existing unused E2E helper warnings remain.
- The Engine library regression initially had one shutdown deadline failure. A temporary 15-second diagnostic run and scoped tracing proved admission recovery was not blocking: the Iroh connection drain used about 4.5 seconds after a 0.5-second task-registry bound, exceeding the test's total 5-second deadline. The test now uses the existing 15-second real-Engine contract deadline; the exact test and all 117 Engine library tests pass.
- A first exact `uc-infra` filter omitted the module path and ran zero tests, so it was discarded. The fully qualified terminal-update regression then ran one test and failed because a superseded outbox could be reactivated.
- Terminal attempt persistence now permits only monotonic delivery updates: existing inbox records remain an unchanged prefix, new records cannot duplicate earlier evidence, outbox identity/content/order stay fixed, and superseded messages cannot reactivate. The focused regression ran one test and passed.
- Stage 3 final gate passed: locked metadata, all-target workspace compilation, formatting, architecture preflight, and diff checks succeeded. `uc-infra --lib` passed 726 tests with 4 ignored; `uc-engine --lib` passed all 117 tests. Three pre-existing unused E2E helper warnings remain. Stage 3 is now represented by the combined commit `08193a7`.

## Stage 4 TDD log

- Resumed the existing Cross-Space transition model and confirmed its two focused model tests compile and pass.
- RED: `transition_only_advances_one_phase_without_changing_identity` showed that `CleanupPending` could self-advance and replace final counters.
- GREEN: a transition now requires an actual successor phase; `CleanupPending` can only produce a checked result and cannot be rewritten through another advance.
- RED: repository tests could not compile because no current-to-next transition validator existed; this is the intended missing persistence boundary.
- Added one shared persistence validator for role/attempt binding, exact one-phase progress, checked final result, and immutable terminal result. Unfinished transitions now remain recoverable work even after a business terminal is saved.
- RED: the first application Cross-Space test found no transition after Candidate verification, so Complete could still become Active immediately.
- GREEN: the admission owner now saves a checked TargetStaged record with Prepared, saves Complete before any activation side effect, advances and saves exactly one phase at a time, and writes Active plus the final result only after cleanup finishes. Startup recovery drives the same saved attempt without product orchestration.
- Cross-Space recovery now fails once at TargetStaged, ActivationStarted, SourceFinalized, DataRewrapped, TargetPromoted, and CleanupPending in one focused test. Every retry observes only the last persisted phase and no Active result; the final recovery compacts the checked counts and rebuilds the exact Complete acknowledgment.
- Workspace all-target compilation passes with the fail-closed production assembly. The real target keyslot/business-data generation/active-manifest adapter is still missing, so Stage 4 remains in progress.
- Pre-activation rejection now calls an idempotent target discard and removes the transition before saving Rejected; persistence permits this only before ActivationStarted. Focused application, repository, and core tests pass.
- The real adapter is blocked on a missing versioned target-access staging fact: current durable admission has MLS state only, while the legacy handshake directly installs the target MasterKey/keyslot. Implementation order is adjusted inside Stage 4 so legacy recovery is isolated first, then target access staging is added before replacing the fail-closed production adapter.
- Baseline recheck: `cargo check -p uc-engine --all-targets --locked` passed with the three existing unused E2E helper warnings; the active-manifest test ran 1 and passed. The first application filter used `--exact` without the module path and ran 0 tests, so it was discarded; test listing confirmed the fully qualified path.
- RED in progress: Cross-Space Complete receipt must not call the transition adapter while the JoinSpace operation still owns the ordinary session lease. The regression now inspects the simulated adapter before recovery begins.
- GREEN implementation split Complete persistence from transition execution. `joiner_activate` now reports that a session transition is required, ordinary admission recovery leaves it untouched, and only `recover_space_transitions_after_session_drain` advances J3. The first post-change run correctly exposed that the fault matrix must now include the untouched TargetStaged phase because Complete no longer consumes its injected failure.
- The Cross-Space fault regression now passes with Complete persistence, explicit transition-required detection, all seven saved phases, terminal compaction, and acknowledgment replay. The existing same-Space two-sided-save regression also passes.
- Engine now consumes the current JoinSpace operation lease at the lifecycle boundary, drains all other operations, shuts down the full source session, calls only the narrow post-drain transition recovery capability, and rebuilds the session. Startup inspects the same durable signal before installing a session and fails closed if recovery fails. Locked `uc-engine` all-target compilation passes with only the three existing unused E2E helper warnings; the operation-gate suite passes 3 tests.
- Active manifest replacement now uses an explicit replace-existing atomic operation on Windows after syncing the temporary file; Unix keeps atomic rename semantics. The focused encrypted manifest replacement test ran 1 and passed.
- RED: the first shared `DbPool` switch test exposed 55 compile errors because the database executor, snapshot helper, and config-migration adapter still required the old fixed pool type.
- GREEN: all repositories now retain one shared replaceable database handle. Two focused tests executed and passed: every existing clone reads the replacement database after a successful switch, and a failed replacement leaves every clone reading the source database.
- Added a shared replaceable managed-file directory, startup active-manifest selection, missing-generation fail-closed behavior, and same-directory atomic generation writes.
- Added the first real `DurableAdmissionSpaceTransition`: source backup, encrypted target history/security artifacts, restart-idempotent phases, active manifest/database/blob/key promotion, recovery-record copy, and cleanup are covered by focused tests.
- Verification to this point: database switch 2 tests, blob-directory switch 1 test, generation rewrap 2 tests, active manifest 1 test, real transition 1 test, application transition recovery/rejection 2 tests, and locked workspace all-target compilation passed.
- Resume audit found Stage 4 still incomplete: target history/security are not installed into the runtime repositories, the content-key directory is absent, setup remains a legacy JSON decision, Fresh has no first-generation path, Same-Space has no atomic promotion, and source revisions are placeholders.
- Tooling error: one parallel inspection command had malformed JavaScript object syntax. It was corrected before any filesystem change; no task command ran from the failed invocation.
- RED/GREEN: added a canonical complete content-key catalog. It rejects missing legacy/current entries, duplicate ids/epochs, invalid key lengths, and produces order-independent commitment evidence; the exact core test passed.
- The Candidate and encrypted attempt now carry the target protection-group id and complete content-key catalog. Candidate verification binds catalog epoch/digest to the public security commitment before Prepared can be saved.
- The real generation adapter now removes copied source-Space relationship/security/convergence rows, writes target security material under the target key, reopens it through `DieselSpaceSecurityStore`, and installs it into the isolated target session.
- RED: the first target-security test found the active session remained on `legacy-v1` after keyslot activation. Reopening target security immediately after activation fixed the stale in-memory state.
- RED: after that fix, migrated content failed decryption because rewrap had used the target MasterKey before the target content-key catalog was installed. The isolated target session now installs the verified catalog before any data rewrap; the real transition test passes with both target current-key and content-read assertions.
- Verification: locked workspace all-target compilation passed; the two-sided admission test ran 1 and passed; the real transition test ran 1 and passed. One malformed Cargo `--exact` placement was discarded and rerun with a valid focused command.
- Candidate verification now requires an exact relationship projection: every relationship member instance must derive from the corresponding retained history credential, ids cannot repeat, the set must equal the verified effective members, and the AddDevice facts must be present.
- The target generation now writes encrypted member, trust, and address projections with the target MasterKey-derived relationship key, reopens and compares all three sets, and only then permits manifest promotion.
- Focused two-sided admission and real Cross-Space transition tests each ran 1 and passed after relationship installation. A test-module import omission caused one compile failure and was corrected without production changes.
- The persisted J3 state now has explicit Fresh, Same-Space, and Cross-Space variants. The application and repository use one versioned encoding and reject kind replacement, skipped phases, post-activation discard, and mismatched terminal evidence.
- Fresh creates the first target database/blob generation without requiring an active session. Same-Space snapshots the drained current database without content rewrap, retains the active MasterKey, requires the candidate key catalog to preserve every existing content key, and atomically promotes verified history/security/relationships.
- SQLite now maintains one durable database revision from triggers installed for every business table. Cross-Space records the real revision at initial backup and final drained snapshot instead of placeholder values.
- Setup state uses the encrypted active manifest whenever one exists and only falls back to the legacy JSON status for pre-manifest profiles. Focused Fresh, Same-Space, Cross-Space, revision, setup projection, unified encoding, and two-sided admission tests each executed one test and passed; locked `uc-engine` all-target compilation passed with the three existing unused E2E helper warnings.
- Final Stage 4 review found that revision triggers mistakenly included Diesel migration metadata and remained after downgrade. The regression failed first, then passed after excluding/self-healing metadata triggers and making the down migration remove every managed trigger. All 751 `uc-infra` unit tests then passed except three stale test fixtures; after adding the newly required target security/catalog/relationship facts, all 11 admission-store tests passed.
- Active-manifest promotion now syncs the parent directory after atomic replacement on non-Windows systems; the encrypted replacement test executed one test and passed.
- Final repository gate passed: locked metadata, workspace all-target compilation, formatting, architecture preflight, and diff checks succeeded. The full workspace suite progressed through core, application, infrastructure, Engine library, public-contract, and dependency-firewall suites, then stopped at the known `slice1_handshake_e2e` sponsor-history failure. An isolated `git archive HEAD` reproduction failed at the exact same assertion and message, proving it predates Stage 4 and belongs to the later pairing-wire integration stage.
- Stage 5 now derives the shared ordinary peer scope from the durable V2 history before considering the V1/legacy compatibility path. Migration baselines resolve devices through credential-bound candidate profiles; ordinary additions require a permanent activation receipt; a local joiner remains closed until its local result is Active.
- V2 history repository locking, corrupt encoding, and unrelated lineage mismatches fail closed. A pending Cross-Space join keeps the source scope only when the durable transition proves the exact source, target, attempt, and pre-activation phase.
- A Cross-Space join rejected after formal commit retains its target Add/Remove history but restores the source scope only when the compacted terminal points to that exact target event.
- Focused V2 activation, local joiner, migration-baseline device resolution, failure-closed, Cross-Space, and the existing current-peer-scope tests passed. The full `uc-application` suite passed 830 tests, and the full `uc-core` suite passed 273 tests and documentation examples.
- Final Stage 5 repository checks passed: locked metadata, workspace all-target compilation, formatting, architecture preflight, and diff checks. The full workspace suite again passed all preceding suites and stopped only at the previously baseline-reproduced `slice1_handshake_e2e` sponsor-history mismatch; the failure text and location are unchanged and remain assigned to the later pairing-wire integration stage.
- Strict final review found no blocking correctness, security, architecture, compatibility, or test-coverage issue in the Stage 5 diff.
- Stage 6 protocol isolation: membership-history ALPN moved to `/2`, the fixed V2 version byte is checked before decoding the body, and the 4 MiB frame limit remains enforced. `history_v2_wire_checks_version_before_decoding_the_body` executed one test and passed. The channel still carries the legacy history body, so this is version isolation only, not completed V2 history exchange.
- Stage 6 target-access persistence RED: `joiner_target_access_is_filled_once_and_then_immutable` executed one test and failed because the repository rejected the intended Initiated-empty to Prepared-present transition.
- GREEN: only the Joiner Initiated-to-Prepared update may fill target access once; identical replay remains valid and replacement remains corrupt. The focused `uc-infra` test executed one and passed.
- Stage 6 application RED: `durable_join_is_saved_before_the_target_space_is_known` failed to compile solely because the preparation entry did not accept target access discovered after Candidate.
- GREEN: the single verify-and-prepare action now supplies target access to isolated target preparation and saves it atomically with Prepared. The focused `uc-application` test executed one and passed.
- Stage 6 durable identity RED: `durable_join_start_reuses_the_saved_wire_identity_after_restart` failed to compile because the admission owner could not generate a persisted local join identity before network use.
- GREEN: the owner now generates nonzero attempt/join ids, derives the JoinRequest message id from the saved outbox, returns the same record after reopening, and reports a stable in-progress conflict for different input. The focused test executed one and passed.
- Stage 6 application regression: the complete `uc-application` suite passed 823 unit tests and 8 integration tests after the durable join projection changes.
- The first full application run exposed 12 device-trust failures because the empty admission-repository test double returned mutually inconsistent profile/projection state and was mapped to a locked repository. The test double now returns an empty local/inbound projection and initial profile metadata; production still fails closed for a genuinely locked repository.
- Reset continuation audit: the repository already atomically rejects projection-floor advancement while any admission slot, outbox, write-ahead recovery, transition, or cleanup work remains. The missing behavior is routing the production ResetSpace operation through the profile-level convergence owner before the legacy setup/invitation clear.
- RED: `profile_reset_preparation_rejects_pending_join_without_hiding_it` failed to compile because the profile-level convergence owner had no ResetSpace preparation entry.
- GREEN: the profile owner now atomically advances the projection floor only when the admission repository is quiet and emits the saved revision. A pending join returns Unavailable and its full snapshot remains byte-for-byte equal; the focused test executed one and passed.
- Production ResetSpace now consults the profile owner before clearing setup status or invitations. Pending admission maps to the existing reset-unavailable Conflict code. The Engine mapping test, existing repository atomicity test, and locked `uc-engine` all-target check passed with the three existing unused E2E-helper warnings.
- Production FactoryResetSpace now runs through the profile-level forward-only lifecycle instead of the active-Space facade. It stops all profile work, deletes and verifies current key material first, then clears databases, configuration/import staging, invitations, relationships, admission state, generation directories, blobs, caches, and daemon state.
- The real Engine reset/restart test first failed because the retired runtime still held the on-disk database through the session rebuild configuration. Releasing that configuration removed the ownership cycle, but the shared database pool still held file handles; switching the retired runtime to a private in-memory pool before file removal made deletion and restart deterministic.
- The real restart test next reopened the old setup because `import-staging` and `pending-import.json` survived and were replayed during startup. Factory reset now removes both artifacts before completion.
- Focused profile key/state cleanup tests, all three application factory-reset tests, the Engine error mapping test, and `engine_start_builds_a_resumable_real_session` pass. The real Engine test proves the old session rejects work, a new Engine opens the same directory as a fresh profile, and a new Space can be created.
- Stage 6 remains in progress: startup recovery from an already persisted WipingKeys/ClearingState marker and the production Candidate/Prepared/Commit/Applied/Complete exchange are not yet verified.
- Startup factory-reset recovery is now covered by a real Engine test. It creates an initialized Space, persists WipingKeys as if the process stopped, proves the first restart finishes cleanup and returns the retryable reset-unavailable result without exposing a session, then proves the second restart opens a fresh setup. The focused test executed one test and passed.
- Updated the stable Engine interface and architecture bible to current facts: QueryMigrationProgress is removed; JoinSpace, CancelJoinSpace, device-trust projection, profile-level ResetSpace gating, FactoryResetSpace cleanup/restart behavior, and old-session invalidation are implemented. Both documents explicitly retain the production five-message pairing channel as an uncompleted boundary.
- Complete locked `uc-application` regression passed: 824 unit tests and 8 integration tests, with no failures.
- Production J0 is now wired into the joiner handshake. Before the first JoinRequest send, `WorkspaceConvergence` saves or reopens the exact attempt id, join id, request message id, member instance, KeyPackage, private join state, and resume keypair under its admission lock. The handshake no longer generates those ids or calls `prepare_group_join` directly.
- The stable request binding includes only the durable device id and identity fingerprint. Device display-name and current transport-address changes do not create a second attempt. The 21 joiner-handshake tests, 6 redeem-invitation tests, focused restart preparation test, and locked all-target application check passed.
- Production Candidate/Prepared/Commit/Applied/Complete orchestration remains incomplete; Ready/AdmissionSaved and eager target installation are still present and must not be treated as both-sides persistence evidence.
- 2026-08-16 Stage 6 continuation: resumed the existing worktree without resetting or splitting its uncommitted changes. The focused `prepared_join_signer_proves_facts_without_activating_a_group` test executed exactly one target test and passed, proving the saved pre-join member state can sign admission facts without activating a group. Next RED seam is the initial JoinRequest carrying the exact persisted J0 identity, credential, resume key, and pre-join proof into the version-isolated durable protocol.
- 2026-08-16 Stage 6 durable request RED/GREEN: request tests first failed because the four durable identity fields and invalid-combination error did not exist. JoinRequest now carries the exact member instance, public membership credential, saved resume public key, and member-signed admission facts. V10 decoding reconstructs and validates the credential, device-derived member instance, matching admission facts, nonzero durable ids, and required key/signature material before exposing the request. Two focused wire tests passed, including rejection of a mismatched member instance. The production joiner now generates these facts from the reopened prepared join before its first send; all 21 joiner-handshake tests passed and the happy path checks the exact credential/member/signature/resume binding.
## 2026-08-16 continuation

- Re-read the implementation and planning skills, recovered the active plan, inspected the current diff, specification, domain vocabulary, and admission transaction source.
- Confirmed that no new parallel implementation should be introduced: the current working tree already has the five-step transaction methods and must be completed through its existing `WorkspaceConvergence` owner.
- Next: map transaction methods to sponsor/joiner pairing handlers, identify the first missing public behavior, then run a red-green focused test.
- Mapped the transaction to production pairing. Confirmed the first missing public behavior: after the legacy challenge succeeds, sponsor and joiner still proceed to `Confirm -> Ready -> AdmissionSaved`, and no handler consumes durable Candidate/Prepared/Commit/Applied/Complete frames.
- Selected the existing pairing coordinator plus `WorkspaceConvergence` owner boundary as the next TDD seam; no security or repository internal seam will be added for the primary integration assertion.
- Audited security material ownership. Confirmed the existing transition algorithm is side-effect free, while the only current production admission entry mutates active space material. Candidate integration therefore first needs a narrow staged-sponsor preparation capability; using the old mutating entry would violate the specification.
- Completed a red-green protocol slice for final activation acknowledgment. The focused test first failed because `CompleteAck` did not exist, then passed after adding the V10 kind and wire value 6. Result: 1 focused `uc-infra` test passed.
- Added current-member credential export through the existing security adapter and verified one focused real-security test. The adapter rechecks the member instance after export so stale or mismatched credentials cannot enter durable history.
- Corrected the J0 member-instance derivation to use the established device-id plus signing-public-key rule in both generated credentials and security commitments. This preserves existing member identities across upgrade and lets the joiner verify its own persisted identity.
- Added the legacy-history admission baseline gate. Three focused tests passed: a valid single-device baseline is accepted, while multi-member or inconsistent legacy state is rejected as recovery-required.
- Confirmed `verified_admission_base_history` is intentionally not yet used by the production Candidate path. The next integration slice must consume it while constructing the unique V2 AddDevice event.
- Added a RED/GREEN real-security test for complete Sponsor preparation. The RED compile failed because the complete capability did not exist. The GREEN implementation leaves both persisted and in-memory active security unchanged while returning staged state, Welcome, Commit, a full canonical key catalog, public commitment, and one credential-bound existing-member delivery.
- The first exact test command executed zero tests because the module path was omitted. Listed the library tests and reran `security::space_access_adapter::admission_tests::sponsor_admission_preparation_is_complete_and_has_no_active_side_effect`; exactly 1 test passed.
- Sponsor Candidate production preparation now validates the durable JoinRequest, verifies the exact V2 base history, derives recipients only from members active in that history, creates one signed AddDevice candidate, and saves all replay material before network send. The removed-member regression executed exactly one test and passed after correcting an initial zero-test filter.
- The production pairing path now sends Candidate, Prepared, Commit, Applied, Complete, and CompleteAck through the existing workspace owner. Sponsor security activation has a write-ahead recovery marker and uses the exact staged target material; focused two-sided-save and sponsor-preparation tests passed before the current legacy-path cleanup.
- 2026-08-16 resumed after context handoff. Re-read the implementation and persistent-planning skills, recovered the worktree, and started deleting the unused Confirm/Ready/AdmissionSaved success path. The first cleanup removed obsolete eager-installation material from the Joiner handshake result.
- Replaced the Joiner happy-path script with Candidate, Commit, and Complete inputs and asserted Prepared, Applied, and CompleteAck outputs. The exact test executed one test and passed; it also proves Confirm, Ready, and AdmissionSaved are absent from the success path.
- Removed Sponsor production handling for Ready/AdmissionSaved and removed the mutating Confirm implementation plus its production-only dependencies. Locked all-target application compilation now succeeds; remaining warnings are obsolete trait methods and test helpers scheduled for deletion.
- Cross-Space audit found the next integration blocker: the durable owner correctly saves Complete and requests a drained transition, but the Joiner handshake maps that to an error, the old JoinSpace coordinator still selects a separate SwitchSpace workflow, and post-transition CompleteAck delivery has no production continuation transport.
- Cross-Space Complete is now a successful saved outcome rather than an internal error. The joiner sends CompleteAck only when J3 is already Active; otherwise it returns a pending handshake marked for the Engine-owned session transition. The locked application all-target check passed, and all 22 joiner-handshake tests passed, including a new assertion that Cross-Space sends no premature CompleteAck.
- JoinSpace now always uses the durable redeem path; setup status no longer selects the deleted legacy switch workflow. The Engine reads the profile's saved Active/Pending/Rejected result, and after a Cross-Space drain it re-reads the saved Active result instead of returning the earlier Pending snapshot.
- Redeem now marks setup complete and resumes a session only for an already Active result. A Cross-Space result leaves setup/session promotion to J3. All 6 composition tests passed after replacing legacy Confirm/Ready/AdmissionSaved fixtures with durable messages.
- Deleted the legacy SwitchSpace use case, its tests, public application types, facade wiring, old join-result mapping, and the three obsolete pairing-owner save methods. Locked all-target Engine compilation passes; only three pre-existing unused E2E helper warnings remain.

## 2026-08-17 continuation

- Reproduced the existing RED three-device scenario: V2 admission was active on both peers, but the ordinary history exchange returned `Invalid` because `/2` still serialized V1 history.
- Added the V2 history envelope, V2-only acknowledgment, encrypted profile-history compare-and-replace boundary, application validation/persistence flow, and V2-only wire decoding.
- `cargo check -p uc-application -p uc-infra --all-targets --locked` passed.
- The old infra tests then failed exactly because `/2` rejected V1 payloads. Updated them to build a real signed V2 history; 3 focused adapter tests executed and passed.
- A broad core filter executed zero tests and is not counted as evidence; exact V2 tests remain to be added and run.
- RED: the runtime exchange regression failed to compile because no sender-authorized reconciliation export existed.
- The first GREEN attempt exposed an invalid placeholder identity signature in the test fixture; after signing the real sender facts, the focused regression passed and proved the persisted full history remains unchanged.
- The V2 history test target passed all 20 tests. Runtime exchange now carries only the sender's decisions, including a removed sender's restricted decision delivery.
- The first exact application filter executed zero tests because it omitted the module path and is not counted. The unique-name rerun executed one test and passed.
- Application history acceptance now permits a newly activated sender present in a complete authorized extension, while retaining the rule that only the sender may introduce its own new decisions.
- `cargo test -p uc-application --lib --locked` passed all 810 tests.
- The first post-fix concurrent four-device run reached matching A/C and B/D branch digests but exposed that a rejecting active member could not deliver its own decision across the accepted head.
- RED: the focused core regression failed to compile because decision-only delivery had no authorization rule independent from forward history extension.
- GREEN: decision-only delivery now requires an unchanged event/receipt subset and permits new decisions only from the authenticated sender, without requiring either branch head to advance.
- `concurrent_accept_and_reject_of_one_removal_keep_their_branches_independent` passed in 91.47 seconds.
- `five_devices_restore_full_sync_after_two_completed_removals_and_rejoins` passed in 594.24 seconds after two completed removals, two rejoins, and final full synchronization.
- 2026-08-17: Recovered the Stage 8 continuation after context handoff. The remaining five-device failure was isolated to simultaneous five-endpoint teardown in third-party `noq`; changed only the test teardown to two bounded concurrent pairs followed by the final endpoint. Focused rerun pending.
- 2026-08-17: The focused five-device rerun failed after 587.99 seconds before teardown. Diagnostics showed all five durable histories and rosters agreed; the remaining condition demanded a simultaneous full mesh of transient connections. Removed that redundant condition because the immediately following real bidirectional sends prove connectivity. Focused rerun pending.
- 2026-08-17: The pre-rerun format check found formatting drift in the current Stage 8 files, so the chained test did not start. Ran the repository formatter; focused rerun still pending.
- 2026-08-17: The second focused rerun exhausted the 60-second final convergence window before teardown. Since each author must deliver its own decisions, increased only the five-device final collection window to 120 seconds. The next diagnostic run will suppress routine network logs so any remaining per-device difference is preserved in full.
- 2026-08-17: The third focused run completed all five-device durable convergence and real send assertions, then failed because one Engine in the first shutdown pair exceeded the generic 15-second deadline. The two-node batching avoided the prior third-party overflow. Set a five-device-only 45-second shutdown deadline; the complete 18-scenario target is the next verification.
- 2026-08-17: The first full-target command accidentally used the test harness default parallelism. Three scenarios passed, then concurrent multi-device tests triggered the known third-party endpoint overflow and aborted the process. This run is discarded; rerun all 18 with `--test-threads=1`.
- 2026-08-17: The serial matrix finished with 14 passed, 2 failed, 2 ignored. Both failures mixed durable history completion with transient connectivity/query state. Updated convergence waits to require matching saved history only, and `send_and_verify` now waits for its exact peer connection with retry-safe queries before sending. Focused failing scenarios are next.
- 2026-08-17: Formatting and the E2E target compilation passed. `members_converge_when_sponsor_stays_offline_after_joining_c` then passed in 85.86 seconds with durable convergence and pair-specific bidirectional connectivity verified separately.
- 2026-08-17: The focused five-device run reached pair-specific sends but one ready connection raced back to unavailable; the old diagnostics then masked the explicit offline report. `send_and_verify` now retries only explicit zero-side-effect offline or temporary dispatch-unavailable outcomes and never retries accepted, pending, errored, or ambiguous outcomes.
- 2026-08-17: The next five-device run failed with a fully preserved all-zero send report (`target_count=0` and every outcome count zero), proving dispatch had no target and performed no send. Added only this exact zero-side-effect result to the safe retry set.
- 2026-08-17: The first expression of the all-zero retry case did not compile because `target_count` is a Debug-only projection. Replaced it with the underlying empty `per_target` collection plus zero outcome counts.
- 2026-08-17: A later five-device run reached teardown and proved that even a two-node shutdown batch can trigger the third-party endpoint overflow. Replaced concurrent batches with sequential shutdown while retaining the five-device-only 45-second per-engine deadline.
- 2026-08-17: The next run timed out waiting for a pair's online/connection projection before send. Removed that projection gate; real SendText outcomes now drive the bounded safe retry directly at a 250 ms cadence.
- 2026-08-17: `five_devices_restore_full_sync_after_two_completed_removals_and_rejoins` passed end to end in 704.03 seconds: matching durable history, all real sends, and five sequential clean shutdowns. Removed the remaining test-helper unused-assignment warning before the final matrix rerun.
- 2026-08-17: The next serial matrix reached 14 passed, 2 failed, 2 ignored. Both failures were single-target Duplicate results after a prior zero-target retry. Added Duplicate as an idempotent settled result and retained the receiver-history verification.
- 2026-08-17: `rejecting_a_member_removal_keeps_an_unaffected_member_connection_usable` passed in 42.94 seconds and `restarted_member_pairs_with_a_member_added_while_it_was_offline` passed in 47.80 seconds after accepting explicit deduplicated delivery as settled evidence.
- 2026-08-17: The next serial matrix finished 15 passed, 1 failed, 2 ignored. The sole failure was a pure single-target send error with no other outcome. Added that exact idempotently retryable shape to the bounded send loop; focused rerun is next.
- 2026-08-17: `restarted_member_pairs_with_a_member_added_while_it_was_offline` passed in 50.72 seconds with the pure-error retry boundary. Final serial matrix rerun is next.
- 2026-08-17: The next serial matrix finished 15 passed, 1 failed, 2 ignored because five-device convergence completed between the last loop observation and the diagnostic snapshot. The printed final snapshot was fully converged. The deadline path now evaluates that final roster/history snapshot before deciding failure.
- 2026-08-17: `five_devices_restore_full_sync_after_two_completed_removals_and_rejoins` passed in 598.05 seconds with the deadline's final snapshot evaluated as authoritative. Final serial matrix confirmation is next.
- 2026-08-17: Final serial 18-scenario matrix passed: 16 passed, 0 failed, 2 ignored in 1387.57 seconds. The ignored tests explicitly require official v0.19 physical data/running peer and are not counted as passed. Repository checks and final staged commits remain.
- 2026-08-17: Began the final 63-item acceptance audit. Corrected the planning status from prematurely complete to in progress and created `acceptance_audit.md` with per-item implementation/test evidence. Ten items currently need direct proof or an explicit external-fixture skip: exhaustive message faults, every durable crash boundary, third-member completion recovery, receipt/successor reordering, historical binary fixtures, unknown-version mapping, all size limits, actual old-binary V3 guard behavior, and DeviceTrust revision corruption/overflow behavior. Stage 3 also has two adjacent implementation commits (`051e678`, `21b7b33`) and needs final history treatment after code evidence is complete.
- 2026-08-17: Source inspection confirmed some audit gaps are missing production behavior, not only missing tests. `completion_recovery_routes`, `completion_recovery_deliveries`, `resume_peers`, and the CompletionHelper role are persisted but unused; the V2 history exchange sends one full image instead of bounded pages; pairing allocates the declared first-frame length without the required 4 MiB check. Expanded the plan with Stages 9-12 so each remaining behavior/proof group receives its own red-green verification and commit.
- 2026-08-17: Resumed Stage 9 after handoff. Recovered session 99374: `unknown_profile_metadata_version_fails_closed` ran 1 test and passed (761 filtered out). Reviewed the current Stage 9 diff and located the remaining DeviceTrust revision validation/test boundary in `DieselAdmissionAttemptStore`.
- 2026-08-17: A planning append initially targeted a line found in `progress.md` rather than the current tail of `findings.md`; the patch made no changes. Re-read both tails and appended under their actual final lines.
- 2026-08-17: Stage 9 RED confirmed the history-frame boundary helper was absent at compile time, then `profile_counter_corruption_fails_closed` executed 1 test and failed because metadata with ordinal 1 and revision 0 reopened successfully. Added the minimal pre-allocation history length gate and rejected revision below the allocated ordinal.
- 2026-08-17: Stage 9 focused GREEN: profile-related filter ran 12 tests; the history-frame boundary test ran 1; membership history V2 ran 22; full `uc-infra --lib` ran 766 tests with 762 passed and 4 pre-existing ignored. Stale expected revision, `u64::MAX` overflow, cross-Space monotonic revision, unknown versions, and pairing/history 4 MiB boundaries all passed.
- 2026-08-17: Stage 9 repository checks in progress. `cargo metadata --locked`, `cargo fmt --all -- --check`, architecture preflight, and `git diff --check` passed; workspace all-target check is still running in session 46526.
- 2026-08-17: Stage 9 final checks passed: workspace all-target check completed in 1m35s; the added floor corruption test and ordinal/revision overflow rollback test each ran 1 and passed; formatting, architecture preflight, and diff checks passed again. Stage 9 is ready for scoped commit.
- 2026-08-17: Self-review rejected a misleading cross-Space test that only replaced opaque history bytes. Its real transition replacement first failed because SourcePrepared is not a persisted initial transition; corrected the test to exercise the production TargetStaged -> ActivationStarted sequence.
- 2026-08-17: Stage 9 committed and is now `85197c6` (`fix(membership): enforce admission protocol bounds`), excluding `.planning/`. Began Stage 10 source-led design of deterministic 256-record history pages, encrypted receiver progress, and final whole-image verification.
- 2026-08-17: Resumed Stage 10 after handoff. The first application/infra check failed only because three legacy workspace-state conversions did not initialize the new pending page-transfer map. Added empty initialization at each conversion boundary and restarted the affected-package check.
- 2026-08-17: The affected application/infra check passed. Removed the obsolete whole-history exchange type and public methods, retained sender validation inside page export, replaced silent transfer switching with fail-closed clearing, and replaced lossy page-index fallbacks with explicit range errors.
- 2026-08-17: Added application-level Stage 10 coverage using a real 257-event signed history: out-of-order page rejection without formal-history replacement, first-page persistence across owner restart, duplicate-page idempotence, conflicting-transfer clearing, final-page-only application, and a two-page sender-to-receiver loopback proving both sides retain identical bytes.
- 2026-08-17: First Stage 10 application run executed 3 tests: conflicting-transfer clearing passed; restart/final comparison and end-to-end failed because the 257-event fixture contained many remote removals, which correctly pause for the receiver's own decisions. Reworked only the fixture to use activated additions, preserving the production decision gate.
- 2026-08-17: Second Stage 10 application run passed restart/final application and conflicting-transfer clearing. End-to-end reached sender export but correctly rejected a fixture identity mismatch: the signer used sponsor while announcement material used the default test device. Aligned the fixture announcement identity; production authorization remains unchanged.
- 2026-08-17: The corrected 257-event sender-to-receiver test ran 1 real test and passed. Extended the existing encrypted workspace-state round trip with a real pending page carrying the sensitive marker, and documented paging ownership, restart behavior, conflict rejection, and final-only formal replacement in the architecture bible.
- 2026-08-17: Core paging filter ran 2 real tests and passed. The encrypted-state test initially failed to compile because its fixture used a crate-private event-id constructor; switched the fixture to the public hex parser and retained identical data.
- 2026-08-17: Encrypted pending-page round trip ran 1 real test and passed, including raw database and auxiliary-file marker scans. Self-review found per-page version/count checks occurred only during final assembly; moved the shared envelope validation before receiver persistence and reused it during complete import.
- 2026-08-17: After early envelope validation, all 3 application paging tests passed. Full core history suite ran 24 tests with 23 passed and exposed one Stage 10 test-migration assertion that compared a sender-filtered runtime digest with the unfiltered local digest; restored the prior contract by checking the same event head/depth while retaining each view's self-consistent digest.
- 2026-08-17: Full Stage 10 affected suites passed: `uc-core` membership history 24/24, `uc-application --lib` 811/811, and `uc-infra --lib` 762 passed with 4 pre-existing ignored. Obsolete whole-history symbols are absent; repository preflight, formatting, and diff checks passed before final workspace verification.
- 2026-08-17: Stage 11 resumed from the authenticated recovery message and helper-security groundwork. Added a failing repository test proving the missing pre-send challenge durability boundary, then added encrypted profile storage for the exact challenge. The focused restart test ran 1 real test and passed; no helper attempt exists before authentication.
- 2026-08-17: Stage 11 completed authenticated third-member recovery after the joiner has durably saved Commit and Applied. The helper saves one signed transport-bound challenge before accepting the resume-key response, verifies the exact event and permanent receipt, applies only its bound security delivery, and replays the identical Complete after restart or duplicate response. The joiner remains Pending until that Complete validates.
- 2026-08-17: Commit recovery routes now carry only member instance, device id, transport public key, and transport address. Network frames have a tested 4 MiB pre-allocation bound, reject unknown versions, and require the challenge transport identities to match the live connection. Changed transport identity and repeated terminal completion are covered directly.
- 2026-08-17: Stage 11 verification passed: 28/28 V2 history tests, 813/813 application library tests, 769 infrastructure tests with 4 pre-existing ignored, the production Engine protocol assembly test, locked metadata, workspace all-target compilation, repository architecture preflight, formatting, and diff checks. Stage 12 exhaustive evidence remains next; no physical three-device run was performed in this stage.
- 2026-08-17: Stage 12 closed the in-repository proof gaps. Durable Commit now rejects recovery-route collections above 256 on both encode and untrusted decode. Direct tests cover the 256/257 activation-receipt page boundary, applying a saved receipt before verifying that member's later event, out-of-order Commit/Applied/Complete with exact no-write state preservation, and owner reconstruction from disk after each durable admission stage.
- 2026-08-17: The official historical ciphertext fixture set and problem-baseline executable are not present or configured on this machine. Acceptance items 41 and 49 are recorded as skipped, not passed. The two physical mobile-device checks remain skipped as before.
- 2026-08-17: Stage 12 final verification passed: membership history 30/30, application library 815/815, and infrastructure 769 passed with 4 pre-existing ignored. Locked metadata, workspace all-target compilation, formatting, repository architecture preflight, and diff checks all passed. Stage 12 is ready for its scoped commit; external fixture, old-binary, and physical-device items remain skipped.
- 2026-08-17: Final audit calibrated two UniFFI lifecycle tests: shutdown timing now starts after successful startup, and factory reset invalidates the old runtime before the host reconstructs a fresh empty instance. The complete UniFFI public-contract target passed 22/22.
- 2026-08-17: The first final full-workspace run passed the prior UniFFI failures and then exposed a stale HarmonyOS dependency allowlist. `serde_json` has been a required runtime dependency since device-trust result mapping was added; the focused boundary test reproduced the exact mismatch, then passed after the allowlist was synchronized. The prohibition on direct internal-crate dependencies remains unchanged.
- 2026-08-17: A second complete workspace test run is in progress. External historical ciphertext, problem-baseline binary, and two physical-device checks remain skipped rather than passed.
- 2026-08-17: The second complete workspace test run passed with no failures, including all 22 UniFFI public-contract tests and all 4 HarmonyOS workspace-boundary tests. Final locked metadata, workspace all-target check, formatting, architecture preflight, and diff checks also passed.
- 2026-08-17: Final audit is complete for repository-owned implementation and automated evidence. The specification status and architecture record now state the exact boundary: official historical ciphertext, the problem-baseline executable, and two physical-device checks remain external skips.
- 2026-08-17: Final audit is now `36d16c2` (`test(engine): align lifecycle acceptance with reset contract`), excluding `.planning/`. The PR context audit updated the domain glossary and architecture maintenance record in this same final-audit commit.
- 2026-08-17: Combined the two adjacent Stage 3 slices into `08193a7` so every implementation stage has exactly one commit. The final tree hash remained `c3f50f3e900de108098ba6b4f5ac2a57b21bcb86`; `backup/adr-023-before-stage3-squash-20260817` preserves the pre-rewrite branch.
- 2026-08-17: Goal-level commit audit passed: the 14 planned entries (Stages 0-12 plus final audit) exactly match the 14 commits after baseline `b390487`, in order. Every implementation-stage commit is independently addressable, and the current branch has no upstream.
- 2026-08-17: Post-rewrite delivery gates passed: locked metadata, workspace all-target check, formatting, repository architecture preflight, and diff check. The full workspace test result remains applicable because the final tree is byte-identical to the already-tested pre-rewrite tree.
- 2026-08-17: Rechecked external acceptance inputs. No `UC_V019_*` inputs, official historical ciphertext fixture set, or problem-baseline executable are present; the only downloaded `uniclipboard-cli` is unrelated version `0.4.0-alpha.1`. One physical iPhone is visible, but no Android or HarmonyOS device and no complete physical multi-device setup are available, so device acceptance remains skipped rather than partially claimed.
- 2026-08-17: Rebased all 14 stages onto `b390487`, retaining the mainline stale-removed-device repair and the ADR-023 lifecycle contract. The post-rebase tree is `20dfd37ff95c16ab048b4396bc7d9869258f78e0`. Full workspace tests passed after removing one obsolete legacy-protocol test and aligning the real-host acceptance with factory-reset runtime invalidation; final metadata, compilation, formatting, architecture, and diff checks also passed.
