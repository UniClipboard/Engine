# 进度记录

## Session: 2026-08-02

### Phase 1: 仓库与约束摸底

- **Status:** complete
- **Started:** 2026-08-02
- Actions taken:
  - 确认用户需求与完成标准。
  - 读取项目摸底和文件化规划流程。
  - 恢复并检查已有规划记录，确认旧计划已完成且不属于本任务。
  - 回顾 Issue #950、#952、#953、#954、#955 的真实内容和状态。
  - 读取根维护规则、核心层规则、仓库说明、架构文档和近期提交。
  - 确认跨层职责、稳定入口、安全持久化和 P2P 默认约束。
  - 完整读取核心、应用和基础设施维护规则。
  - 定位配对消息、成员、信任和地址模型。
  - 证实当前成员登记不会传播，加入确认只携带 sponsor 自身信息。
  - 还原 sponsor 与 joiner 的一对一保存顺序，并区分安全组更新与完整成员互认。
  - 审计现有上线探测与在线状态事件，确认它们只覆盖已知地址设备。
  - 确认普通连接的成员与安全组双重准入，以及当前配对消息已使用 v4。

### Phase 2: 端到端流程审计

- **Status:** complete
- Actions taken:
  - 还原一对一配对两端的成员、信任、地址和安全组更新顺序。
  - 检查在线状态探测、普通连接准入与配对消息版本。
  - 识别候选成员不能提前写入正式成员表的边界。
  - 检查密文持久化规则与现有成员、信任、地址行映射，发现相关敏感字段仍按早期格式直接落库。
  - 检查安全组更新待发送机制，确认离线失败可保留，Engine 有定时退避和上线唤醒重试。
  - 检查 Engine 启动、暂停、恢复和关闭路径，确认长任务必须由运行句柄统一关闭。
  - 检查旧空间自动安全升级，确认其单一负责人和后台生命周期可作为结构参考。
  - 检查安全移除与当前保护组准入，确认旧 issue 的长期共享密钥证明已经不满足当前安全要求。
  - 检查 sponsor 生成的已有成员保护更新，确认它们未转交给 joiner，导致 sponsor 离线时存在保护版本缺口。
  - 检查持久地址裁剪及测试，确认缺少三节点第三方地址拨号证明。

### Phase 3: 目标规格设计

- **Status:** complete
- Actions taken:
  - 初稿曾指定 `uc-application::MembershipConvergence` 为完整流程唯一负责人；该名称已由 Phase 6 的 `SpaceMembershipGossip` 替代。
  - 定义候选与正式成员边界、双向证明、保护版本补齐、失败分类和公开状态。
  - 定义在线、离线、重启、移除、版本不兼容和多分支场景。
  - 初稿曾考虑使用共享保护状态作证明；该决定已由 Phase 6 的“当前 OpenMLS 成员树加每成员独立签名”替代。
  - 决定 pairing wire 使用 v5，并将通讯录信息先保存为加密候选。

### Phase 4: 分阶段实施计划

- **Status:** complete
- Actions taken:
  - 初稿划分 Phase 0 至 Phase 7；Phase 6 收敛范围后，当前实施计划调整为 Phase 0 至 Phase 5 加一条并行加密迁移工作线。
  - 为每阶段指定负责人、修改范围、验证、退出条件和删除项。
  - 把密文持久化、第三方地址验证、移除反例和 sponsor 离线列为硬关卡。
  - 定义三台真实设备核心验收流程及跨平台矩阵。

### Phase 5: 复核与交付

- **Status:** complete
- Actions taken:
  - 完成 `spec.md` 与 `implementation_plan.md`。
  - 用 Issue #949、#950、#952、#953、#954、#955 和当前代码重新核对设计。
  - 复核并确定 v4/v5 行为，没有遗留开放决策。
  - 完成仓库规定的非行为改动交付检查。

### Phase 6: 按 Gossip 方案收敛第一版

- **Status:** complete
- Actions taken:
  - 用户确认采用 Space Membership Gossip。
  - 将方案收敛为“空间通讯录传播、OpenMLS 当前成员确认、三设备离线收敛”。
  - 把完整流程唯一负责人调整为 `SpaceMembershipGossip`。
  - 把互认证明从组共享秘密修正为每台设备独立的 OpenMLS 成员签名。
  - 定义有界 `Digest`、`RequestMissing`、`EventBatch`、挑战、证明和确认消息。
  - 将通用 CRDT、Kademlia、libp2p、WebRTC、复杂分支和并发成员变化移到后续路线图。
  - 将旧关系数据加密调整为并行工作线，并保留为正式交付门槛。
  - 将实施计划收敛为 Phase 0 至 Phase 5，加一条并行加密迁移工作线。
  - 重新明确第一版核心验收：A 离线、C 经 B 加入、B 离线、A 上线后 A 与 C 自动同步。

### Phase 7: 文档一致性与最终验证

- **Status:** complete
- Actions taken:
  - 清理过程记录中容易被误认为当前决定的旧负责人、旧证明方式和旧阶段表述。
  - 将当前决定统一为：Gossip 传播通讯录，OpenMLS 决定成员资格，每台设备用自己的成员签名确认身份。
  - 全文复核规格、实施计划和过程记录，确认第一版边界、负责人、身份依据、版本策略和验收场景一致。
  - 重新运行仓库规定的全部非行为改动交付检查。
- Files created/modified:
  - `.planning/2026-08-02-space-membership-auto-pairing-spec/task_plan.md`
  - `.planning/2026-08-02-space-membership-auto-pairing-spec/findings.md`
  - `.planning/2026-08-02-space-membership-auto-pairing-spec/progress.md`
  - `.planning/2026-08-02-space-membership-auto-pairing-spec/spec.md`
  - `.planning/2026-08-02-space-membership-auto-pairing-spec/implementation_plan.md`

## Test Results

| Test | Expected | Actual | Status |
|------|----------|--------|--------|
| Git 工作区初始状态 | 无未提交产品代码变更 | `main` 与 `origin/main` 一致，工作区干净 | 通过 |
| `cargo metadata --locked --format-version 1` | 依赖元数据可解析 | 成功生成元数据 | 通过 |
| `cargo check --workspace --all-targets --locked` | 全工作区检查成功 | 成功；仅有一个既存的未使用引用 warning | 通过 |
| `cargo fmt --all -- --check` | Rust 格式正确 | 无差异 | 通过 |
| `node scripts/architecture/check-engine-repository.mjs` | 仓库架构规则通过 | 全部检查通过 | 通过 |
| `git diff --check` | 无空白错误 | 无输出 | 通过 |
| 三台真实设备自动互认 | 本阶段只产出规格和计划 | 未执行 | 跳过 |
| 三节点第三方地址转交 | B 停止后 A 使用 B 转交的 C 地址连接 C | 真实 Iroh 连接成功 | 通过 |
| 当前成员独立签名 | 正常互验；篡改、冒用和移除后拒绝 | 3 个聚焦测试通过 | 通过 |
| sponsor 离线后的安全更新转交 | C 保存并转交 B 为 A 生成的更新；B 退出后 A 应用并验证 C | 聚焦测试通过 | 通过 |

以上检查已在 Phase 7 文档清理完成后重新执行。

### Phase 8: 实施 Phase 0 关键前提

- **Status:** complete
- **Started:** 2026-08-02
- Actions taken:
  - 重新读取规格、实施计划、当前工作区和各层维护规则。
  - 确认生产代码尚未实现 Space Membership Gossip。
  - 核对 Engine 稳定入口、现有两设备网络测试和移动验收宿主。
  - 修正验收驱动缺口：本地三 Engine 测试直接使用稳定入口，真机使用最小验收宿主，不新增正式 CLI。
  - 增加三节点真实网络测试：B 转交 C 地址并停止后，A 仍能直接连接 C。
  - 运行聚焦测试，地址转交场景通过；确认现有持久地址形状可供 Gossip 转交。
  - 先运行成员签名测试并确认因能力不存在而失败，再实现本机签名和当前成员树验证。
  - 成员签名正例、内容与身份篡改反例、成员移除反例全部通过。
  - 再用完整空间安全适配器运行端口级红绿测试，确认能力读取当前已持久化成员状态。
  - 新增独立 `CurrentMemberSignaturePort`；未解锁或成员状态未就绪时不会退回旧共享秘密。
  - 增加三方安全更新转交测试：B 接纳 C 后退出，C 转交更新，A 应用后验证 C 的当前成员签名。
- Current red-green target:
  - Phase 0 完成；进入敏感关系数据加密迁移。

### Phase 9: 实施 Parallel A 与 Phase 1-3

- **Status:** in_progress
- **Started:** 2026-08-02
- Actions taken:
  - 增加统一的加密关系表和可恢复迁移状态。
  - 将成员、信任和地址仓储切换为密文读写，迁移后物理删除旧明文表和残留页。
  - 在创建、解锁和静默恢复时检查关系存储可读性；锁定时不读取或迁移。
  - 删除三类旧明文行模型和映射器。
  - 增加候选通讯录领域模型、合并规则、失败状态和来源信息。
  - 增加候选仓储端口和加密实现，候选与正式关系共用密文存储规则。
  - 验证 121 个数据库仓储测试、27 个空间流程测试和 6 个候选规则测试。
  - 验证成员、信任、地址、候选名称、候选地址和安全更新的明文探针均找不到原文。
  - 先运行 `SpaceMembershipGossip` 聚焦测试，确认因统一负责人尚不存在而失败。
  - 实现候选种子接收、合并、按变化写回、过期清理和重启恢复入口。
  - 5 个应用层候选流程测试通过；重复输入不重复写，冲突状态可跨重启保存。
  - 增加切换空间关系清理能力；新空间握手成功后先清除旧成员、信任、地址和候选，再保存新 sponsor。
  - 增加数据库级四类关系清理测试；切换空间 14 个流程测试和 Engine 全目标检查通过。
  - 切换完整成功后主动断开旧空间连接，再按新空间联系人重新连接。
  - 修复双设备确认最后一条消息可能在服务端结束时丢失的问题；两个真实网络节点现在能在专用通道上完成双向登记。
  - 增加双设备确认安全反例和协议版本测试；11 个网络适配器测试全部通过且输出无错误。
  - 增加双方同时确认的收敛测试；最终只有一套成员、信任、地址和完成状态。
  - 增加正式当前成员身份来源；锁定、缺少设备名称和正常身份读取 3 个测试通过。
  - 为共享网络节点增加“先构造确认能力、后安装入站处理”的两步入口，解决应用负责人和网络处理器之间的构造顺序循环。
  - 将加密候选仓库、当前成员独立签名能力和同一解锁会话接入 Engine 正式依赖装配。
  - 在正式 Engine 组装中创建唯一 `SpaceMembershipGossip`，并将确认通道安装到交付网络节点；真实网络探针从失败转为通过。
  - 验证确认通道 15 项、成员传播流程 10 项、Engine 宿主组装 8 项全部通过。
  - 验证 `cargo check --workspace --all-targets --locked`、格式、架构检查、元数据和差异检查通过；仅保留仓库原有的未使用引用警告。
- Current red-green target:
  - Phase 3 配对 v5 与通讯录传播：先用失败测试固定 sponsor 和 joiner 的有界通讯录种子契约。
  - 重新运行 `cargo test -p uc-infra pairing::wire::tests::confirm_round_trips --locked`，确认测试因 `SponsorConfirm` 尚无 `membership_seeds` 字段而编译失败；失败原因与预期一致。
  - 将 pairing wire 升到 v5，`SponsorConfirm` 现可往返携带候选通讯录种子；空列表保持既有行为。
  - 为候选批次、地址、安全更新数量和单条更新大小增加统一边界；4 个拒绝测试先失败后通过，确认编码和接收复用同一领域规则。
  - `SpaceMembershipGossip` 已能从正式成员、正式地址和本次成员更新生成有序 sponsor 种子；测试确认 A、B 被包含、C 被排除，且 A 获得 6 到 7 的安全更新。

## Error Log

| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-08-02 | bb-browser 守护进程未能启动 | 1 | 使用 GitHub 官方接口完成 issue 检索 |
| 2026-08-02 | 首次补丁预期的活动计划值错误 | 1 | 补丁未写入；读取真实值后重试成功 |
| 2026-08-02 | 猜测的 iroh pairing 子目录不存在 | 1 | 改用 `rg --files` 定位实际文件 |
| 2026-08-02 | 猜测的 peer admission 文件不存在 | 1 | 改用符号搜索定位实际模块 |
| 2026-08-02 | 按常见布局读取 `openmls_traits/src/lib.rs` 失败 | 1 | 该 crate 使用自定义库入口；改读其 Cargo 清单和实际源码文件 |
| 2026-08-02 | 规划进度落后于工作区实际改动 | 1 | 根据现有测试结果和工作区 diff 补记完成项 |
| 2026-08-02 | Engine 全目标检查发现四个旧集成测试缺少关系清理依赖 | 1 | 在共享测试 helper 增加空实现并补齐装配点后检查通过 |
| 2026-08-02 | 双设备确认测试在专用通道建立后由服务端提前关闭，发起方得到传输失败 | 1 | 正在按处理阶段和双向流收尾顺序定位，不重复只运行相同测试 |
| 2026-08-02 | 双设备确认服务端写完最终确认后立即结束，连接关闭早于消息送达 | 2 | 服务端等待发起方读取后关闭；真实双节点测试通过 |
| 2026-08-02 | 加密成员传播待发送测试找不到仓储适配器 | 1 | 补充并注册 `EncryptedMembershipOutboxRepository`，保持错误分类与候选仓储一致 |
| 2026-08-02 | 首次补记过程记录时补丁上下文不匹配 | 1 | 读取文件尾部后改为按实际结尾追加，未重复使用错误上下文 |
| 2026-08-02 | 待发送记录被加密关系表的类型约束拒绝 | 1 | 功能尚未发布，直接补全当前前向迁移允许的关系类型，保持单一数据库定义 |
| 2026-08-02 | 旧版本探测测试立即读取服务端握手计数得到 0 | 1 | 协议分类已正确；改为带截止时间等待服务端观察到握手，再断言未打开数据流 |
| 2026-08-02 | 补记上述测试错误时补丁上下文位置不匹配 | 1 | 读取文件尾部后按实际表格和阶段位置更新 |
| 2026-08-02 | `EngineConfig` 脱敏调试输出的条件编译写法无法解析 | 1 | 改为先构造调试对象，再在 `dev-tools` 下追加布尔字段 |

### Phase 9 continuation: 加密成员传播待发送箱

- 先运行重启与明文探针测试，确认因仓储适配器缺失而编译失败。
- 增加加密待发送仓储适配器，并接到统一关系存储已有的查询、保存和删除能力。
- 修正测试调用为稳定端口要求的引用参数；待重跑确认跨重启和明文探针结果。
- 首次重跑已进入真实数据库写入，但发现迁移约束遗漏 `membership_outbox`；已补全当前未发布迁移。
- 再次重跑通过：待发送记录跨数据库重启恢复，且文件扫描找不到代表性设备名、地址和安全更新明文。
- 新增 sponsor 传播待办测试并先确认缺少统一准备入口、待发送存储依赖和新设备信息而编译失败。
- 实现统一准备入口：返回给 joiner 的旧成员种子保持不变，同时为每个非 sponsor 旧成员加密保存新 joiner 的候选批次；聚焦测试已通过。
- 连续安全更新测试先因缺少窄安全状态入口失败；新增入口后，4→5→6 顺序应用测试通过。
- 两节点真实网络测试先因成员确认接收端无法应用更新而失败；现在 C 可把 A 所需更新随握手转交，A 先升级再完成双方证明，测试通过。
- 待发送交付测试先因传输入口缺失而失败；现在只有匹配确认会删除加密待办，其余响应会保留并写入退避时间。
- 入站事件批次测试先因统一接收入口缺失而失败；现在候选保存成功后才确认，且不会提前写正式成员。

### Phase 9 completion 与 Phase 10 公开状态

- 完成自签名设备公告、摘要、差异请求、事件批次、确认和主动成员核对。
- 完成单轮后台恢复、启动立即执行、上线唤醒、暂停、恢复和关闭等待；Engine 只持有一个后台运行句柄。
- 候选重试使用指数退避和稳定偏移；内部状态可从加密待办跨重启恢复。
- 公开 `QueryMembershipConvergence`，只返回四种状态和各类待办数量，不返回设备身份。
- 公开查询直接走 `Engine::start` 与 `Engine::execute`；创建新空间后返回完成且所有计数为零，随后正常关闭。
- UniFFI、HarmonyOS N-API 和 iOS/Android 共用验收宿主均已接入同一查询；HarmonyOS 最小宿主在启动和恢复后读取状态。
- 聚焦验证通过：应用当前空间状态、Engine 公开契约、Engine 最外层运行、UniFFI 运行、HarmonyOS 映射与宿主接线、移动验收命令。

### Phase 10 continuation: 已验证成员原子提交

- 审计确认 `promote_verified_peer` 当前分四次保存地址、信任、成员和 `Ready` 候选，存在正式关系部分可见的窗口。
- 先增加数据库回滚测试，并用 SQLite 触发器强制成员写入失败；首次运行按预期因统一提交入口不存在而编译失败，红灯与目标缺口一致。
- 加密关系存储现在用一次事务提交地址、信任、成员和 `Ready` 候选，并在事务内重新读取、解密核对四项写入。
- 数据库回滚测试通过：强制第三项写入失败后，前三类正式关系均不存在，候选仍保持提交前状态。
- 应用层失败测试先因缺少统一提交依赖而失败，接线后通过；流程失败时没有正式成员可见，候选保留在 `Verifying` 供重启恢复。

### Phase 10 continuation: 真实旧版本成员识别

- 审计确认加入空间的 v5 拨号已经能识别只支持旧 pairing 通道的 sponsor。
- 审计确认成员确认和通讯录传播的真实网络拨号仍把旧版本设备归为离线；公开状态虽然支持 `WaitingForUpgrade`，但真实网络尚无法产生该分类。
- 当前目标：抽出只读旧通道探测，并分别覆盖成员确认与待发送通讯录传播；不得发送邀请消息、创建配对会话或写关系。
- 已抽出并接入只读旧通道探测；首条真实网络测试已返回正确的“版本不兼容”，但服务端握手计数断言早于异步接收任务，需改为截止时间等待。
- 将服务端观察改为截止时间等待后，自动互认与待发送通讯录两条真实旧节点测试均通过；两条测试同时确认旧节点只接受协议握手，没有打开数据流。
- 原有旧 sponsor 加入拦截与空间 `WaitingForUpgrade` 恢复测试均通过；只保留仓库既有的 `InboundSnapshotRebuild` 未使用引用警告，本任务不修改该警告。
- 真实旧版本成员识别完成，进入三 Engine 稳定入口自动化核心场景。
- 已确认三 Engine 测试不需要正式 CLI：仅用 `dev-tools` 选择本地邀请地址，其余加入、查询、发送、历史核对和关闭均使用稳定 Engine 入口。
- 开始接入 `dev-tools` 本地邀请服务覆盖；首次格式检查发现条件编译标记不能位于连续方法调用中，已改为分步构造脱敏调试输出。

### Phase 10 continuation: 三 Engine 自动化核心场景

- 新增 `crates/uc-engine/tests/space_membership_auto_pairing_e2e.rs`，直接启动三份完整 Engine；每台设备使用独立目录与安全存储，并在重启时复用原状态。
- 测试通过本地邀请服务完成两次正式加入，覆盖 B 离线后的 A/C 自动互认、双向完整正文落库，以及 A/C 重启后的再次双向传输。
- 首次 `--no-run` 编译通过。
- 首次真实运行在 B 的成员列表等待阶段超时。核对后确认加入已成功，A 已看见 B；失败来自测试错误地要求 B 的关系列表包含 B 自己。当前列表只保证返回本机保存的关系，因此已改为两侧只断言对方存在。

### Phase 10 error additions

- `2026-08-02`：三 Engine 核心测试首次运行在 A/B 成员等待阶段超时（attempt 1）；原因是测试把本机设备也作为关系列表必含项，已按公开列表的实际语义修正。
- `2026-08-02`：首次补记三 Engine 进度时补丁上下文不匹配（attempt 1）；已读取文件真实结尾后按当前结构追加。
- `2026-08-02`：三 Engine 核心测试第二次运行通过两次加入，但 B 关闭、A 恢复后 A/C 未在 30 秒内同时达到已登记、完成且在线（attempt 1）；正在拆分公开成员、收敛状态和在线状态定位真实缺口。
- `2026-08-02`：新测试首次格式检查发现一个函数签名不符合仓库格式（attempt 1）；已运行统一格式化修正。
- `2026-08-02`：持久重试调度测试首次编译缺少测试模块的 `Duration` 引用（attempt 1）；生产代码已编译，补齐局部测试引用后重跑。
- `2026-08-02`：修复重试唤醒后，三 Engine 测试已完成 B 离线时的 A/C 自动互认和首次双向正文落库，但 C 在重启前关闭时超过 15 秒截止（attempt 1）；A 已正常关闭，正在定位 C 的具体生命周期停顿点。
- `2026-08-02`：首次一次性修改运行中关闭与恢复判断时，补丁因后半段上下文不匹配而未应用（attempt 1）；已拆成按真实代码位置的独立改动，避免重复错误上下文。

### Phase 10 retry scheduling result

- 新增按候选与待发送记录的最早持久重试时间安排下一轮运行；没有待办时仍保持低频核对，整轮失败使用有界延迟避免空转。
- 聚焦调度测试通过，证明 31.5 秒的持久截止会转化为 30.5 秒后的后台唤醒，而不是退回约五分钟例行检查。
- 三 Engine 真实运行证明该修复使 A/C 在 B 已关闭时完成互认并完成首次双向完整正文核对。
- Gossip 运行任务现在能在网络核对进行期间响应暂停和关闭；被中止在 `Verifying` 的候选会在恢复轮次立即继续，两个聚焦测试均通过。
- 三 Engine 完整测试最终通过：A 离线、C 经 B 加入、B 关闭、A 恢复后 A/C 自动互认；双方首次双向正文逐字落库；A/C 正常关闭并复用原目录和原安全存储恢复后，再次双向正文逐字落库。
- 完整测试总耗时约 77 秒，成功判断全部使用截止时间轮询，没有用固定休眠作为通过依据。
- 首次同步上述完成状态时补丁上下文校验失败；读取文件真实内容后已按当前位置更新。

### Phase 10 completion: 锁定边界、幂等与全量回归

- 锁定操作现在先停止并等待成员传播，再清除本地密钥；恢复会话和口令解锁后会继续原有待办。
- 后台暂停完成前会先结束正在进行的网络核对，避免锁定完成后仍有旧请求继续运行。
- 三 Engine 验收增加锁定状态断言：锁定时成员状态不可读取，恢复后 A 与 C 仍能自动互认并继续双向同步。
- 重复公告、重复安全更新、重复确认和确认响应丢失均增加明确验收，31 个成员传播测试全部通过。
- 恢复出厂设置改为清空整组加密关系，不再在密钥删除后尝试逐条解密；锁定状态下的真实 Engine 验收通过。
- 修正历史文件传输迁移测试，使其不再要求生产关系迁移执行不安全回退；`uc-infra` 全部 677 项测试中 673 通过、4 项按原定义忽略。
- `uc-application` 770 项测试全部通过；`uc-engine` 113 项库测试和相关集成测试通过。
- 三 Engine 完整场景再次通过，耗时 77 秒；测试会尝试本机可用邀请地址，避免整套回归负载下单一地址偶发不可达。
- 日志审计确认新增传播路径只记录数量、结果类别和固定失败种类，不记录设备名、指纹、完整地址、证明或密钥。
- `cargo metadata`、全工作区检查、格式检查、架构检查和差异检查全部通过；仅保留仓库既有的 `InboundSnapshotRebuild` 未使用警告。
- 三台真实设备尚未执行，按规则记为跳过，不计为通过。

### Phase 10 最终完成审计补充

- 修复 sponsor 已完成安全接纳、但成员传播待发送保存失败时把整次加入判为失败的问题。待发送保存失败不再制造半完成加入；joiner 仍收到完整旧成员种子，并由 joiner 路径继续收敛。
- 修复 joiner 已安装安全组状态、但本地候选种子保存失败时错误回退 setup 状态的问题。此时加入保持完成，旧成员仍可通过 sponsor 已保存的待发送记录主动完成互认。
- 增加两条故障测试，分别证明 sponsor 待发送保存失败仍返回完整恢复种子，以及 joiner 候选保存失败仍保持已完成加入；两条均通过。
- 将 Iroh 本机地址变化通知接入唯一 Gossip 运行任务。地址变化后立即刷新自签名公告并传播，不再等待最长约五分钟的低频核对；事件驱动测试通过。
- 修复候选识别出旧协议后永久停在 `WaitingForUpgrade` 的问题。版本不兼容现在保存下一次重试时间；旧设备升级后，下一次持久重试自动完成互认并把状态恢复为 `Complete`。
- 最终重跑：`uc-application` 773 项全部通过；`uc-infra` 673 项通过、4 项按原定义忽略；`uc-engine --all-targets --features dev-tools` 全部通过，其中三 Engine 核心场景耗时 76.74 秒。
- 最终仓库检查通过：metadata、全工作区全目标检查、格式检查、架构检查和差异检查。仅保留仓库既有的 `InboundSnapshotRebuild` 未使用引用警告。
- 三台真实设备仍未执行，继续明确记为“跳过”，不计为通过；因此 Phase 10 和整个规格仍保持进行中。

### Phase 10 真实设备准备复核

- 当前主机只发现一台在线 Android 真机；iPhone 处于离线状态，未发现 HarmonyOS 真机，因此不具备三设备核心场景的执行条件。
- 使用当前工作区源码成功生成 Android 验收包；首次构建受旧临时缓存缺失生成文件影响，换用全新临时目录后完整构建通过。
- Android 验收包大小约 22 MB，SHA-256 为 `c18fb8d18eb6ab18a254d50ceaff0f4be0522f2c7b284af1da9f0dbc7aff28fd`。
- 向在线 Android 真机安装时被设备的 USB 安装许可拦截，返回 `INSTALL_FAILED_USER_RESTRICTED`；设备上没有旧验收包可继续执行。
- 本次只证明当前源码可生成 Android 真机验收包，不记为真机流程通过；三设备核心场景继续记为“跳过”。
- iOS 验收宿主首次完整应用构建失败，原因是生成项目通过短名称链接时优先选择了动态库；改为明确链接静态库后，通用 iOS 真机目标完成编译、签名和系统校验。
- iOS 静态链接防回归测试先在旧配置上按预期失败，应用最小修复后通过；移动验收宿主共 15 项测试全部通过。
- iOS 验收应用约 66 MB，主程序 SHA-256 为 `0528677df4006bd254d7dfc580d37d39ce6a299e86d9be6126c14b545af6a90c`；由于 iPhone 离线，本次未安装或启动，记为“跳过”。
- HarmonyOS 验收应用和交付包均从当前源码成功生成；验收应用约 81 MB，SHA-256 为 `efa8601fde0e62401b5cf7c7a75543c1165dd031ded613b3a2f3b3c6385bf396`，交付包约 24 MB，SHA-256 为 `7eff48e9be12d2268eca69f1b6c4a5396bdb91212d9f22eae9f982c4110fbb17`。
- 未发现 HarmonyOS 真机，因此 HarmonyOS 也只证明构建和签名通过，不记为设备验收通过。
- 修复后的最终仓库检查通过：metadata、全工作区全目标检查、格式检查、架构检查和差异检查；仍只有仓库既有的未使用引用警告。

### Phase 11: Android 与 iOS 模拟器端到端验收

- 已确认本机有 `Pixel_8` Android 模拟器、一台已启动的 iOS 模拟器和多台可启动的 iOS 模拟器。
- 已确认 Rust 的 Android ARM64 与 Apple Silicon iOS 模拟器目标均已安装。
- 已确定用 Android 模拟器作为 B、两台 iOS 模拟器作为 A/C，真实执行三设备离线互认和重启恢复。
- 当前缺口是 iOS 模拟器构建及外部结果读取，以及 Android 命令脚本的明确设备选择；下一步按测试先行补齐。
- 首次聚焦测试命令错误地传入多个过滤参数，Cargo 拒绝执行；改为运行整个宿主边界测试文件。
- 随后本机编译缓存进程被系统权限拦截；禁用该缓存后得到有效红灯，三个新增约束均因目标能力缺失而失败。
- 已补充 iOS 模拟器构建、外部命令与脱敏结果文件，并让 Android 安装和命令脚本强制指定模拟器序列号；宿主边界 9 项测试全部通过。
- Android `Pixel_8` 模拟器已用独立端口启动，下一步等待开机并安装当前源码构建。
- iOS 模拟器应用首次完整构建成功，但启动时系统拒绝无签名应用访问安全存储，Engine 因此没有完成启动，外部命令按预期拿不到结果。
- 已将该故障转成宿主边界测试：模拟器构建不得关闭签名；下一步使用模拟器临时签名重建并重新验证安全存储。
- iOS 临时签名约束先红后绿；重建产物包含系统认可的应用身份，已重新安装到两个 iOS 模拟器。
- Android 首次安装构建复用了损坏的临时目录，缺失数据库绑定生成文件；已将“使用独立干净编译目录”加入下一条红灯约束。
- Android 干净目录约束先红后绿，当前源码完成全新构建并成功安装到 `emulator-5556`。
- 三个宿主均返回正常运行；B 创建空间，A 经 B 加入并完成收敛，A 随后暂停且 B 明确观察到 A 离线。
- A 离线期间，C 经 B 加入成功；随后 B 暂停，C 明确观察到 B 离线，A 恢复。
- B 保持离线时，A/C 自动互认：双方均有两台对端记录并互相显示在线。C 收敛完成；A 仍有一条等待离线 B 的待办，不影响 A/C 直连验收。
- 下一步补充 iOS 外部正文逐字核对命令，先以宿主边界红灯证明缺口。
- iOS 正文核对入口先红后绿，并确保内部临时导出文件在比较后立即删除；更新包构建成功。
- 更新安装保留了 A/C 数据，但恢复时发现启动结果与命令结果会竞争同一结果文件，脚本错误读到 `started` 而不是 `space_unlocked`。
- 已将“每条命令带唯一编号且只接受匹配结果”加入新的红灯约束，修复后再继续正文与重启验收。
- iOS 命令编号约束先红后绿；新脚本先确认 Engine 就绪，再只接受本次命令编号对应的脱敏结果。
- A→C 首次发送成功，C 从真实持久历史读取并逐字核对 42 字节正文一致；C→A 同样逐字核对 42 字节一致。
- A/C 均通过宿主关闭命令完成正常关闭，终止进程后重新启动并分别返回明确的空间解锁成功结果。
- 重启后 A/C 都从持久状态恢复两台对端记录并互相显示在线，B 继续离线。
- A→C 重启后发送成功，C 逐字核对 41 字节正文一致；C→A 同样逐字核对 41 字节一致。
- Android B 与两台 iOS 模拟器的完整离线互认、首次双向正文、A/C 重启和再次双向正文全部通过；真实设备矩阵仍未执行，继续记为跳过。
- 移动验收宿主 18 项测试全部通过；iOS/Android 脚本语法、iOS 配置、模拟器应用签名和三个安装实例均核对通过。
- 最终仓库检查通过：metadata、全工作区全目标检查、格式检查、架构检查和差异检查。只保留仓库既有的 `InboundSnapshotRebuild` 未使用引用警告。
- 三个 Engine 实例均通过宿主命令正常关闭，两个 iOS 验收应用进程已终止，Android 模拟器已正常退出。
- Phase 11 完成；模拟器验收记为通过，三台真实设备验收继续明确记为跳过，不计为通过。

### Checkpoint 准备

- 用户确认当前自动化与模拟器验收结果可作为阶段性 checkpoint。
- 三台真实设备核心场景仍未执行，继续明确记为“跳过”，不计为通过。
- checkpoint 只包含本任务的规格、实现和验收记录，不包含后续 Space Setup 依赖重构设计。
