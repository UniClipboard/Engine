# 进度记录

## Session: 2026-08-01

### Implementation Phase 0: 行为与兼容基线

- **Status:** complete
- Actions taken:
  - 恢复设计计划和当前工作区状态。
  - 读取 TDD、测试反模式和深模块设计规范。
  - 将计划切换为实施阶段，下一步先建立旧 `Ready` 数据兼容红灯。
  - 根据会话交接再次核对分支、未提交文件和规划记录；确认尚未修改生产代码，无关剪贴板测试继续排除在本次范围外。
  - 确认兼容测试可复用真实仓储构造器和会话中的当前空间身份，不需要新增生产接口。
  - 新增旧 `Ready` 材料缺少保护组标识的真实 SQLite 兼容测试，并确认红灯准确失败为 `ready space has no protection group identity`。
  - 实现旧 `Ready` 材料验证、单次标识回填、加密保存和会话刷新；定向测试由红转绿。
  - 从当前 wire v1 实现提取请求与接纳回复的实际编码字节，准备固化为兼容样本。
  - 将 v1 请求和接纳回复固化为精确字节样本，兼容测试通过并移除临时输出。
- Files created/modified:
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/task_plan.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/progress.md`

### Phase 1: 当前结构审计

- **Status:** complete
- Actions taken:
  - 读取模块设计和文件化规划规范。
  - 确认当前工作区与改动范围。
  - 建立本次重构方案的独立计划目录。
  - 核对 Issue 951 原计划，确认当前实现偏离既定的“小入口、隐藏协议与恢复细节”方向。
  - 统计当前差异并确认复杂度已影响无关测试装配。
  - 读取核心升级模型和应用协调者，确认公开接口暴露了几乎全部内部步骤。
  - 审计持久化、会话、空间访问、网络、启动组装和测试，定位复杂度主要集中在过大的安全适配器、过浅的端口和外泄的重试策略。
  - 纠正对并行剪贴板测试改动的错误归因，并将其排除出重构范围。

### Phase 2: 目标模块设计

- **Status:** complete
- Actions taken:
  - 开始定义唯一负责模块、最小外部入口和内部实现分区。
  - 对比现有群组更新与 Iroh 安装模式，确定保留协议安装形状，但不复制其业务重试位置。
  - 核对群组材料创建、加入和回复缓存的原子性要求，确定复用既有群组加入模型并去除协议对通用密钥材料的反向依赖。
  - 发现旧 `Ready` 记录缺少新增保护组标识的兼容缺口，并确认 OpenMLS 当前群组标识不能直接区分并发临时组。
  - 完成 `AutomaticLegacyUpgrade` 目标接口、内部保护接缝和各层职责设计。

### Phase 3: 迁移计划

- **Status:** complete
- Actions taken:
  - 制定六阶段行为保持型迁移顺序。
  - 为每阶段定义通过条件、提交顺序和验证命令。
  - 明确旧接口、重复类型、浅层测试和兼容壳的删除要求。
- Files created/modified:
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/design.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/task_plan.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/progress.md`

### Phase 4: 方案验证与交付

- **Status:** complete
- Actions taken:
  - 开始按当前代码、仓库规则和安全不变量核对方案。
  - 搜索计划要求删除的旧接口和参数，确认均对应当前升级路径，没有误纳入无关功能。
  - 运行核心、应用、基础设施的自动升级定向测试，当前行为基线全部通过。
  - 完成仓库元数据、全工作区检查、格式、架构、差异和规划文档空白检查。
  - 方案已完成，可从阶段 0 的兼容红灯开始实施。
- Files created/modified:
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/design.md`
- Files created/modified:
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/task_plan.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/findings.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/progress.md`
- Files created/modified:
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/task_plan.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/findings.md`
  - `.planning/2026-08-01-legacy-upgrade-refactor-plan/progress.md`

## Test Results

| Test | Input | Expected | Actual | Status |
|------|-------|----------|--------|--------|
| 核心与应用升级基线 | 两个 `legacy_upgrade` 测试目标 | 当前行为全部通过 | 14 项通过 | pass |
| 基础设施升级基线 | `uc-infra` 的 `legacy_upgrade` 过滤测试 | 安全、网络、存储行为通过 | 7 项通过 | pass |
| 仓库元数据 | `cargo metadata --locked --format-version 1` | 成功 | 成功 | pass |
| 全工作区检查 | `cargo check --workspace --all-targets --locked` | 成功 | 成功，保留 1 条既有提醒 | pass |
| 格式 | `cargo fmt --all -- --check` | 无差异 | 无差异 | pass |
| 架构 | `node scripts/architecture/check-engine-repository.mjs` | 通过 | 通过，含 OpenMLS 4 项 | pass |
| 差异 | `git diff --check` | 无错误 | 无错误 | pass |
| 规划文档空白 | 尾随空白扫描 | 无命中 | 无命中 | pass |
| 旧 `Ready` 兼容红灯 | 缺少保护组标识的真实加密材料 | 自动回填并保持稳定 | 当前描述入口报缺少标识 | expected fail |
| 旧 `Ready` 兼容绿灯 | 同一真实加密材料，连续查询并重开仓储 | 标识稳定且持久化 | 1 项通过 | pass |
| wire v1 编码样本 | 代表性请求与接纳回复 | 字节与当前协议完全一致 | 1 项通过 | pass |
| 核心升级行为矩阵 | `uc-core` 升级测试 | 9 项通过 | 9 项通过 | pass |
| 应用升级行为矩阵 | `uc-application` 升级测试 | 5 项通过 | 5 项通过 | pass |
| 基础设施升级行为矩阵 | `uc-infra` 升级过滤测试 | 7 项通过 | 7 项通过 | pass |
| 核心模型首次复测 | 删除核心唯一哈希依赖后的旧锁文件 | 正常编译 | `--locked` 要求先同步锁文件 | blocked until lock refresh |
| 私有尝试存储 | 真实 SQLite 重开、恢复、清理和明文扫描 | 全部通过 | 1 项通过，仅有 2 条迁移中提醒 | pass |

### Implementation Phase 1: 收紧核心模型

- **Status:** complete
- Actions taken:
  - 阶段 0 全部通过后进入核心模型收紧。
  - 先修改核心契约测试，确认红灯准确显示缺少不透明重放编号、通用接纳结果和显式组标识转换。
  - 核心状态和缓存模型完成最小替换，定向测试由编译红灯转为 8 项通过。
  - 新增安全模块的摘要与重放编号测试；首次红灯命中新入口缺失，并发现一处旧测试仍调用已删除缓存方法。
  - 摘要与重放编号计算迁入安全模块，两项定向测试通过；开始清理核心旧计算入口和测试替身中的算法复制。
  - 删除重复接纳类型后确认编译红灯准确集中在网络映射、安全端口和应用日志仍按旧扁平字段访问。
  - 网络、安全实现和应用测试已切到通用接纳结果；全仓已无重复接纳类型名称，正在修正剩余的嵌套字段断言。
  - 将重启恢复测试改为只通过新保护入口观察请求复用，确认红灯准确为专用实现尚不存在。
  - 应用生产流程已切换为四动作保护接口；五个场景测试的三组旧替身已合并为单一保护替身。
  - 修正场景断言改读统一快照后，应用升级测试 5 项全部通过。
  - 通用 `SpaceAccessPorts` 已移除可选升级字段，唯一构造点开始同时产出通用端口和同步引擎专属保护实现。
  - 同步引擎改用专属 `LegacyProtectionPort`，`uc-engine` 全目标检查通过。
  - 从上一次会话恢复后核对实施计划、工作区和 `AGENTS.md`；确认防止复杂度外泄的反模式约束已写入，无关剪贴板测试改动仍排除在本次范围外。
  - 当前迁移阻塞已收敛为私有尝试存储测试中的两个旧方法名，下一步先完成该测试迁移。
  - 将真实 SQLite 重启与密文测试切到基础设施私有尝试存储方法，定向测试 1 项通过。
  - 复测暴露两条迁移中间态提醒：公开构造器接收了仅 crate 内可见的存储接缝，以及尝试拆分方法暂未使用；两者将随旧转发实现删除一并收口。
  - 先从核心删除旧安全端口、仓储端口和私密准备类型，编译红灯准确指向专用模块与通用适配器的旧转发链。
  - 将回填、证明、尝试恢复、接纳、加入和回复重放正文迁入专用保护模块，通用群组接纳只保留不透明重放编号。
  - 第一次基础设施检查的生产代码已编译，剩余红灯集中在仍直接测试已删除旧方法的浅层测试，下一步改为从专用保护接口验证同一行为。
  - 证明绑定、旧数据回填和原子回复测试已改从专用保护接口观察，基础设施全目标检查通过且无新提醒。
  - 基础设施 `legacy_upgrade` 过滤测试 9 项通过；该过滤不包含名称为 `legacy_ready` 和 `legacy_admission` 的两项安全测试，需单独复测。
  - 补跑专用保护接口上的四项安全测试，旧数据回填和原子回复重放均通过。
  - 全仓扫描旧安全端口、仓储端口、私密准备类型、字段和方法名，无残留；实施阶段 2 完成。
  - 应用入口替换为 `AutomaticLegacyUpgrade`，构造依赖已包含发送能力，发现窗口、上线触发、重试节奏和关闭句柄已收入模块。
  - 应用首次检查的生产代码已编译，红灯准确指向尚未迁移的旧协调者集成测试；保留一条任务前已存在的未使用导出提醒。
  - 旧集成测试已移到负责模块旁边，多设备发送替身改为共享内存端点表，不需要为测试保留公开单轮入口。
  - 应用升级场景测试 5 项全部通过，仅保留任务前已存在的 `InboundSnapshotRebuild` 提醒。
  - 网络适配器改为先创建出站能力、再挂入站端点，专用 `assembly/legacy_upgrade.rs` 隐藏循环构造。
  - 主组装已删除 15 秒窗口、30 秒重试、独立建组参数和升级循环，只保留一次安装与一次关闭。
  - 新增运行时生命周期测试，确认离线事件不触发、上线事件立即触发、关闭后订阅释放；应用升级测试现为 6 项通过。
  - `uc-engine` 全目标检查通过，阶段 3 和 4 完成。
  - 将内部布尔参数收紧为私有发现阶段，全仓已无 `allow_isolated_bootstrap` 和旧协调者名称。
  - 扫描旧端口、旧类型、旧字段、旧循环与旧组装结果，无残留；`git diff --check` 通过。
  - 核对无关剪贴板端到端测试差异，仍是恢复时已有的 9 行接收策略断言调整，本次未将它纳入升级重构。
  - 最终核心全量验收通过：171 项单元、13 项密钥周期契约、9 项升级规则和 19 项文档测试。
  - 最终应用全量验收通过：731 项单元测试和 10 项文件传输测试；仅保留一条任务前提醒。
  - 最终基础设施全量验收通过：主测试 622 项通过、4 项跳过，其他契约与端到端测试全部通过，文档测试 16 项通过、3 项跳过。
  - 引擎自身 109 项测试通过；全目标验收在无关剪贴板端到端测试的“恢复接收”断言失败，期望 1、实际 0，本次不修改该并行变更。
  - 完成最终代码自审：自动升级在安装时接管上线事件、发现期、定时重试和关闭；主组装只保留一次安装与一次关闭，顺序无新增生命周期缺口。
  - 发现网络错误路径验证缺口，补齐错误版本、超大消息、未知身份和超时四项测试；定向测试 5 项全部通过。
  - 补测后的基础设施全量验收通过：主测试 626 项通过、4 项跳过，其他契约与端到端测试全部通过，文档测试 16 项通过、3 项跳过。
  - 再次完成全工作区检查、仓库元数据、格式、架构和差异检查；均通过，仅保留任务前已有的 `InboundSnapshotRebuild` 未使用提醒。
  - 再次单独复跑无关剪贴板测试，仍在 `slice2_phase2_clipboard_e2e.rs:850` 失败，期望 1、实际 0；确认它没有因本次重构变化。
  - 设备验收：跳过。本次为内部重构，未执行实体设备矩阵，不计为通过。
  - 严格完成审计重新打开阶段 5：当前真实网络错误路径证据不足，且设计指定的引擎全目标命令在工作区仍失败，不能据此宣称目标完成。
  - 为后台异常可见性增加红灯测试；首次误用精确过滤导致 0 项运行，修正命令后测试按预期因缺少 `task.panicked` 记录失败。
  - 关闭路径现在区分正常取消与异常退出，异常会记录稳定事件和任务名称；红灯测试转绿。
  - 将应用测试中的三套成员、身份、安全和传输替身合并为一个 `UpgradeWorld`，所有七个场景通过最终设备状态验证，定向测试 7 项通过。
  - 新增三项真实回环连接测试；错误版本和超大消息准确复现“拒绝字节写出后连接提前关闭”，客户端只得到连接中断。
  - 网络处理器在所有成功和拒绝回包后继续持有连接直到对端关闭；错误版本和超大消息测试转绿。
  - 未知身份测试首次未发送任何字节，服务端没有观察到新通道并在 10 秒后退出；改为发送完整请求后，真实身份拒绝测试通过且应用入口调用次数为 0。
  - 增加真实成功往返测试，确认已知设备的合法请求会调用应用入口一次并完整返回结果；网络适配器定向测试现为 8 项通过。
  - 增加单设备发现窗口时序测试；初次逐步推进后仍失败，定位到阶段判断使用系统时钟而定时器使用运行时时钟。
  - 统一后台循环时间来源后，测试证明第 15 秒前不建组、窗口结束时自动建组；应用升级场景现为 8 项通过。
  - 将每轮结果从“创建/加入”等过程名称收敛为“当前状态 + 本轮是否改变”，应用场景测试保持通过。
  - 本轮重新执行应用层全量验收：733 项主测试和 10 项文件传输测试全部通过，仅保留任务前已有的 `InboundSnapshotRebuild` 未使用提醒。
  - 本轮重新执行基础设施全量验收：主测试 629 项通过、4 项跳过，其余契约、回环网络和端到端测试全部通过；文档测试 16 项通过、3 项跳过。
  - 在临时隔离工作区中应用本次重构并排除明确禁止修改的 9 行剪贴板测试差异；引擎 109 项单元测试及其余全部集成测试通过，剪贴板接收策略测试仍失败。
  - 在完全干净的当前基线 `9a403d7` 上单独复跑同一剪贴板测试，得到相同的“期望接收 1、实际 0”失败，证明该失败不是本次自动升级重构引入；临时工作区均已清理。
  - 本轮重新执行核心层全量验收：171 项主测试、13 项密钥周期契约、9 项升级规则和 19 项文档测试全部通过。
  - 完成最终仓库检查：元数据、全工作区检查、格式、架构规则和差异检查全部通过；旧协调者、旧仓储端口、旧准备类型、旧安全端口和旧布尔参数扫描均无残留。
  - `design.md` 十项完成标准均有代码与测试证据，Implementation Phase 5 完成。

## 十项完成标准核对

| 标准 | 结论 | 证据摘要 |
|------|------|----------|
| 主组装只安装和关闭 | 完成 | 主组装只调用一次安装并持有一个关闭句柄；发现与重试常量仅在负责模块内 |
| 应用层单一负责入口 | 完成 | `AutomaticLegacyUpgrade` 接管运行事件与已认证请求 |
| 核心只保留规则与契约 | 完成 | 核心无摘要、证明计算和网络编码依赖 |
| 删除公开仓储步骤 | 完成 | 旧仓储端口无残留，未完成尝试存储仅在基础设施内部可见 |
| 删除私密准备类型 | 完成 | 旧公开准备类型无残留，私密加入状态仅在基础设施内部保存 |
| 通用密钥材料解除协议依赖 | 完成 | 只保存不透明重放编号，不再认识升级请求或接纳类型 |
| 通用适配器不负责升级流程 | 完成 | 自动升级实现位于专用安全模块，通用适配器只提供群组材料操作 |
| 旧数据安全回填 | 完成 | 真实加密仓储的回填与重开测试通过 |
| 行为保持 | 完成 | 两台、三台、并发、离线、丢回复、重启、未知成员和密文测试通过 |
| 测试面向负责模块 | 完成 | 场景测试通过负责模块表达行为，不再从组装方重复七步流程 |

## 本轮最终验收

| 检查 | 结果 |
|------|------|
| 核心层全量测试 | 通过：171 项主测试、13 项密钥周期契约、9 项升级规则、19 项文档测试 |
| 应用层全量测试 | 通过：733 项主测试、10 项文件传输测试 |
| 基础设施全量测试 | 通过：629 项主测试，4 项按仓库约定跳过；其余测试全部通过 |
| 引擎全量测试 | 本次范围通过；唯一失败可在干净基线复现，属于方案明确禁止修改的剪贴板接收策略测试，不计为本次重构通过 |
| 仓库交付检查 | 通过：元数据、全工作区检查、格式、架构规则、差异检查、旧结构扫描 |
| 设备验收 | 跳过：未执行实体设备矩阵，不计为通过 |

## Error Log

| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-08-01 | 错误归因无关测试改动 | 1 | 读取具体差异后纠正并排除出方案 |
| 2026-08-01 | 删除核心唯一哈希依赖后锁文件过期 | 1 | 用 Cargo 同步依赖关系后重新带锁验证 |
| 2026-08-01 | 跨四文件补丁末尾 hunk 不完整 | 1 | 补丁未写入，拆成通用依赖、引擎依赖和构造点三步应用 |
| 2026-08-01 | 私有尝试存储测试仍调用旧方法名 | 1 | 替换为 `clear_pending_attempt` 和 `load_pending_attempt` 后复测通过 |
| 2026-08-01 | 记录定向测试时多文件补丁未匹配 | 2 | 两次均未写入任何内容，读取文件尾部后拆分为小补丁 |
| 2026-08-01 | 迁移应用测试后缺少 `HashMap` 导入 | 1 | 补充标准库导入后 5 项场景测试全部通过 |
| 2026-08-01 | 同时替换发现阶段与测试调用时补丁上下文过时 | 1 | 补丁未写入，读取格式化后的实际调用再精确替换 |
| 2026-08-01 | 核心全量测试缺少新显式组标识的导入 | 1 | 补充 `ProtectionGroupId` 导入后重跑全量核心测试 |
| 2026-08-01 | 引擎全目标测试被无关剪贴板策略测试阻断 | 1 | 保留并行变更不修改，单独复跑确认并继续其余验收 |
| 2026-08-01 | 网络适配器缺少四类错误路径验证 | 1 | 增加错误版本、超大消息、未知身份和超时测试，5 项网络定向测试通过 |
| 2026-08-01 | 新增身份测试替身使用了不存在的错误类型 | 1 | 删除自造替身，复用仓库现有身份计算实现后通过 |
| 2026-08-01 | 后台异常红灯命令使用错误的精确过滤 | 1 | 去掉精确匹配后真实运行 1 项，并按预期因缺少异常记录失败 |
| 2026-08-01 | 未知身份网络测试只打开通道但未发送数据 | 1 | 发送完整请求以真正建立远端双向流后，身份拒绝测试通过 |
| 2026-08-01 | 排除并行剪贴板差异后引擎全目标测试仍被旧断言阻断 | 1 | 在完全干净的 `9a403d7` 基线上复跑同一测试并得到相同失败，确认是既有基线问题；保留并行变更且不修改 |
| 2026-08-01 | 规划完成检查脚本没有执行权限，且旧标题格式导致阶段计数为 10/4 | 1 | 改用 `sh` 执行并把实施阶段标题统一为脚本可识别的阶段格式 |
